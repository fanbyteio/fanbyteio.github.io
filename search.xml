<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Python 大数据处理：PySpark RDD 编程基础</title>
    <url>/2026/05/28/Python/PySpark/</url>
    <content><![CDATA[<h1 id="基础准备"><a href="#基础准备" class="headerlink" title="基础准备"></a>基础准备</h1><p>PySpark的编程模型：</p>
<ol>
<li>通过SparkContext对象，完成数据输入</li>
<li>输入数据后得到RDD对象，对RDD对象进行迭代计算</li>
<li>最终通过RDD对象的成员方法，完成数据输出工作</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pyspark <span class="keyword">import</span> SparkConf, SparkContext  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 创建SparkConf对象————链式调用</span></span><br><span class="line">conf = SparkConf().setMaster(<span class="string">&quot;local[*]&quot;</span>).setAppName(<span class="string">&quot;test_spark_app&quot;</span>)  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 以下这段等同于上面这一行的作用,  </span></span><br><span class="line"><span class="comment"># conf = SparkConf()  </span></span><br><span class="line"><span class="comment"># conf.setMaster(&quot;local[*]&quot;)  </span></span><br><span class="line"><span class="comment"># conf.setAppName(&quot;test_spark_app&quot;)  </span></span><br><span class="line">  </span><br><span class="line"><span class="comment"># 基于SparkConf类对象创建SparkContext对象  </span></span><br><span class="line">sc = SparkContext(conf = conf)  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 打印PySpark的运行版本  </span></span><br><span class="line"><span class="built_in">print</span>(sc.version)  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 停止SparkContext对象的运行(停止PySpark程序)  </span></span><br><span class="line">sc.stop()</span><br></pre></td></tr></table></figure>
<p>PySpark的功能都是从SparkContext对象作为开始</p>
<hr>
<h1 id="数据输入"><a href="#数据输入" class="headerlink" title="数据输入"></a>数据输入</h1><p>RDD对象称之为分布式弹性数据集，是PySpark中数据计算的载体，它可以:</p>
<ul>
<li>提供数据存储</li>
<li>提供数据计算的各类方法</li>
<li>数据计算的方法，返回值依旧是RDD(RDD迭代计算)<br>后续对数据进行各类计算，都是基于RDD对象进行</li>
</ul>
<p>输入数据到Spark(即得到RDD对象)：</p>
<ul>
<li>通过SparkContext的parallelize成员方法，将Python数据容器转换为RDD对象</li>
<li>通过SparkContext的textFile成员方法，读取文本文件得到RDD对象</li>
</ul>
<hr>
<h1 id="数据计算"><a href="#数据计算" class="headerlink" title="数据计算"></a>数据计算</h1><h2 id="map方法"><a href="#map方法" class="headerlink" title="map方法"></a>map方法</h2><p>map算子(成员方法)</p>
<ul>
<li>接受一个处理函数，可用lambda表达式快速编写</li>
<li>对RDD内的元素逐个处理，并返回一个新的RDD<br>链式调用</li>
<li>对于返回值是新RDD的算子，可以通过链式调用的方式多次调用算子</li>
</ul>
<h2 id="flatMap方法"><a href="#flatMap方法" class="headerlink" title="flatMap方法"></a>flatMap方法</h2><ul>
<li>计算逻辑和map一样</li>
<li>比map多一个解除一层嵌套的功能。</li>
</ul>
<h2 id="reduceByKey方法"><a href="#reduceByKey方法" class="headerlink" title="reduceByKey方法"></a>reduceByKey方法</h2><ul>
<li>接受一个处理函数，对数据进行两两计算</li>
</ul>
<h2 id="filter方法"><a href="#filter方法" class="headerlink" title="filter方法"></a>filter方法</h2><ul>
<li>接收一个处理函数，可用lambda快速编写</li>
<li>函数对RDD数据逐个处理，得到True的保留至返回值的RDD中</li>
</ul>
<h2 id="distinct方法"><a href="#distinct方法" class="headerlink" title="distinct方法"></a>distinct方法</h2><ul>
<li>完成对RDD内数据的去重操作</li>
</ul>
<h2 id="sortBy方法"><a href="#sortBy方法" class="headerlink" title="sortBy方法"></a>sortBy方法</h2><ul>
<li>接收一个处理函数，可用lambda快速编写</li>
<li>函数表示用来决定排序的依据</li>
<li>可以控制升序或降序</li>
<li>全局排序需要设置分区数为1</li>
</ul>
<hr>
<h1 id="数据输出"><a href="#数据输出" class="headerlink" title="数据输出"></a>数据输出</h1><h2 id="输出为Python对象"><a href="#输出为Python对象" class="headerlink" title="输出为Python对象"></a>输出为Python对象</h2><ul>
<li>collect:将RDD内容转换为list</li>
<li>reduce:对RDD内容进行自定义聚合</li>
<li>take:取出RDD的前N个元素组成list</li>
<li>count:统计RDD元素个数</li>
</ul>
<h2 id="输出到文件中"><a href="#输出到文件中" class="headerlink" title="输出到文件中"></a>输出到文件中</h2><p>RDD输出到文件的方法：</p>
<ul>
<li>rdd.saveAsTextFile(路径)</li>
<li>输出的结果是一个文件夹</li>
<li>有几个分区就输出多少个结果文件<br>如何修改RDD分区：</li>
<li>SparkConf对象设置conf.set(“spark.default.parallelism”, “1”)</li>
<li>创建RDD的时候，sc.parallelize方法传入numSlices参数为1</li>
</ul>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/My-Second-Brain/tree/main/myblog/source/_posts/python-learning/Stage3_PySpark" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>

]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>大数据与分布式计算</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>PySpark</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 基础语法：变量、类型与表达式</title>
    <url>/2026/03/12/Python/%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95/</url>
    <content><![CDATA[<h1 id="字面量"><a href="#字面量" class="headerlink" title="字面量"></a>字面量</h1><p>常用的值的类型：<br><img src="https://raw.githubusercontent.com/NimbusFan/blog-images/main/assets/%E5%B8%B8%E7%94%A8%E7%9A%84%E5%80%BC%E7%9A%84%E7%B1%BB%E5%9E%8B.png" alt="常用的值的类型.png"></p>
<hr>
<h1 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h1><p>单行注释：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 单行注释</span></span><br></pre></td></tr></table></figure>

<p>多行注释：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    多行注释</span></span><br><span class="line"><span class="string">    使用一对三个双引号引起来</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = <span class="number">10</span>   <span class="comment"># 赋值</span></span><br><span class="line">a = a - <span class="number">10</span>  <span class="comment">#运算</span></span><br></pre></td></tr></table></figure>

<p>print语句可以输出多份数据</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(内容<span class="number">1</span>，内容<span class="number">2</span>，......，内容N)</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 直接查看数据类型</span></span><br><span class="line">string_type = <span class="built_in">type</span>(<span class="string">&quot;fanyunfei&quot;</span>)  </span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看变量的数据类型</span></span><br><span class="line">name = <span class="string">&quot;fanyunfei&quot;</span></span><br><span class="line">name_type = <span class="built_in">type</span>(name)  </span><br></pre></td></tr></table></figure>

<hr>
<h1 id="数据类型转换"><a href="#数据类型转换" class="headerlink" title="数据类型转换"></a>数据类型转换</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">int</span>()   <span class="comment"># 转整数</span></span><br><span class="line"><span class="built_in">float</span>()  <span class="comment"># 转浮点数</span></span><br><span class="line"><span class="built_in">str</span>()   <span class="comment"># 转字符串</span></span><br></pre></td></tr></table></figure>

<p>注意：只含有数字的字符串才可转为整数&#x2F;浮点数，但浮点数形式的字符串无法直接转整数</p>
<hr>
<h1 id="标识符"><a href="#标识符" class="headerlink" title="标识符"></a>标识符</h1><h2 id="标识符命名规则："><a href="#标识符命名规则：" class="headerlink" title="标识符命名规则："></a>标识符命名规则：</h2><p>（1）内容限定：</p>
<p>​    只允许出现中文、字母、数字、下划线。</p>
<p>​    不推荐使用中文，不能以数字作为开头</p>
<p>（2）大小写敏感</p>
<p>（3）不可使用关键字</p>
<h2 id="Python所有关键字"><a href="#Python所有关键字" class="headerlink" title="Python所有关键字"></a>Python所有关键字</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> keyword</span><br><span class="line"><span class="built_in">print</span>(keyword.kwlist)</span><br></pre></td></tr></table></figure>

<p>python所有的关键字：</p>
<p>‘False’, ‘None’, ‘True’, ‘and’, ‘as’, ‘assert’, ‘async’, ‘await’, ‘break’, ‘class’, ‘continue’, ‘def’, ‘del’, ‘elif’, ‘else’, ‘except’, ‘finally’, ‘for’, ‘from’, ‘global’, ‘if’, ‘import’, ‘in’, ‘is’, ‘lambda’, ‘nonlocal’, ‘not’, ‘or’, ‘pass’, ‘raise’, ‘return’, ‘try’, ‘while’, ‘with’, ‘yield’</p>
<hr>
<h1 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h1><h2 id="基础运算符"><a href="#基础运算符" class="headerlink" title="基础运算符"></a>基础运算符</h2><table>
<thead>
<tr>
<th align="center">+</th>
<th align="center">加</th>
</tr>
</thead>
<tbody><tr>
<td align="center">-</td>
<td align="center">减</td>
</tr>
<tr>
<td align="center">*</td>
<td align="center">乘</td>
</tr>
<tr>
<td align="center">&#x2F;</td>
<td align="center">除</td>
</tr>
<tr>
<td align="center">&#x2F;&#x2F;</td>
<td align="center">整除</td>
</tr>
<tr>
<td align="center">%</td>
<td align="center">取余</td>
</tr>
<tr>
<td align="center">**</td>
<td align="center">指数</td>
</tr>
</tbody></table>
<h2 id="复合赋值运算符"><a href="#复合赋值运算符" class="headerlink" title="复合赋值运算符"></a>复合赋值运算符</h2><table>
<thead>
<tr>
<th align="center">+&#x3D;</th>
<th align="center">c +&#x3D; a等效于c &#x3D; c + a</th>
</tr>
</thead>
<tbody><tr>
<td align="center">-&#x3D;</td>
<td align="center">c -&#x3D; a等效于c &#x3D; c - a</td>
</tr>
<tr>
<td align="center">*&#x3D;</td>
<td align="center">c *&#x3D; a等效于c &#x3D; c * a</td>
</tr>
<tr>
<td align="center">&#x2F;&#x3D;</td>
<td align="center">c &#x2F;&#x3D; a等效于c &#x3D; c &#x2F; a</td>
</tr>
<tr>
<td align="center">&#x2F;&#x2F;&#x3D;</td>
<td align="center">c &#x2F;&#x2F;&#x3D; a等效于c &#x3D; c &#x2F;&#x2F; a</td>
</tr>
<tr>
<td align="center">%&#x3D;</td>
<td align="center">c %&#x3D; a等效于c &#x3D; c % a</td>
</tr>
<tr>
<td align="center">**&#x3D;</td>
<td align="center">c **&#x3D; a等效于c &#x3D; c ** a</td>
</tr>
</tbody></table>
<h2 id="多变量赋值"><a href="#多变量赋值" class="headerlink" title="多变量赋值"></a>多变量赋值</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">left, right = <span class="number">0</span>, <span class="built_in">len</span>(nums) - <span class="number">1</span>   </span><br></pre></td></tr></table></figure>
<p>等价于：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">left = <span class="number">0</span></span><br><span class="line">right = <span class="built_in">len</span>(nums) - <span class="number">1</span></span><br></pre></td></tr></table></figure>
<p>也可用于交换变量：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a, b = b, a</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h1><h2 id="字符串的三种定义方式"><a href="#字符串的三种定义方式" class="headerlink" title="字符串的三种定义方式"></a>字符串的三种定义方式</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name = <span class="string">&#x27;fanyunfei&#x27;</span>  <span class="comment"># 单引号定义法</span></span><br><span class="line">name = <span class="string">&quot;fanyunfei&quot;</span>  <span class="comment">#双引号定义法</span></span><br><span class="line">name = <span class="string">&quot;&quot;&quot;fanyunfei&quot;&quot;&quot;</span>  <span class="comment">#三引号定义法，如果被变量接受，就作为字符串，如果不被接受就作为注释</span></span><br></pre></td></tr></table></figure>

<p>引号的嵌套：（1）可以使用\进行转义，解除引号作用，变成普通字符串（2）单引号内可以写双引号，双引号内可以写单引号</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">&quot;Hello\nWorld&quot;</span>)    <span class="comment"># \n,换行符</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;Hello\tWorld&quot;</span>)    <span class="comment"># \t,制表符</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;Hello&quot;</span>，end = <span class="string">&#x27;&#x27;</span>)  <span class="comment"># print输出后不换行</span></span><br></pre></td></tr></table></figure>

<h2 id="字符串拼接"><a href="#字符串拼接" class="headerlink" title="字符串拼接"></a>字符串拼接</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">&quot;fanyunfei&quot;</span> + <span class="string">&quot;英俊潇洒风流倜傥有钱多金智商高&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>注意：此方法无法和非字符串类型进行拼接</p>
<h2 id="字符串格式化"><a href="#字符串格式化" class="headerlink" title="字符串格式化"></a>字符串格式化</h2><p>通过占位的形式拼接字符串。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name = <span class="string">&quot;pitejingji&quot;</span></span><br><span class="line">set_up_year = <span class="number">2026</span></span><br><span class="line">stock_price = <span class="number">19.99</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;%s成立于 %d年，今天的股价是%f&quot;</span> %(name,set_up_year,stock_price))</span><br></pre></td></tr></table></figure>

<h2 id="字符串格式化的精度控制"><a href="#字符串格式化的精度控制" class="headerlink" title="字符串格式化的精度控制"></a>字符串格式化的精度控制</h2><p>使用辅助符号”m.n”来控制数据的宽度和精度</p>
<p>m，控制宽度，要求是数字(很少使用)，如果设置的宽度小于数字自身，不生效</p>
<p>.n，控制小数点精度，要求是数字，会进行小数的四舍五入</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">num1 = <span class="number">12.3456</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;数字12.34宽度限制7，小数精度3，结果是：%7.3f&quot;</span> %num1)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;数字12.34宽度限制10，小数精度6，结果是：%10.6f&quot;</span> %num1)</span><br></pre></td></tr></table></figure>

<h2 id="快速字符串格式化"><a href="#快速字符串格式化" class="headerlink" title="快速字符串格式化"></a>快速字符串格式化</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;<span class="subst">&#123;name&#125;</span>成立于<span class="subst">&#123;set_up_year&#125;</span>年，今天的股价是<span class="subst">&#123;stock_price&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>注意：此方法不会做精度控制。</p>
<h2 id="对表达式进行格式化"><a href="#对表达式进行格式化" class="headerlink" title="对表达式进行格式化"></a>对表达式进行格式化</h2><p>表达式指一个具有明确结果的代码语句。如1+1，type(“fanyunfei”)。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">&quot;1 + 1的结果是：%d&quot;</span> %(<span class="number">1</span> + <span class="number">1</span>))</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;1 + 1的结果是：<span class="subst">&#123;<span class="number">1</span> +<span class="number">1</span>&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;字符串在python中的类型是：%s&quot;</span> %<span class="built_in">type</span>(<span class="string">&quot;fanyunfei&quot;</span>))</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="数据输入"><a href="#数据输入" class="headerlink" title="数据输入"></a>数据输入</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">input</span>()    <span class="comment">#获取键盘输入的数据</span></span><br><span class="line"><span class="built_in">input</span>(提示信息)    <span class="comment"># 在使用者输入内容之前显示提示信息</span></span><br></pre></td></tr></table></figure>

<p>注意：input得到的数据永远都是字符串类型。</p>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/01_Data_Type" title="fab fa-github"><span>fab fa-github</span></a></p>
</div>

]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>基础语法</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 函数基础：定义、参数与返回值</title>
    <url>/2026/03/15/Python/%E5%87%BD%E6%95%B0%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h1 id="函数的基础定义"><a href="#函数的基础定义" class="headerlink" title="函数的基础定义"></a>函数的基础定义</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">函数名</span>(<span class="params">传入参数</span>):</span><br><span class="line">    函数体</span><br><span class="line">    <span class="keyword">return</span> 返回值</span><br></pre></td></tr></table></figure>

<p>注意：</p>
<ul>
<li>参数&#x2F;返回值如不需要，可以省略</li>
<li>如果不使用return，函数会返回None，类型是：&lt;class ‘NoneType’&gt;</li>
<li>函数必须先定义后使用</li>
</ul>
<hr>
<h1 id="函数的传入参数"><a href="#函数的传入参数" class="headerlink" title="函数的传入参数"></a>函数的传入参数</h1><ul>
<li>函数定义中的参数为<strong>形式参数</strong></li>
<li>函数调用中的参数为<strong>实际参数</strong></li>
<li>函数的参数数量不限(0~N)，使用逗号分隔开</li>
<li>传入参数的时候，要和形式参数一一对应，逗号隔开?</li>
</ul>
<hr>
<h1 id="函数返回值"><a href="#函数返回值" class="headerlink" title="函数返回值"></a>函数返回值</h1><h2 id="函数返回值的定义"><a href="#函数返回值的定义" class="headerlink" title="函数返回值的定义"></a>函数返回值的定义</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">函数</span>(<span class="params">参数...</span>):</span><br><span class="line">    函数体</span><br><span class="line">    <span class="keyword">return</span> 返回值</span><br><span class="line"></span><br><span class="line"><span class="comment"># 通过return关键字向调用者返回数据</span></span><br><span class="line">变量=函数(参数)</span><br></pre></td></tr></table></figure>

<p>函数体在遇到return后就结束，写在return后的代码不会执行</p>
<h2 id="None类型"><a href="#None类型" class="headerlink" title="None类型"></a>None类型</h2><h3 id="什么是None"><a href="#什么是None" class="headerlink" title="什么是None"></a>什么是None</h3><p>None是类型’NoneType’的字面量，用于表示:空的、无意义的</p>
<h3 id="函数如何返回None"><a href="#函数如何返回None" class="headerlink" title="函数如何返回None"></a>函数如何返回None</h3><ul>
<li>不使用return语句即返回None</li>
<li>主动return None</li>
</ul>
<h3 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h3><ul>
<li><p>用在函数无返回值上</p>
</li>
<li><p>用在if判断上：在if判断中，None等同于False；一般用于在函数中主动返回None，配合if判断做相关处理</p>
</li>
<li><p>用于声明无内容的变量上:定义变量但暂时不需要变量有具体值，可以用None来代替</p>
</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">• <span class="comment">#暂不赋予变量具体值</span></span><br><span class="line">• name = <span class="literal">None</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="函数说明文档"><a href="#函数说明文档" class="headerlink" title="函数说明文档"></a>函数说明文档</h1><h2 id="函数说明文档的作用："><a href="#函数说明文档的作用：" class="headerlink" title="函数说明文档的作用："></a>函数说明文档的作用：</h2><p>对函数进行说明解释，帮助更好理解函数的功能</p>
<h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">func</span>(<span class="params">x,y</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    函数说明</span></span><br><span class="line"><span class="string">    :param x:参数x的说明</span></span><br><span class="line"><span class="string">    :param x:参数y的说明</span></span><br><span class="line"><span class="string">    :return:返回值的说明</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    函数体</span><br><span class="line">    <span class="keyword">return</span> 返回值</span><br></pre></td></tr></table></figure>

<p>:param用于解释参数（parameter，参数）</p>
<p>:return用于解释返回值</p>
<hr>
<h1 id="函数的嵌套调用"><a href="#函数的嵌套调用" class="headerlink" title="函数的嵌套调用"></a>函数的嵌套调用</h1><p>函数A中执行到调用函数B的语句，会将函数B全部执行完成后，继续执行函数A的剩余内容</p>
<hr>
<h1 id="变量的作用域"><a href="#变量的作用域" class="headerlink" title="变量的作用域"></a>变量的作用域</h1><h2 id="变量作用域"><a href="#变量作用域" class="headerlink" title="变量作用域"></a>变量作用域</h2><p>变量作用域指的是变量的作用范围</p>
<p>变量主要分为两类:<strong>局部变量</strong>和<strong>全局变量</strong></p>
<p><strong>局部变量</strong>是定义在函数体内部的变量，只在函数体内部生效</p>
<p><strong>全局变量</strong>是在函数体内、外都能生效的变量</p>
<h2 id="global关键字"><a href="#global关键字" class="headerlink" title="global关键字"></a>global关键字</h2><p><code>global</code> 用于在函数内部声明某个变量来自全局作用域，从而允许在函数内部修改该全局变量。</p>
<hr>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/04_Functions" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>函数</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 数据库编程：SQL 语法与 MySQL 实践</title>
    <url>/2026/05/21/Python/SQL/</url>
    <content><![CDATA[<h1 id="基础命令"><a href="#基础命令" class="headerlink" title="基础命令"></a>基础命令</h1><p>SQL语言分类：</p>
<ul>
<li>DDL (Data Definition Language)数据定义</li>
<li>DML (Data Manipulation Language)数据操作</li>
<li>DCL(Data Control Language)数据控制</li>
<li>DQL (Data Query Language)数据查询</li>
</ul>
<p>MySQL命令行环境下的基础命令:</p>
<table>
<thead>
<tr>
<th align="center">SQL命令</th>
<th align="center">结果</th>
</tr>
</thead>
<tbody><tr>
<td align="center">mysql -uroot -p</td>
<td align="center">以root用户，通过密码进行登录</td>
</tr>
<tr>
<td align="center">show databases；</td>
<td align="center">查看有哪些数据库</td>
</tr>
<tr>
<td align="center">use 数据库名;</td>
<td align="center">使用某个数据库</td>
</tr>
<tr>
<td align="center">show tables;</td>
<td align="center">查看数据库内有哪些表</td>
</tr>
<tr>
<td align="center">exit</td>
<td align="center">退出MySQL的命令行环境</td>
</tr>
</tbody></table>
<hr>
<h1 id="SQL的语法特征"><a href="#SQL的语法特征" class="headerlink" title="SQL的语法特征"></a>SQL的语法特征</h1><ol>
<li>大小写不敏感</li>
<li>可以单行或多行书写，最后以<code>;</code>号结束</li>
<li>SQL支持注释:<ul>
<li>单行注释：– 注释内容(–后面一定要有一个空格)</li>
<li>单行注释：# 注释内容(#后面可以不加空格，推荐加上)</li>
<li>多行注释：<code>/* 注释内容 */</code></li>
</ul>
</li>
</ol>
<hr>
<h1 id="DDL-数据定义"><a href="#DDL-数据定义" class="headerlink" title="DDL-数据定义"></a>DDL-数据定义</h1><h2 id="库管理"><a href="#库管理" class="headerlink" title="库管理"></a>库管理</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看数据库</span></span><br><span class="line"><span class="keyword">SHOW</span> DATABASES;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 使用数据库</span></span><br><span class="line">USE 数据库名称;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 创建数据库</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE 数据库名称[CHARSET UTF8];</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除数据库</span></span><br><span class="line"><span class="keyword">DROP</span> DATABASE 数据库名称;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看当前使用的数据库</span></span><br><span class="line"><span class="keyword">SELECT</span> DATABASE();</span><br></pre></td></tr></table></figure>
<h2 id="表管理"><a href="#表管理" class="headerlink" title="表管理"></a>表管理</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看有哪些表(需要先选择数据库)</span></span><br><span class="line"><span class="keyword">SHOW</span> TABLES；</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 创建表</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> 表名称(</span><br><span class="line">	列名称 列类型，</span><br><span class="line">	列名称 列类型，</span><br><span class="line">	......</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除表</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> 表名称;</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> 表名称;</span><br></pre></td></tr></table></figure>
<p>列类型：</p>
<table>
<thead>
<tr>
<th align="center">列名称</th>
<th align="center">列类型</th>
</tr>
</thead>
<tbody><tr>
<td align="center">int</td>
<td align="center">整数</td>
</tr>
<tr>
<td align="center">float</td>
<td align="center">浮点数</td>
</tr>
<tr>
<td align="center">varchar(长度)</td>
<td align="center">文本，长度为数字，做最大长度限制，最大可填255</td>
</tr>
<tr>
<td align="center">date</td>
<td align="center">日期类型</td>
</tr>
<tr>
<td align="center">timestamp</td>
<td align="center">时间戳类型</td>
</tr>
</tbody></table>
<hr>
<h1 id="DML-数据操作"><a href="#DML-数据操作" class="headerlink" title="DML-数据操作"></a>DML-数据操作</h1><h2 id="INSERT数据插入"><a href="#INSERT数据插入" class="headerlink" title="INSERT数据插入"></a>INSERT数据插入</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"># []代表可以省略的内容</span><br><span class="line"><span class="keyword">INSERT INTO</span> 表[(列<span class="number">1</span>, 列<span class="number">2</span>, ..., 列N)] <span class="keyword">VALUES</span>(值<span class="number">1</span>, 值<span class="number">2</span>, ..., 值N)[, (值<span class="number">1</span>, 值<span class="number">2</span>, ..., 值N),...,(值<span class="number">1</span>, 值<span class="number">2</span>, ..., 值N)]</span><br></pre></td></tr></table></figure>
<h2 id="DELETE数据删除"><a href="#DELETE数据删除" class="headerlink" title="DELETE数据删除"></a>DELETE数据删除</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"># []代表可以省略的内容</span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> 表名称 [<span class="keyword">WHERE</span>条件判断]</span><br><span class="line"></span><br><span class="line"><span class="comment">/*  </span></span><br><span class="line"><span class="comment">条件判断：列 操作符 值</span></span><br><span class="line"><span class="comment">操作符： = &lt; &gt; &lt;= &gt;= !=等等，</span></span><br><span class="line"><span class="comment">如：id = 5  id &lt; 3  id &gt;= 6  id != 5</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>
<h2 id="UPDATE-数据更新"><a href="#UPDATE-数据更新" class="headerlink" title="UPDATE 数据更新"></a>UPDATE 数据更新</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> 表名 <span class="keyword">SET</span> 列<span class="operator">=</span>值 [<span class="keyword">WHERE</span> 条件判断]</span><br></pre></td></tr></table></figure>
<p><strong>注意：字符串的值出现在SQL语句中，必须要用单引号包围起来。</strong></p>
<hr>
<h1 id="DCL-数据控制"><a href="#DCL-数据控制" class="headerlink" title="DCL-数据控制"></a>DCL-数据控制</h1><hr>
<h1 id="DQL-数据查询"><a href="#DQL-数据查询" class="headerlink" title="DQL-数据查询"></a>DQL-数据查询</h1><h2 id="基础查询"><a href="#基础查询" class="headerlink" title="基础查询"></a>基础查询</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"># 基础查询的语法</span><br><span class="line"><span class="keyword">SELECT</span> 字段列表<span class="operator">|</span><span class="operator">*</span><span class="keyword">FROM</span> 表</span><br><span class="line"># 过滤查询的语法</span><br><span class="line"><span class="keyword">SELECT</span> 字段列表<span class="operator">|</span><span class="operator">*</span><span class="keyword">FROM</span> 表 <span class="keyword">WHERE</span> 条件判断</span><br></pre></td></tr></table></figure>
<h2 id="分组聚合"><a href="#分组聚合" class="headerlink" title="分组聚合"></a>分组聚合</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 字段<span class="operator">|</span>聚合函数 <span class="keyword">FROM</span> 表 [<span class="keyword">WHERE</span> 条件] <span class="keyword">GROUP</span> <span class="keyword">BY</span> 列 </span><br><span class="line">聚合函数有:</span><br><span class="line"><span class="operator">-</span> <span class="built_in">SUM</span>(列) 求和</span><br><span class="line"><span class="operator">-</span> <span class="built_in">AVG</span>(列) 求平均值</span><br><span class="line"><span class="operator">-</span> <span class="built_in">MIN</span>(列) 求最小值</span><br><span class="line"><span class="operator">-</span> <span class="built_in">MAX</span>(列) 求最大值</span><br><span class="line"><span class="operator">-</span> <span class="built_in">COUNT</span>(列<span class="operator">|</span><span class="operator">*</span>) 求数量</span><br></pre></td></tr></table></figure>
<p>注意：</p>
<ul>
<li>一个SQL中可以写多个聚合</li>
<li>GROUP BY中出现了哪个列，哪个列才能出现在SELECT中的非聚合中</li>
</ul>
<h2 id="排序分页"><a href="#排序分页" class="headerlink" title="排序分页"></a>排序分页</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"># 只写n表示：只显示几条数据</span><br><span class="line"># n和m都写表示：跳过前n行，从第n <span class="operator">+</span> <span class="number">1</span>条开始往下取m条</span><br><span class="line"><span class="keyword">SELECT</span> 列<span class="operator">|</span>聚合函数<span class="operator">|</span><span class="operator">*</span> <span class="keyword">FROM</span> 表</span><br><span class="line"><span class="keyword">WHERE</span> ...</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> ...</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> ... [<span class="keyword">ASC</span> <span class="operator">|</span> <span class="keyword">DESC</span>]</span><br><span class="line">LIMIT n[, m]</span><br></pre></td></tr></table></figure>
<p>注意：</p>
<ul>
<li>WHERE、GROUP BY、ORDER BY、LIMIT均可按需求省略</li>
<li>SELECT和FROM是必写的</li>
<li>执行顺序:FROM -&gt;WHERE -&gt;GROUP BY和聚合函数 -&gt;SELECT -&gt;ORDER BY -&gt;LIMIT</li>
</ul>
<hr>
<h1 id="Python操作MySQL"><a href="#Python操作MySQL" class="headerlink" title="Python操作MySQL"></a>Python操作MySQL</h1><h2 id="基础使用"><a href="#基础使用" class="headerlink" title="基础使用"></a>基础使用</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导包</span></span><br><span class="line"><span class="keyword">from</span> pymysql <span class="keyword">import</span> Connection</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取链接对象</span></span><br><span class="line">链接对象 = Connection(主机，端口，账户，密码)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 得到游标对象</span></span><br><span class="line">游标对象 = 链接对象.cursor()</span><br><span class="line">链接对象.select_db(<span class="string">&quot;&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行SQL语句</span></span><br><span class="line">游标对象.execute(<span class="string">&quot;&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 得到全部的查询结果（封装在元组内）</span></span><br><span class="line">results = 游标对象.fetchall()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 关闭和数据库的连接</span></span><br><span class="line">链接对象.close()</span><br></pre></td></tr></table></figure>
<h2 id="数据插入"><a href="#数据插入" class="headerlink" title="数据插入"></a>数据插入</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导包</span></span><br><span class="line"><span class="keyword">from</span> pymysql <span class="keyword">import</span> Connection</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取链接对象</span></span><br><span class="line">链接对象 = Connection(主机，端口，账户，密码)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 得到游标对象</span></span><br><span class="line">游标对象 = conn.cursor()</span><br><span class="line">链接对象.select_db(<span class="string">&quot;&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 执行SQL语句 </span></span><br><span class="line">游标对象.execute(<span class="string">&quot;&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 确认提交</span></span><br><span class="line">链接对象.commit()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 关闭和数据库的连接</span></span><br><span class="line">链接对象.close()</span><br></pre></td></tr></table></figure>
<p>也可以通过代码设置自动提交，无需手动commit</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">链接对象 = Connection(</span><br><span class="line">	主机，</span><br><span class="line">	端口，</span><br><span class="line">	账户，</span><br><span class="line">	密码</span><br><span class="line">	autocommit = <span class="literal">True</span>	<span class="comment"># 设置自动提交</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage2_OOP/02_SQL" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>
]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>数据库与后端开发</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>SQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 函数进阶：作用域、参数与匿名函数</title>
    <url>/2026/03/17/Python/%E5%87%BD%E6%95%B0%E8%BF%9B%E9%98%B6/</url>
    <content><![CDATA[<h1 id="函数多返回值"><a href="#函数多返回值" class="headerlink" title="函数多返回值"></a>函数多返回值</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test_return</span>():</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>, <span class="string">&quot;Hello&quot;</span>, <span class="literal">True</span></span><br><span class="line">x, y, z = test_return()</span><br><span class="line"><span class="built_in">print</span>(x, y, z)</span><br></pre></td></tr></table></figure>

<h1 id="函数多种传参方式"><a href="#函数多种传参方式" class="headerlink" title="函数多种传参方式"></a>函数多种传参方式</h1><h2 id="位置参数"><a href="#位置参数" class="headerlink" title="位置参数"></a>位置参数</h2><p>调用函数时根据函数定义的参数位置来传递参数</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">name, age, gender</span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;姓名是：<span class="subst">&#123;name&#125;</span>,年龄是：<span class="subst">&#123;age&#125;</span>,性别是：<span class="subst">&#123;gender&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 位置参数</span></span><br><span class="line">user_info(<span class="string">&#x27;小明&#x27;</span>, <span class="number">20</span>, <span class="string">&#x27;男&#x27;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="关键字参数"><a href="#关键字参数" class="headerlink" title="关键字参数"></a>关键字参数</h2><p>调用函数时通过“键 &#x3D; 值”的形式传递参数，消除参数的顺序需求</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">name, age, gender</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;您的名字是:<span class="subst">&#123;name&#125;</span>，年龄是:<span class="subst">&#123;age&#125;</span>,性别是:<span class="subst">&#123;gender&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">#关键字传参</span></span><br><span class="line">user_info(name=<span class="string">&quot;小明&quot;</span>,age=<span class="number">20</span>,gender=<span class="string">&quot;男&quot;</span>)</span><br><span class="line"><span class="comment"># 可以不按照固定顺序</span></span><br><span class="line">user_info(age=<span class="number">20</span>,gender=<span class="string">&quot;男&quot;</span>,name=<span class="string">&quot;小明&quot;</span>)</span><br><span class="line"><span class="comment">#可以和位置参数混用，位置参数必须在前，且匹配参数顺序</span></span><br><span class="line">user_info(<span class="string">&quot;小明&quot;</span>,age=<span class="number">20</span>,gender=<span class="string">&quot;男&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="不定长参数"><a href="#不定长参数" class="headerlink" title="不定长参数"></a>不定长参数</h2><p>不定长参数也叫可变参数.用于不确定调用的时候会传递多少个参数(不传参也可以)的场景</p>
<p>不定长参数的类型:</p>
<h3 id="（1）位置传递"><a href="#（1）位置传递" class="headerlink" title="（1）位置传递"></a>（1）位置传递</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">*args</span>):</span><br><span class="line">    <span class="built_in">print</span>(args)</span><br><span class="line"></span><br><span class="line"><span class="comment"># (&#x27;TOM&#x27;,)</span></span><br><span class="line">user_info(<span class="string">&#x27;TOM&#x27;</span>)</span><br><span class="line"><span class="comment"># (&#x27;TOM&#x27;, 18)</span></span><br><span class="line">user_info(<span class="string">&#x27;TOM&#x27;</span>, <span class="number">18</span>)</span><br></pre></td></tr></table></figure>

<p>注意:传进的所有参数都会被args变量收集，它会根据传进参数的位置合并为一个元组(tuple)，args是元组类型</p>
<h3 id="（2）关键字传递"><a href="#（2）关键字传递" class="headerlink" title="（2）关键字传递"></a>（2）关键字传递</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">**kwargs</span>):</span><br><span class="line">    <span class="built_in">print</span>(kwargs)</span><br><span class="line"></span><br><span class="line"><span class="comment"># &#123;&#x27;name&#x27; : &#x27;TOM&#x27;, &#x27;age&#x27; : 18, &#x27;id&#x27; : 110&#125;</span></span><br><span class="line">user_info(name=<span class="string">&#x27;TOM&#x27;</span>, age=<span class="number">18</span>, <span class="built_in">id</span>=<span class="number">110</span>)</span><br></pre></td></tr></table></figure>

<p>传入的参数是“键&#x3D;值”格式，被kwargs接收后根据“键&#x3D;值”组成字典</p>
<h2 id="缺省参数"><a href="#缺省参数" class="headerlink" title="缺省参数"></a>缺省参数</h2><p>缺省参数也叫默认参数，调用函数时可不传该默认参数的值,如果传入该参数的值，则修改默认参数值</p>
<p>注意:<strong>函数定义和调用时，所有位置参数必须出现在默认参数前</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">name,age,gender = <span class="string">&#x27;男&#x27;</span></span>):</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;您的名字是<span class="subst">&#123;name&#125;</span>，年龄是<span class="subst">&#123;age&#125;</span>，性别是<span class="subst">&#123;gender&#125;</span>&#x27;</span>)</span><br><span class="line">user_info(<span class="string">&#x27;TOM&#x27;</span>, <span class="number">20</span>)</span><br><span class="line">user_info(<span class="string">&#x27;Rose&#x27;</span>, <span class="number">18</span>, <span class="string">&#x27;女&#x27;</span>)</span><br></pre></td></tr></table></figure>

<h1 id="匿名函数"><a href="#匿名函数" class="headerlink" title="匿名函数"></a>匿名函数</h1><h2 id="函数作为参数传递"><a href="#函数作为参数传递" class="headerlink" title="函数作为参数传递"></a>函数作为参数传递</h2><p>函数本身作为参数，传入另一个函数中进行使用。</p>
<p>将函数传入的作用在于:传入计算逻辑，而非传入数据。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test_func</span>(<span class="params">func</span>):</span><br><span class="line">    result = func(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;func参数的类型是”：<span class="subst">&#123;func&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;计算结果：<span class="subst">&#123;result&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">compute</span>(<span class="params">x, y</span>):</span><br><span class="line">    <span class="keyword">return</span> x + y</span><br><span class="line"></span><br><span class="line">test_func(compute)</span><br></pre></td></tr></table></figure>

<h2 id="lambda匿名函数"><a href="#lambda匿名函数" class="headerlink" title="lambda匿名函数"></a>lambda匿名函数</h2><p>函数的定义中:</p>
<ul>
<li>def关键字，可以定义带有名称的函数,可以基于名称<strong>重复使用</strong></li>
<li>lambda关键字，可以定义匿名函数(无名称),只可<strong>临时使用一次</strong>。</li>
</ul>
<p>匿名函数定义语法:</p>
<p>lambda 传入参数:函数体(一行代码)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test_func</span>(<span class="params">compute</span>):</span><br><span class="line">    result = compute(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">    <span class="built_in">print</span>(result)</span><br><span class="line"></span><br><span class="line">test_func(<span class="keyword">lambda</span> x, y: x + y)        <span class="comment">#结果:3</span></span><br></pre></td></tr></table></figure>

<p>注意事项：</p>
<ul>
<li>匿名函数用于临时构建一个函数，只用一次的场景</li>
<li>匿名函数的定义中，函数体只能写一行代码，如果函数体要写多行代码，不可用lambda匿名函数，应使用def定义带名函数</li>
</ul>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/06_Advanced_Functions" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>函数进阶</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 常用内置函数与基础操作</title>
    <url>/2026/08/01/Python/%E5%B8%B8%E7%94%A8%E5%86%85%E7%BD%AE%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h1 id="常用内置函数"><a href="#常用内置函数" class="headerlink" title="常用内置函数"></a>常用内置函数</h1><h2 id="len"><a href="#len" class="headerlink" title="len()"></a><code>len()</code></h2><p>获取字符串、列表、集合等对象的长度。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">len</span>(nums)</span><br><span class="line"><span class="built_in">len</span>(s)</span><br></pre></td></tr></table></figure>

<h2 id="enumerate"><a href="#enumerate" class="headerlink" title="enumerate()"></a><code>enumerate()</code></h2><p>同时获得元素的下标和值（枚举）。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> i, num <span class="keyword">in</span> <span class="built_in">enumerate</span>(nums):</span><br><span class="line">    <span class="built_in">print</span>(i, num)</span><br></pre></td></tr></table></figure>

<h2 id="max-和-min"><a href="#max-和-min" class="headerlink" title="max() 和 min()"></a><code>max()</code> 和 <code>min()</code></h2><p>求最大值和最小值。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">max</span>(<span class="number">3</span>, <span class="number">5</span>)          <span class="comment"># 5</span></span><br><span class="line"><span class="built_in">min</span>(<span class="number">3</span>, <span class="number">5</span>)          <span class="comment"># 3</span></span><br><span class="line"><span class="built_in">max</span>([<span class="number">1</span>, <span class="number">3</span>, <span class="number">2</span>])     <span class="comment"># 3</span></span><br></pre></td></tr></table></figure>

<h2 id="sorted"><a href="#sorted" class="headerlink" title="sorted()"></a><code>sorted()</code></h2><p>返回一个新的排序结果，不修改原对象。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">nums = [<span class="number">3</span>, <span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line">new_nums = <span class="built_in">sorted</span>(nums)     <span class="comment"># [1, 2, 3]</span></span><br></pre></td></tr></table></figure>

<p>降序排列：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">new_nums1 = <span class="built_in">sorted</span>(nums, reverse=<span class="literal">True</span>)      <span class="comment"># [3, 2, 1]</span></span><br></pre></td></tr></table></figure>

<p>字符串排序：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">new_nums2 = <span class="built_in">sorted</span>(<span class="string">&quot;eat&quot;</span>)       <span class="comment"># [&#x27;a&#x27;, &#x27;e&#x27;, &#x27;t&#x27;]</span></span><br></pre></td></tr></table></figure>

<h2 id="ord"><a href="#ord" class="headerlink" title="ord()"></a><code>ord()</code></h2><p>将字符转换成对应的 Unicode 编码数字。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ord</span>(<span class="string">&#x27;a&#x27;</span>)</span><br><span class="line"><span class="built_in">ord</span>(<span class="string">&#x27;b&#x27;</span>)</span><br></pre></td></tr></table></figure>

<p>常用来把小写字母映射到 <code>0～25</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ord</span>(<span class="string">&#x27;a&#x27;</span>) - <span class="built_in">ord</span>(<span class="string">&#x27;a&#x27;</span>)  <span class="comment"># 0</span></span><br><span class="line"><span class="built_in">ord</span>(<span class="string">&#x27;b&#x27;</span>) - <span class="built_in">ord</span>(<span class="string">&#x27;a&#x27;</span>)  <span class="comment"># 1</span></span><br><span class="line"><span class="built_in">ord</span>(<span class="string">&#x27;c&#x27;</span>) - <span class="built_in">ord</span>(<span class="string">&#x27;a&#x27;</span>)  <span class="comment"># 2</span></span><br></pre></td></tr></table></figure>

<h2 id="zip"><a href="#zip" class="headerlink" title="zip()"></a><code>zip()</code></h2><p>同时遍历多个可迭代对象。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> h, pre, suf <span class="keyword">in</span> <span class="built_in">zip</span>(height, pre_max, suf_max):</span><br><span class="line">    <span class="built_in">print</span>(h, pre, suf)</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">b = [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br><span class="line"></span><br><span class="line"><span class="built_in">list</span>(<span class="built_in">zip</span>(a, b))      <span class="comment"># [(1, 4), (2, 5), (3, 6)]</span></span><br></pre></td></tr></table></figure>

<p>注意：如果几个列表长度不同，<code>zip()</code> 会在最短列表结束时停止。</p>
<h2 id="sort"><a href="#sort" class="headerlink" title="sort()"></a><code>sort()</code></h2><p>直接修改原列表，返回值是 <code>None</code>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">nums.sort()</span><br></pre></td></tr></table></figure>

<h2 id="sum"><a href="#sum" class="headerlink" title="sum()"></a><code>sum()</code></h2><p>求和：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sum</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>])      <span class="comment"># 6</span></span><br></pre></td></tr></table></figure>

<h2 id="abs"><a href="#abs" class="headerlink" title="abs()"></a><code>abs()</code></h2><p>求绝对值：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">abs(-5)      # 5</span><br></pre></td></tr></table></figure>

<h2 id="reversed"><a href="#reversed" class="headerlink" title="reversed()"></a><code>reversed()</code></h2><p>倒序遍历：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> num <span class="keyword">in</span> <span class="built_in">reversed</span>(nums):       <span class="comment"># 不会修改原列表</span></span><br><span class="line">    <span class="built_in">print</span>(num)</span><br></pre></td></tr></table></figure>

<p>转换成列表：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">list</span>(<span class="built_in">reversed</span>(nums))</span><br></pre></td></tr></table></figure>

<h2 id="any"><a href="#any" class="headerlink" title="any()"></a><code>any()</code></h2><p>只要有一个条件为真，就返回 <code>True</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">any</span>([<span class="literal">False</span>, <span class="literal">True</span>, <span class="literal">False</span>])       <span class="comment"># True</span></span><br></pre></td></tr></table></figure>

<h2 id="all"><a href="#all" class="headerlink" title="all()"></a><code>all()</code></h2><p>所有条件都为真，才返回 <code>True</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">all</span>([<span class="literal">True</span>, <span class="literal">True</span>, <span class="literal">False</span>])        <span class="comment"># False</span></span><br></pre></td></tr></table></figure>

<h1 id="字典dict常用操作"><a href="#字典dict常用操作" class="headerlink" title="字典dict常用操作"></a>字典<code>dict</code>常用操作</h1><h2 id="get"><a href="#get" class="headerlink" title="get()"></a><code>get()</code></h2><p>如果 <code>char</code> 存在，返回对应值；不存在则返回 <code>0</code>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">字典.get(key, 默认值)</span><br></pre></td></tr></table></figure>

<p>统计次数的经典写法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">need[char] = need.get(char, <span class="number">0</span>) + <span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>等价于：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> char <span class="keyword">in</span> need:</span><br><span class="line">    need[char] += <span class="number">1</span></span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    need[char] = <span class="number">1</span></span><br></pre></td></tr></table></figure>

<h2 id="删除键"><a href="#删除键" class="headerlink" title="删除键"></a>删除键</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">del</span> hashmap[key]</span><br></pre></td></tr></table></figure>

<h1 id="字符串str常用操作"><a href="#字符串str常用操作" class="headerlink" title="字符串str常用操作"></a>字符串<code>str</code>常用操作</h1><h2 id="join"><a href="#join" class="headerlink" title="join()"></a><code>join()</code></h2><p>将多个字符串拼接起来。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;&#x27;</span>.join([<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;e&#x27;</span>, <span class="string">&#x27;t&#x27;</span>])      <span class="comment"># &#x27;aet&#x27;</span></span><br></pre></td></tr></table></figure>

<h1 id="常用特殊值"><a href="#常用特殊值" class="headerlink" title="常用特殊值"></a>常用特殊值</h1><p>正无穷：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">min_len = float(&quot;inf&quot;)</span><br></pre></td></tr></table></figure>

<p>负无穷：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">max_num = float(&quot;-inf&quot;)</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>进阶编程与工程基础</category>
      </categories>
      <tags>
        <tag>python</tag>
        <tag>内置函数</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 异常处理与模块化开发</title>
    <url>/2026/03/24/Python/%E5%BC%82%E5%B8%B8%E3%80%81%E6%A8%A1%E5%9D%97%E4%B8%8E%E5%8C%85/</url>
    <content><![CDATA[<h1 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h1><p>1.什么是异常:<br>程序运行的过程中出现了错误<br>2.bug是什么意思:<br>就是指异常的意思，历史上因为小虫子(bug)导致计算机失灵的案例，所以延续至今，bug就代表软件出现错误。</p>
<hr>
<h1 id="异常的捕获"><a href="#异常的捕获" class="headerlink" title="异常的捕获"></a>异常的捕获</h1><h2 id="捕获常规异常："><a href="#捕获常规异常：" class="headerlink" title="捕获常规异常："></a>捕获常规异常：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">	可能发生错误的代码</span><br><span class="line"><span class="keyword">except</span>:</span><br><span class="line">	如果出现异常执行的代码</span><br></pre></td></tr></table></figure>
<h2 id="捕获指定异常："><a href="#捕获指定异常：" class="headerlink" title="捕获指定异常："></a>捕获指定异常：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">	<span class="built_in">print</span>(name)</span><br><span class="line"><span class="keyword">except</span> NameError <span class="keyword">as</span> e:</span><br><span class="line">	<span class="built_in">print</span>(<span class="string">&#x27;变量名称未定义错误&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="捕获多个异常："><a href="#捕获多个异常：" class="headerlink" title="捕获多个异常："></a>捕获多个异常：</h2><p>当捕获多个异常时，把要捕获的异常类型的名字放在except 后，并使用元组的方式进行书写。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">	<span class="built_in">print</span>(<span class="number">1</span>/<span class="number">0</span>)</span><br><span class="line"><span class="keyword">except</span> (NameError, ZeroDivisionError):</span><br><span class="line">	<span class="built_in">print</span>(<span class="string">&#x27;ZeroDivision错误...&#x27;</span>)</span><br></pre></td></tr></table></figure>
<h2 id="异常else："><a href="#异常else：" class="headerlink" title="异常else："></a>异常else：</h2><p>else表示的是如果没有异常要执行的代码。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">	<span class="built_in">print</span>(<span class="number">1</span>)</span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">	<span class="built_in">print</span>(e)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">	<span class="built_in">print</span>(<span class="string">&#x27;我是else，是没有异常的时候执行的代码)</span></span><br></pre></td></tr></table></figure>
<h2 id="异常的finally："><a href="#异常的finally：" class="headerlink" title="异常的finally："></a>异常的finally：</h2><p>finally表示的是无论是否异常都要执行的代码，例如关闭文件。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">	f= <span class="built_in">open</span>(<span class="string">&quot;test.txt&#x27;,&#x27;r&#x27;)</span></span><br><span class="line"><span class="string">except Exception as e:</span></span><br><span class="line"><span class="string">	f= open(&#x27;test.txt&#x27;,&#x27;w&#x27;)</span></span><br><span class="line"><span class="string">else:</span></span><br><span class="line"><span class="string">	print(&#x27;没有异常，真开心&quot;</span>)</span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">	f.close()</span><br></pre></td></tr></table></figure>
<p>其中，else和finally可写可不写</p>
<hr>
<h1 id="异常的传递性"><a href="#异常的传递性" class="headerlink" title="异常的传递性"></a>异常的传递性</h1><p>当函数func01中发生异常，并且没有捕获处理这个异常的时候，异常会传递到函数func02，当func02也没有捕获处理这个异常的时候，main函数会捕获这个异常，这就是异常的传递性.</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">func01</span>():</span><br><span class="line">	<span class="built_in">print</span>(<span class="string">&quot;这是func01开始)</span></span><br><span class="line"><span class="string">	num=1/0</span></span><br><span class="line"><span class="string">	print(&quot;</span>这是funcO1结束<span class="string">&quot;)</span></span><br><span class="line"><span class="string">	</span></span><br><span class="line"><span class="string">def func02():</span></span><br><span class="line"><span class="string">	print(&quot;</span>这是funcO2开始<span class="string">&quot;)</span></span><br><span class="line"><span class="string">	func01()</span></span><br><span class="line"><span class="string">	print(&quot;</span>这是func02结束<span class="string">&quot;)</span></span><br><span class="line"><span class="string">	</span></span><br><span class="line"><span class="string">def main():</span></span><br><span class="line"><span class="string">	try:</span></span><br><span class="line"><span class="string">		func02()</span></span><br><span class="line"><span class="string">	except Exception as e:</span></span><br><span class="line"><span class="string">		print(e)</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="Python模块"><a href="#Python模块" class="headerlink" title="Python模块"></a>Python模块</h1><h2 id="模块的导入"><a href="#模块的导入" class="headerlink" title="模块的导入"></a>模块的导入</h2><p>Python 模块(Module)，是一个 Python 文件，以.py 结尾，模块能定义函数，类和变量，模块里也能包含可执行的代码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">[<span class="keyword">from</span> 模块名] <span class="keyword">import</span> [模块 | 类 | 变量 | 函数 | *] [<span class="keyword">as</span> 别名]</span><br></pre></td></tr></table></figure>
<p>常用的组合形式如:</p>
<ul>
<li>import 模块名</li>
<li>from 模块名 import 类、变量、方法等</li>
<li>from 模块名 import *</li>
<li>import 模块名 as 别名</li>
<li>from 模块名 import 功能名 as 别名</li>
</ul>
<p>注意：</p>
<ul>
<li>from可以省略，直接import即可</li>
<li>as别名可以省略</li>
<li>通过”.”来确定层级关系</li>
<li>模块的导入一般写在代码文件的开头位置</li>
</ul>
<h2 id="自定义模块"><a href="#自定义模块" class="headerlink" title="自定义模块"></a>自定义模块</h2><h2 id="制作自定义模块"><a href="#制作自定义模块" class="headerlink" title="制作自定义模块"></a>制作自定义模块</h2><p>每个Python文件都可以作为一个模块，模块的名字就是文件的名字。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># my_module.py文件</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test</span>(<span class="params">a, b</span>):</span><br><span class="line">	<span class="built_in">print</span>(a + b)</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># test_my_module.py文件</span></span><br><span class="line"><span class="keyword">from</span> my_modle <span class="keyword">import</span> test</span><br><span class="line">	test(<span class="number">10</span>, <span class="number">20</span>)</span><br><span class="line">	</span><br><span class="line"><span class="keyword">import</span> my_module</span><br><span class="line">	my_module.test(<span class="number">10</span>, <span class="number">20</span>)</span><br></pre></td></tr></table></figure>
<h2 id="命名空间污染"><a href="#命名空间污染" class="headerlink" title="命名空间污染"></a>命名空间污染</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#模块1代码</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">my_test</span>(<span class="params">a,b</span>):</span><br><span class="line">	<span class="built_in">print</span>(a + b)</span><br><span class="line">	</span><br><span class="line"><span class="comment">#模块2代码</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">my_test</span>(<span class="params">a, b</span>):</span><br><span class="line">	<span class="built_in">print</span>(a - b)</span><br><span class="line"></span><br><span class="line"><span class="comment">#导 入模块和调用功能代码</span></span><br><span class="line"><span class="keyword">from</span> my_module1 <span class="keyword">import</span> my_test</span><br><span class="line"><span class="keyword">from</span> my_module2 <span class="keyword">import</span> my_test</span><br><span class="line"></span><br><span class="line"><span class="comment">#my_test函数是模块2中的函数</span></span><br><span class="line">my_test(<span class="number">1</span>, <span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<p>注意:当导入多个模块的时候，且模块内有同名功能，当调用这个同名功能的时候，调用到的是后导入模块的功能</p>
<h2 id="main-变量"><a href="#main-变量" class="headerlink" title="__main__变量"></a><code>__main__</code>变量</h2><p>实际开发中，为了为了让模块能够在项目中达到想要的效果，通常会在py文件中添加一些测试信息，例如，在m_module1.py文件中添加测试代码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test</span>(<span class="params">a, b</span>):</span><br><span class="line">	<span class="built_in">print</span>(a + b)</span><br><span class="line">	</span><br><span class="line">test(<span class="number">1</span>,<span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<p>问题:<br>此时，无论是当前文件，还是其他已经导入了该模块的文件，在运行的时候都会自动执行test函数的调用<br>解决方案:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test</span>(<span class="params">a, b</span>):</span><br><span class="line"><span class="built_in">print</span>(a + b)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只在当前文件中调用该函数，其他导入的文件内不符合该条件，则不执行test函数调用</span></span><br><span class="line"><span class="keyword">if</span> __main__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">	test(<span class="number">1</span>, <span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<h2 id="all-变量"><a href="#all-变量" class="headerlink" title="__all__变量"></a><code>__all__</code>变量</h2><p>如果一个模块文件中有<code>__all__</code>变量，当使用from xxx import *导入时，只能导入这个列表中的元素</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># my_module.py文件</span></span><br><span class="line">__all__ = [<span class="string">&#x27;test_A&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test_A</span>():</span><br><span class="line">	<span class="built_in">print</span>(<span class="string">&#x27;test_A&#x27;</span>)</span><br><span class="line">	</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test_B</span>():</span><br><span class="line">	<span class="built_in">print</span>(<span class="string">&#x27;test_B&#x27;</span>)</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> my_module <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">test_A()</span><br><span class="line">test_B()    <span class="comment"># 此时只可以使用test_A，test_B无法使用</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="Python包"><a href="#Python包" class="headerlink" title="Python包"></a>Python包</h1><h2 id="自定义Python包"><a href="#自定义Python包" class="headerlink" title="自定义Python包"></a>自定义Python包</h2><p>包就是一个文件夹，里面可以存放许多Python的模块(代码文件)，通过包，在逻辑上将一批模块归为一类，方便使用。<br><img src="https://raw.githubusercontent.com/NimbusFan/blog-images/main/assets/Python%E5%8C%85%E7%BB%93%E6%9E%84.png" alt="Python包结构.png"></p>
<h3 id="init-py文件"><a href="#init-py文件" class="headerlink" title="__init__.py文件"></a><code>__init__.py</code>文件</h3><p>创建包会默认自动创建的文件，通过这个文件来表示一个文件夹是Python的包，而非普通的文件夹。</p>
<h3 id="创建包的步骤"><a href="#创建包的步骤" class="headerlink" title="创建包的步骤"></a>创建包的步骤</h3><ol>
<li>新建包<code>my_package</code></li>
<li>新建包内模块:<code>my_module_1</code>…<code>my_module_n</code></li>
</ol>
<h3 id="导入包"><a href="#导入包" class="headerlink" title="导入包"></a>导入包</h3><p>方式一：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">包名.模块名.目标</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># import导入</span></span><br><span class="line"><span class="keyword">import</span> 包名.模块名</span><br><span class="line"></span><br><span class="line"><span class="comment"># from导入</span></span><br><span class="line"><span class="keyword">from</span> 包名 <span class="keyword">import</span> 模块名</span><br><span class="line"></span><br><span class="line"><span class="comment"># 直接导入功能</span></span><br><span class="line"><span class="keyword">from</span> 包名.模块名 <span class="keyword">import</span> 功能</span><br></pre></td></tr></table></figure>
<p>方式二：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> 包名 <span class="keyword">import</span> *</span><br></pre></td></tr></table></figure>
<p>注意:必须在<code>_init_.py</code>文件中添加<code>_all_ = []</code>，控制允许导入的模块列表</p>
<h2 id="安装第三方包"><a href="#安装第三方包" class="headerlink" title="安装第三方包"></a>安装第三方包</h2><h3 id="pip"><a href="#pip" class="headerlink" title="pip"></a>pip</h3><p>打开命令提示符程序</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pip install 包名称</span><br></pre></td></tr></table></figure>
<p>由于pip是连接的国外的网站进行包的下载，有的时候会速度很慢。<br>可以通过连接国内的镜像网站提高下载速度:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">pip install -i 镜像网址 包名称</span><br></pre></td></tr></table></figure>
<h3 id="PyCharm"><a href="#PyCharm" class="headerlink" title="PyCharm"></a>PyCharm</h3><ol>
<li>pycharm中提供的安装第三方包的功能：</li>
<li>右下角python解释器</li>
<li>解释器设置</li>
<li>点击“+”搜索想要安装的包</li>
<li>可勾选“选项”填入：-i 镜像网址</li>
<li>安装软件包</li>
</ol>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/08_Exception_Module_Package" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>
]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>异常处理</tag>
        <tag>模块与包</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 数据处理：JSON 解析与 Pyecharts 可视化</title>
    <url>/2026/03/26/Python/%E6%95%B0%E6%8D%AE%E5%8F%AF%E8%A7%86%E5%8C%96/</url>
    <content><![CDATA[<h1 id="JSON数据格式"><a href="#JSON数据格式" class="headerlink" title="JSON数据格式"></a>JSON数据格式</h1><h2 id="什么是JSON"><a href="#什么是JSON" class="headerlink" title="什么是JSON"></a>什么是JSON</h2><p>一种在各个编程语言中流通的数据格式，负责不同编程语言中的数据传递和交互。<br>JSON无非就是一个单独的字典或一个内部元素都是字典的列表。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># json数据的格式可以是：</span></span><br><span class="line">	&#123;<span class="string">&quot;name&quot;</span>:<span class="string">&quot;admin&quot;</span>, <span class="string">&quot;age&quot;</span>:<span class="number">18</span>&#125;</span><br><span class="line">	</span><br><span class="line"><span class="comment"># 也可以是：</span></span><br><span class="line">	[(<span class="string">&quot;name&quot;</span>:<span class="string">&quot;admin&quot;</span>,<span class="string">&quot;age&quot;</span>:<span class="number">18</span>&#125;,(<span class="string">&quot;name&quot;</span>:<span class="string">&quot;root&quot;</span>,<span class="string">&quot;age&quot;</span>:<span class="number">16</span>&#125;,(<span class="string">&quot;name&quot;</span>:<span class="string">&quot;张三&quot;</span>,<span class="string">&quot;age&quot;</span>:<span class="number">20</span>]]</span><br></pre></td></tr></table></figure>
<h2 id="对JSON进行数据转换"><a href="#对JSON进行数据转换" class="headerlink" title="对JSON进行数据转换"></a>对JSON进行数据转换</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导入json模块</span></span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 准备符合json格式要求的python数据</span></span><br><span class="line">data=[&#123;<span class="string">&quot;name&quot;</span>:<span class="string">&quot;老王&quot;</span>,<span class="string">&quot;age&quot;</span>:<span class="number">16</span>&#125;, &#123;<span class="string">&quot;name&quot;</span>:<span class="string">&quot;张三&quot;</span>,<span class="string">&quot;age&quot;</span>:<span class="number">20</span>&#125;]</span><br><span class="line"></span><br><span class="line"><span class="comment"># json.dumps(data)方法把python数据转化为json数据</span></span><br><span class="line">data = json.dumps(data)</span><br><span class="line"><span class="comment"># 显示出中文</span></span><br><span class="line">data = json.dumps(data，ensure_ascii=<span class="literal">False</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># json.loads(data)方法把json数据转化为python数据</span></span><br><span class="line">data =json.loads(data)</span><br></pre></td></tr></table></figure>
<hr>
<h1 id="pyecharts模块介绍"><a href="#pyecharts模块介绍" class="headerlink" title="pyecharts模块介绍"></a>pyecharts模块介绍</h1><ol>
<li>开发可视化图表使用的技术栈是:<br> Echarts框架的Python版本:PyEcharts包</li>
<li>如何安装PyEcharts包:<br> pip install pyecharts</li>
<li>如何查看官方示例<br> 打开官方画廊:<a href="https://gallery.pyecharts.org/#/README">https://gallery.pyecharts.org/#/README</a></li>
</ol>
<hr>
<h1 id="pyecharts快速入门"><a href="#pyecharts快速入门" class="headerlink" title="pyecharts快速入门"></a>pyecharts快速入门</h1><h2 id="构建基础折线图"><a href="#构建基础折线图" class="headerlink" title="构建基础折线图"></a>构建基础折线图</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导包，导入Line功能构建折线图对象</span></span><br><span class="line"><span class="keyword">from</span> pyecharts.charts <span class="keyword">import</span> Line</span><br><span class="line"></span><br><span class="line"><span class="comment"># 得到折线图对象</span></span><br><span class="line">line = Line()</span><br><span class="line"><span class="comment"># 添加x轴数据</span></span><br><span class="line">line.add_xaxis([<span class="string">&quot;中国&quot;</span>，<span class="string">&quot;美国&quot;</span>,<span class="string">&quot;英国&quot;</span>])</span><br><span class="line"><span class="comment"># 添加y轴数据</span></span><br><span class="line">line.add_yaxis(<span class="string">&quot;GDP&quot;</span>, [<span class="number">30</span>, <span class="number">20</span> ,<span class="number">10</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment">#生成图表</span></span><br><span class="line">line.renderO</span><br></pre></td></tr></table></figure>
<h2 id="使用全局配置项设置属性"><a href="#使用全局配置项设置属性" class="headerlink" title="使用全局配置项设置属性"></a>使用全局配置项设置属性</h2><p>pyecharts模块中有很多的配置选项，常用到2个类别的选项: </p>
<ul>
<li><strong>全局配置选项</strong></li>
<li><strong>系列配置选项</strong></li>
</ul>
<p>全局配置选项可以通过<strong>set_global_opts</strong>方法来进行配置，相应的选项和选项的功能如下:<br><img src="https://raw.githubusercontent.com/NimbusFan/blog-images/main/assets/pyecharts%E5%85%A8%E5%B1%80%E9%85%8D%E7%BD%AE.png" alt="pyecharts全局配置.png"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 通过全局配置项设置属性</span></span><br><span class="line">line.set_global_opts(</span><br><span class="line">	title_opts=TitleOpts(<span class="string">&quot;测试&quot;</span>,pos_left=<span class="string">&quot;center&quot;</span>,pos_bottom=<span class="string">&quot;1%&quot;</span>),</span><br><span class="line">	legend_opts=Legendopts(is_show=<span class="literal">True</span>),</span><br><span class="line">	toolbox_opts=ToolboxOpts(is_show=<span class="literal">True</span>),</span><br><span class="line">	visualmap_opts=VisualMapOpts(is_show=<span class="literal">True</span>),</span><br><span class="line">	tooltip_opts=TooltipOpts(is_show=<span class="literal">True</span>),</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/09_Basic_Comprehensive_Case" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>JSON</tag>
        <tag>数据可视化</tag>
        <tag>PyEcharts</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 循环结构：while 与 for 的使用</title>
    <url>/2026/03/14/Python/%E5%BE%AA%E7%8E%AF%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[<h1 id="While-循环"><a href="#While-循环" class="headerlink" title="While 循环"></a>While 循环</h1><p><code>while</code> 循环适用于“只要条件满足，就一直执行”的场景。</p>
<h2 id="基础应用"><a href="#基础应用" class="headerlink" title="基础应用"></a>基础应用</h2><p><code>while</code> 循环的基础语法如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> 条件:</span><br><span class="line">	满足条件时要做的事情<span class="number">1</span></span><br><span class="line">    满足条件时要做的事情<span class="number">2</span></span><br><span class="line">    ...</span><br><span class="line">    满足条件时要做的事情N</span><br></pre></td></tr></table></figure>

<p><strong>⚠️ 注意事项：</strong></p>
<p><strong>条件类型：</strong> 条件的结果一定要是 <code>bool</code> 类型 。<br><strong>防止死循环：</strong> 必须要设置循环的终止条件 。<br><strong>层级关系：</strong> 严格通过空格缩进来判断代码的归属 。</p>
<h2 id="While-循环嵌套"><a href="#While-循环嵌套" class="headerlink" title="While 循环嵌套"></a>While 循环嵌套</h2><p>在复杂的逻辑中，我们可以在循环内部再嵌套循环：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> 条件<span class="number">1</span>:</span><br><span class="line">    满足条件<span class="number">1</span>时要做的事情<span class="number">1</span></span><br><span class="line">    ... </span><br><span class="line">    <span class="keyword">while</span> 条件<span class="number">2</span>:</span><br><span class="line">        满足条件<span class="number">2</span>时要做的事情<span class="number">1</span></span><br><span class="line">        ... </span><br></pre></td></tr></table></figure>

<p>注意：</p>
<ul>
<li>需要设置循环的终止条件</li>
<li>通过空格缩进来判断归属</li>
</ul>
<hr>
<h1 id="For-循环"><a href="#For-循环" class="headerlink" title="For 循环"></a>For 循环</h1><h2 id="基础语法"><a href="#基础语法" class="headerlink" title="基础语法"></a>基础语法</h2><p><code>for</code> 循环主要用于遍历（依次取出）数据集中的元素。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> 临时变量 <span class="keyword">in</span> 待处理数据集(序列):</span><br><span class="line">    循环满足条件时执行的代码</span><br></pre></td></tr></table></figure>

<p>这里的“待处理数据集”，严格来说称之为<strong>序列类型</strong> 。</p>
<p>序列类型是指其内容可以一个个依次取出的一种数据类型，包括：字符串、列表、元组等 。</p>
<p><strong>⚠️ 注意事项：</strong></p>
<ul>
<li><p>无法像 <code>while</code> 那样自定义循环条件，只能被动取出数据处理 。</p>
</li>
<li><p>需要通过空格缩进来判断归属 。</p>
</li>
</ul>
<h2 id="range-语句"><a href="#range-语句" class="headerlink" title="range 语句"></a>range 语句</h2><p><code>range</code> 函数是 <code>for</code> 循环的最佳拍档，用于快速生成数字序列：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">range</span>(num)：		<span class="comment">#获得一个从 0 开始，到 num-1 结束的数字序列 </span></span><br><span class="line"><span class="built_in">range</span>(num1, num2)：		<span class="comment">#获得一个从 num1 开始，到 num2-1 结束的数字序列 </span></span><br><span class="line"><span class="built_in">range</span>(num1, num2, step)：  <span class="comment">#获得一个从 num1 开始，到 num2-1 结束的数字序列，步长为 step (默认为 1) </span></span><br></pre></td></tr></table></figure>

<h2 id="变量作用域"><a href="#变量作用域" class="headerlink" title="变量作用域"></a>变量作用域</h2><p><code>for</code> 循环中临时变量的作用域只被限定在循环内。<br><strong>⚠️ 注意事项：</strong></p>
<ul>
<li><p>虽然在 Python 中，你可以在作用域外访问临时变量，但极其不推荐这样做，这破坏了代码的封装性 。</p>
</li>
<li><p>如需访问临时变量，可以预先在循环外定义它</p>
</li>
</ul>
<h2 id="For-循环的嵌套"><a href="#For-循环的嵌套" class="headerlink" title="For 循环的嵌套"></a>For 循环的嵌套</h2><p>与 <code>while</code> 类似，<code>for</code> 循环也支持嵌套：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> 临时变量 <span class="keyword">in</span> 待处理数据集(序列):</span><br><span class="line">    满足条件时要做的事情<span class="number">1</span></span><br><span class="line">    ... </span><br><span class="line">    <span class="keyword">for</span> 临时变量 <span class="keyword">in</span> 待处理数据集(序列):</span><br><span class="line">        满足条件时要做的事情<span class="number">1</span></span><br><span class="line">        ...</span><br></pre></td></tr></table></figure>

<h2 id="Continue-和-Break"><a href="#Continue-和-Break" class="headerlink" title="Continue 和 Break"></a>Continue 和 Break</h2><h3 id="continue"><a href="#continue" class="headerlink" title="continue"></a>continue</h3><p><code>continue</code> 的作用是：<strong>中断本次循环，直接进入下一次循环</strong> 。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">100</span>): </span><br><span class="line">    语句<span class="number">1</span> </span><br><span class="line">    <span class="keyword">continue</span></span><br><span class="line">    语句<span class="number">2</span> 	<span class="comment"># 此行命令会被跳过</span></span><br></pre></td></tr></table></figure>

<p><code>continue</code> 可用于 <code>for</code> 循环和 <code>while</code> 循环，效果一致 。</p>
<h3 id="break"><a href="#break" class="headerlink" title="break"></a>break</h3><p><code>break</code> 的作用更加坚决：<strong>直接结束整个循环</strong> 。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">100</span>):</span><br><span class="line">    语句<span class="number">1</span></span><br><span class="line">    <span class="keyword">break</span> <span class="comment"># 跳出整个循环</span></span><br><span class="line">    语句<span class="number">2</span></span><br><span class="line">语句<span class="number">3</span></span><br></pre></td></tr></table></figure>

<p><code>break</code> 同样可用于 <code>for</code> 循环和 <code>while</code> 循环，效果完全一致 。</p>
<blockquote>
<p><strong>💡 Break 的底层作用规则：</strong><br>在 Python 以及绝大多数编程语言中，<code>break</code> 遵循以下逻辑：</p>
<ol>
<li><strong>寻找上级：</strong> 当程序运行到 <code>break</code> 时，向外层寻找最近的一个 <code>for/while</code> 循环 。</li>
<li><strong>无视障碍：</strong> 在寻找的过程中，它会穿透所有的 <code>if</code>, <code>elif</code>, <code>else</code> 。</li>
</ol>
</blockquote>
<hr>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/03_Loop_Statements" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>循环结构</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 文件读写与数据持久化</title>
    <url>/2026/03/18/Python/%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<h1 id="文件的编码"><a href="#文件的编码" class="headerlink" title="文件的编码"></a>文件的编码</h1><p>1.什么是编码?<br>编码就是一种规则集合，记录了内容和二进制间进行相互转换的逻辑。编码有许多种，最常用的是UTF-8编码<br>2.为什么需要使用编码?<br>计算机只认识0和1，所以需要将内容翻译成0和1才能保存在计算机中。同时也需要编码，将计算机保存的0和1，反向翻译回可以识别的内容。</p>
<hr>
<h1 id="文件的读取"><a href="#文件的读取" class="headerlink" title="文件的读取"></a>文件的读取</h1><h2 id="打开文件"><a href="#打开文件" class="headerlink" title="打开文件"></a>打开文件</h2><p>使用open函数打开一个已经存在的文件，或者创建一个新文件，语法:<code>open(name, mode, encoding)</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f= <span class="built_in">open</span>(<span class="string">&#x27;python.txt&#x27;</span>,<span class="string">&#x27;r&#x27;</span>, encoding=” UTF-<span class="number">8</span>)		<span class="comment">#encoding的顺序不是第三位，所以不能用位置参数，用关键字参数直接指定</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>name</code>:要打开的目标文件名的字符串(可以包含文件所在的具体路径)。</li>
<li><code>mode</code>:设置打开文件的模式:只读、写入、追加等。</li>
<li><code>encoding</code>:编码格式(推荐使用UTF-8)</li>
</ul>
<p>注意:此时的f是<code>open</code>函数的文件对象，对象是Python中一种特殊的数据类型，拥有属性和方法，可以使用对象.属性或对象.方法对其进行访问</p>
<table>
<thead>
<tr>
<th align="center">模式</th>
<th align="center">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">r</td>
<td align="center">以只读方式打开文件。文件的指针将会放在文件的开头。是默认模式。</td>
</tr>
<tr>
<td align="center">w</td>
<td align="center">以写入方式打开文件。如果该文件已存在则打开文件，并从开头开始编辑，原有内容会被删除。如果该文件不存在，创建新文件。</td>
</tr>
<tr>
<td align="center">a</td>
<td align="center">以追加方式打开文件。如果该文件已存在，新的内容将会被写入到已有内容之后。如果该文件不存在，创建新文件进行写入。</td>
</tr>
</tbody></table>
<h2 id="读操作相关方法"><a href="#读操作相关方法" class="headerlink" title="读操作相关方法"></a>读操作相关方法</h2><table>
<thead>
<tr>
<th align="center">操作</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">文件对象&#x3D;open(file, mode, encoding)</td>
<td align="center">打开文件获得文件对象</td>
</tr>
<tr>
<td align="center">文件对象.read(num)</td>
<td align="center">读取指定长度字节,不指定num读取文件全部</td>
</tr>
<tr>
<td align="center">文件对象.redline()</td>
<td align="center">读取一行</td>
</tr>
<tr>
<td align="center">文件对象.redlines()</td>
<td align="center">取全部行，得到列表</td>
</tr>
<tr>
<td align="center">for line in文件对象</td>
<td align="center">for循环文件行，一次循环得到一行数据</td>
</tr>
<tr>
<td align="center">件对象.close()</td>
<td align="center">关闭文件对象</td>
</tr>
<tr>
<td align="center">with open() as f</td>
<td align="center">通过with open语法打开文件，可以自动关闭</td>
</tr>
</tbody></table>
<h3 id="read-方法"><a href="#read-方法" class="headerlink" title="read()方法:"></a>read()方法:</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">文件对象.read(num)</span><br></pre></td></tr></table></figure>
<p><code>num</code>表示要从文件中读取的数据的长度，如果没有传入num，那么就表示读取文件中所有的数据。<br><code>num</code>:如果文件是以二进制模式打开，单位是字节（Byte），如果是文本模式，单位是字符。</p>
<h3 id="redline-方法："><a href="#redline-方法：" class="headerlink" title="redline()方法："></a>redline()方法：</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">文件对象.readline()</span><br></pre></td></tr></table></figure>
<p>一次读取一行内容</p>
<h3 id="redlines-方法"><a href="#redlines-方法" class="headerlink" title="redlines ()方法:"></a>redlines ()方法:</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">文件对象.readlines()</span><br></pre></td></tr></table></figure>
<p><code>redlines</code>是按照行的方式把整个文件中的内容进行一次性读取，以每一行的数据为一个元素，返回一个列表。</p>
<h3 id="for循环读取文件行"><a href="#for循环读取文件行" class="headerlink" title="for循环读取文件行"></a>for循环读取文件行</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> line <span class="keyword">in</span> <span class="built_in">open</span>(<span class="string">&quot;python.txt, &quot;</span><span class="string">r&quot;)</span></span><br><span class="line"><span class="string">	print(line)</span></span><br></pre></td></tr></table></figure>
<p>每一个<code>line</code>临时变量，就记录了文件的一行数据</p>
<h3 id="close关闭文件对象"><a href="#close关闭文件对象" class="headerlink" title="close关闭文件对象"></a>close关闭文件对象</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f.close()</span><br></pre></td></tr></table></figure>

<p>如果不调用<code>close</code>,同时程序没有停止运行，那么这个文件将一直被Python程序占用。</p>
<h3 id="with-open语法"><a href="#with-open语法" class="headerlink" title="with open语法"></a>with open语法</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&quot;python.txt&quot;</span>,<span class="string">&quot;r&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">f.readlines()</span><br></pre></td></tr></table></figure>

<p>通过在<code>with open</code>的语句块中对文件进行操作，可以在操作完成后自动关闭close文件，避免遗忘掉<code>close</code>方法</p>
<hr>
<h1 id="文件的写入"><a href="#文件的写入" class="headerlink" title="文件的写入"></a>文件的写入</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#1.打开文件</span></span><br><span class="line">f = <span class="built_in">open</span>(<span class="string">&#x27;python.txt&#x27;</span>,<span class="string">&#x27;w&#x27;</span>, encoding=<span class="string">&quot;utf-8&quot;</span> )</span><br><span class="line"></span><br><span class="line"><span class="comment">#2.文件写入</span></span><br><span class="line">f.write(<span class="string">&#x27;hello world&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">#3.刷新内容到硬盘中</span></span><br><span class="line">f.flush()</span><br></pre></td></tr></table></figure>

<p>注意:</p>
<ul>
<li><p>文件不存在时，w模式会创建新文件，文件存在时，会清空原有内容</p>
</li>
<li><p>close()方法，自带了flush()方法的功能</p>
</li>
<li><p>write是把内容写在程序的内存中（<strong>缓冲区</strong>），调用flush后，刷新内容到硬盘中。避免频繁的操作硬盘，导致效率下降</p>
</li>
</ul>
<hr>
<h1 id="文件的追加"><a href="#文件的追加" class="headerlink" title="文件的追加"></a>文件的追加</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#1.打开文件，通过a模式打开即可</span></span><br><span class="line">f=<span class="built_in">open</span>(<span class="string">&#x27;python.txt,a)</span></span><br><span class="line"><span class="string">       </span></span><br><span class="line"><span class="string">#2.文件写入</span></span><br><span class="line"><span class="string">f.write(&#x27;</span>hello world<span class="string">&#x27;)</span></span><br><span class="line"><span class="string">       </span></span><br><span class="line"><span class="string">#3.内容刷新</span></span><br><span class="line"><span class="string">f.flush()</span></span><br></pre></td></tr></table></figure>


<p>注意:如果文件不存在会创建文件，如果文件存在会在最后追加写入文件</p>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/07_File_Operations" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>
]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>文件操作</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 面向对象编程：类、对象与核心特性</title>
    <url>/2026/04/14/Python/%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="初识对象"><a href="#初识对象" class="headerlink" title="初识对象"></a>初识对象</h1><p>在程序中是可以做到和生活中那样，设计表格、生产表格、填写表格的组织形式的。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在程序中设计表格，我们称之为:设计类(class)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	name = <span class="literal">None</span></span><br><span class="line">	</span><br><span class="line"><span class="comment"># 在程序中打印生产表格，我们称之为:创建对象</span></span><br><span class="line"><span class="comment"># 基于类创建对象</span></span><br><span class="line">stu_1 = Student()</span><br><span class="line">stu_2 = Student()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在程序中填写表格，我们称之为:对象属性赋值</span></span><br><span class="line">stu_1.name =<span class="string">&quot;周杰轮&quot;</span>    <span class="comment"># 为学生1对象赋予名称属性值</span></span><br><span class="line">stu_2.name =<span class="string">&quot;林军杰&quot;</span>    <span class="comment"># 为学生2对象赋予名称属性值</span></span><br></pre></td></tr></table></figure>
<hr>
<h1 id="成员方法"><a href="#成员方法" class="headerlink" title="成员方法"></a>成员方法</h1><h2 id="类的语法："><a href="#类的语法：" class="headerlink" title="类的语法："></a>类的语法：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">类名称</span>：    <span class="comment"># class是关键字，表示要定义类</span></span><br><span class="line">	类的属性      <span class="comment"># 类的属性，即定义在类中的变量（成员变量）</span></span><br><span class="line">	</span><br><span class="line">	类的行为      <span class="comment"># 类的行为，即定义在类中的函数（成员方法）</span></span><br></pre></td></tr></table></figure>
<p>注意：函数是写在类外的，定义在类内部的函数习惯上称为<strong>方法</strong></p>
<h2 id="成员方法的语法"><a href="#成员方法的语法" class="headerlink" title="成员方法的语法"></a>成员方法的语法</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">类名称</span>:</span><br><span class="line">	成员变量</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">成员方法</span>(<span class="params">self, 参数列表</span>)：</span><br><span class="line">		成员方法体</span><br><span class="line">		</span><br><span class="line">对象 = 类名称()</span><br></pre></td></tr></table></figure>
<p>self的作用：</p>
<ul>
<li>表示类对象本身的意思</li>
<li>只有通过self，成员方法才能访问类的成员变量</li>
<li>self出现在形参列表中，但是不占用参数位置，因此调用时无需理会</li>
</ul>
<h2 id="创建类对象的语法："><a href="#创建类对象的语法：" class="headerlink" title="创建类对象的语法："></a>创建类对象的语法：</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">对象 = 类名称()</span><br></pre></td></tr></table></figure>
<hr>
<h1 id="类和对象"><a href="#类和对象" class="headerlink" title="类和对象"></a>类和对象</h1><p>类和对象的关系是什么?</p>
<ul>
<li>类是程序中的“设计图纸”</li>
<li>对象是基于图纸生产的具体实体</li>
</ul>
<p>面向对象编程：使用对象进行编程。即设计类，基于类创建对象，并使用对象来完成具体的工作</p>
<hr>
<h1 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h1><p>Python类可以使用:<code>__init__()</code>方法，称之为构造方法。</p>
<ul>
<li>在创建类对象(构造类)的时候，会自动执行。</li>
<li>在创建类对象(构造类)的时候，传入参数时会自动传递给<code>__init__</code>方法使用。</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	name = <span class="literal">None</span>    <span class="comment"># 可以省略</span></span><br><span class="line">	age = <span class="literal">None</span>     <span class="comment"># 可以省略</span></span><br><span class="line">	tel = <span class="literal">None</span>     <span class="comment"># 可以省略</span></span><br><span class="line">	</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self,name,age, tel</span>):</span><br><span class="line">		<span class="variable language_">self</span>.name = name</span><br><span class="line">		<span class="variable language_">self</span>.age = age</span><br><span class="line">		<span class="variable language_">self</span>.tel = tel</span><br><span class="line">		<span class="built_in">print</span>(<span class="string">&quot;student类创建了一个对象&quot;</span>)</span><br><span class="line">		</span><br><span class="line">stu = Student(<span class="string">&quot;周杰轮&quot;</span>，<span class="number">31</span>,<span class="string">&quot;18500006666&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>注意：构造方法也是成员方法，不能忘记在参数列表中提供<code>self</code></p>
<hr>
<h1 id="魔术方法"><a href="#魔术方法" class="headerlink" title="魔术方法"></a>魔术方法</h1><p><code>__init__</code>构造方法，是Python类内置的方法之一。<br>这些内置的类方法各自有各自特殊的功能，称之为<strong>魔术方法</strong></p>
<h2 id="str-字符串魔术方法"><a href="#str-字符串魔术方法" class="headerlink" title="__str__字符串魔术方法"></a><code>__str__</code>字符串魔术方法</h2><p>当类对象需要被转换为字符串时，会输出其内存地址</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span> (<span class="variable language_">self</span>, name, age):</span><br><span class="line">		<span class="variable language_">self</span>.name = name</span><br><span class="line">		<span class="variable language_">self</span>.age = age</span><br><span class="line">		</span><br><span class="line">student = Student(<span class="string">&quot;周杰轮&quot;</span>, <span class="number">11</span>)</span><br><span class="line"><span class="built_in">print</span>(student)        <span class="comment"># 结果:&lt;__main__.Student object at 0x000002200cFD7040&gt;</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span>(student))   <span class="comment"># 结果:&lt;__main__.Student object at Ox000002200cFD7040&gt;</span></span><br></pre></td></tr></table></figure>
<p>但是内存地址没有多大作用，可以通过str 方法，控制类转换为字符串的行为<br> <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	<span class="keyword">def</span>  <span class="title function_">__init__</span> (<span class="variable language_">self</span>, name, age):</span><br><span class="line">		<span class="variable language_">self</span>.name = name</span><br><span class="line">		<span class="variable language_">self</span>.age = age</span><br><span class="line">		</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__str__</span>(<span class="params">self</span>):</span><br><span class="line">		<span class="keyword">return</span> <span class="string">f&quot;student类对象,name=<span class="subst">&#123;self.name&#125;</span>, age=<span class="subst">&#123;self.age&#125;</span>&quot;</span></span><br><span class="line">		</span><br><span class="line">student = Student(<span class="string">&quot;周杰轮&quot;</span>, <span class="number">11</span>)</span><br><span class="line"><span class="built_in">print</span>(student)         <span class="comment"># 结果:Student类对象，name=周杰轮，age=11</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span>(student))    <span class="comment"># 结果:Student类对象，name=周杰轮，age=l1</span></span><br></pre></td></tr></table></figure></p>
<h2 id="lt-小于符号魔术方法"><a href="#lt-小于符号魔术方法" class="headerlink" title="__lt__小于符号魔术方法"></a><code>__lt__</code>小于符号魔术方法</h2><p> 两个对象之间不可以直接进行比较<br> <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span> (<span class="variable language_">self</span>, name, age):</span><br><span class="line">	<span class="variable language_">self</span>.name = nameself.age = age</span><br><span class="line"></span><br><span class="line">stu1 = Student(<span class="string">&quot;周杰伦&quot;</span>,<span class="number">11</span>)</span><br><span class="line">stu2 = Student(<span class="string">&quot;林俊杰&quot;</span>,<span class="number">13</span>)</span><br><span class="line"><span class="built_in">print</span>(stul &lt; stu2)      <span class="comment"># 此处无法比较，会直接报错</span></span><br></pre></td></tr></table></figure><br> 此时需要在类中实现<code>__lt__</code>方法，即可同时完成:小于符号和大于符号2种比较<br> <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, name, age</span>):</span><br><span class="line">		<span class="variable language_">self</span>.name = name</span><br><span class="line">		<span class="variable language_">self</span>.age = age</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__lt__</span>(<span class="params">self, other</span>):</span><br><span class="line">		<span class="keyword">return</span> <span class="variable language_">self</span>.age &lt; other.age</span><br><span class="line">stu1 = Student(<span class="string">&quot;周杰伦&quot;</span>,<span class="number">11</span>)</span><br><span class="line">stu2 = Student(<span class="string">&quot;林俊杰&quot;</span>,<span class="number">13</span>)</span><br><span class="line"><span class="built_in">print</span>(stu1 &lt; stu2)     <span class="comment"># 结果:True</span></span><br><span class="line"><span class="built_in">print</span>(stu1 &gt; stu2)     <span class="comment"># 结果:False</span></span><br></pre></td></tr></table></figure></p>
<h2 id="le-小于等于符号魔术方法"><a href="#le-小于等于符号魔术方法" class="headerlink" title="__le__小于等于符号魔术方法"></a><code>__le__</code>小于等于符号魔术方法</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span> (<span class="variable language_">self</span>, name, age):</span><br><span class="line">		<span class="variable language_">self</span>.name = name</span><br><span class="line">		<span class="variable language_">self</span>.age = age</span><br><span class="line"></span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">___le__</span>(<span class="params">self, other</span>):</span><br><span class="line">		<span class="keyword">return</span> <span class="variable language_">self</span>.age &lt;= other.age</span><br><span class="line">		</span><br><span class="line">stu1 = Student(<span class="string">&quot;周杰轮&quot;</span>,<span class="number">11</span>)</span><br><span class="line">stu2 = Student(<span class="string">&quot;林军杰&quot;</span>,<span class="number">13</span>)</span><br><span class="line"><span class="built_in">print</span>(stu1 &lt;= stu2)    <span class="comment"># 结果:True</span></span><br><span class="line"><span class="built_in">print</span>(stu2 &gt;= stu2)    <span class="comment"># 结果:False</span></span><br></pre></td></tr></table></figure>
<h2 id="eq-比较运算符魔术方法"><a href="#eq-比较运算符魔术方法" class="headerlink" title="__eq__比较运算符魔术方法"></a><code>__eq__</code>比较运算符魔术方法</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__init__</span> (<span class="variable language_">self</span>, name, age):</span><br><span class="line">		<span class="variable language_">self</span>.name = name</span><br><span class="line">		<span class="variable language_">self</span>.age = age</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">def</span> <span class="title function_">__eq__</span>(<span class="params">self, other</span>):</span><br><span class="line">	<span class="keyword">return</span> <span class="variable language_">self</span>.age == other.age</span><br><span class="line"></span><br><span class="line">stu1 = Student(<span class="string">&quot;周杰轮&quot;</span>,<span class="number">11</span>)</span><br><span class="line">stu2 = Student(<span class="string">&quot;林军杰&quot;</span>,<span class="number">11</span>)    </span><br><span class="line"><span class="built_in">print</span>(stu1 == stu2)    <span class="comment"># 结果:True</span></span><br></pre></td></tr></table></figure>
<hr>
<h1 id="封装"><a href="#封装" class="headerlink" title="封装"></a>封装</h1><p>面向对象包含3大主要特性：</p>
<ul>
<li>封装</li>
<li>继承</li>
<li>多态</li>
</ul>
<p>封装是指将现实世界事物在类中描述为属性和方法。</p>
<p>现实事物的部分属性和行为是不公开对使用者开放的。同样在类中描述属性和方法的时候也需要达到这个要求</p>
<p>定义私有成员：成员变量和成员方法的命名均以<code>__</code>作为开头</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Phone</span>:  </span><br><span class="line">    __current_voltage = <span class="number">1</span>  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__keep_single_core</span>(<span class="params">self</span>):  </span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;让cpu以单核模式运行&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>注意：类对象无法访问私有成员，类中的其它成员可以访问私有成员</p>
<hr>
<h1 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h1><h2 id="继承的基础语法"><a href="#继承的基础语法" class="headerlink" title="继承的基础语法"></a>继承的基础语法</h2><p>继承:将从父类那里继承(复制)来成员变量和成员方法(<strong>不含私有</strong>)</p>
<p>子类构建的类对象，可以</p>
<ul>
<li>有自己的成员变量和成员方法</li>
<li>使用父类的成员变量和成员方法</li>
</ul>
<h2 id="单继承和多继承"><a href="#单继承和多继承" class="headerlink" title="单继承和多继承"></a>单继承和多继承</h2><p>类的单继承语法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">类名</span>(<span class="title class_ inherited__">父类名</span>):</span><br><span class="line">	类内容体</span><br></pre></td></tr></table></figure>

<p>类的多继承语法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">类名</span>(父类<span class="number">1</span>, 父类<span class="number">2</span>, ..., 父类N):</span><br></pre></td></tr></table></figure>
<p><strong>注意</strong>：多继承中，如果父类有同名方法或属性，先继承的优先级高于后继承</p>
<h2 id="pass"><a href="#pass" class="headerlink" title="pass"></a>pass</h2><p>pass是占位语句，用来保证函数(方法)或类定义的完整性，表示无内容，空的意思</p>
<hr>
<h1 id="复写和使用父类成员"><a href="#复写和使用父类成员" class="headerlink" title="复写和使用父类成员"></a>复写和使用父类成员</h1><h2 id="复写"><a href="#复写" class="headerlink" title="复写"></a>复写</h2><p>对父类的成员属性或成员方法进行重新定义</p>
<h2 id="调用父类成员"><a href="#调用父类成员" class="headerlink" title="调用父类成员"></a>调用父类成员</h2><p>一旦复写父类成员，那么类对象调用成员的时候，就会调用复写后的新成员。<br>如果需要使用被复写的父类的成员，需要特殊的调用方式:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 方式1:调用父类成员</span></span><br><span class="line">使用成员变量:父类名.成员变量</span><br><span class="line">使用成员方法:父类名.成员方法(<span class="variable language_">self</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方式2:使用super()调用父类成员</span></span><br><span class="line">使用成员变量:<span class="built_in">super</span>().成员变量</span><br><span class="line">使用成员方法:<span class="built_in">super</span>().成员方法()</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="类型注解"><a href="#类型注解" class="headerlink" title="类型注解"></a>类型注解</h1><p>类型注解支持:</p>
<ol>
<li>变量的类型注解</li>
<li>函数(方法)的形参和返回值的类型注解</li>
</ol>
<h2 id="变量的类型注解"><a href="#变量的类型注解" class="headerlink" title="变量的类型注解"></a>变量的类型注解</h2><p>语法1:<strong>变量:类型</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基础数据类型注解</span></span><br><span class="line">var_1: <span class="built_in">int</span> = <span class="number">10</span></span><br><span class="line">var_2: <span class="built_in">float</span> = <span class="number">3.1415926</span></span><br><span class="line">var_3: <span class="built_in">bool</span> = <span class="literal">True</span></span><br><span class="line">Var_4: <span class="built_in">str</span> = <span class="string">&quot;itheima”</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 基础容器类型注解</span></span><br><span class="line"><span class="string">my_list: list = [1, 2, 3]      </span></span><br><span class="line"><span class="string">my_tuple: tuple = (1, 2, 3)</span></span><br><span class="line"><span class="string">my_set: set = &#123;1, 2, 3&#125;</span></span><br><span class="line"><span class="string">my_dict: dict = &#123;&quot;</span>itheima<span class="string">&quot;: 666&#125;</span></span><br><span class="line"><span class="string">my_str: str = &quot;</span>itheima<span class="string">&quot;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 容器类型详细注解</span></span><br><span class="line"><span class="string">my_list: list[int] = [1, 2, 3]</span></span><br><span class="line"><span class="string">my_tuple: tuple[str, int, bool] = (&quot;</span>itheima<span class="string">&quot;, 666, True)</span></span><br><span class="line"><span class="string">my_set: set[int] = &#123;1, 2, 3&#125;</span></span><br><span class="line"><span class="string">my_dict: dict[str, int] = &#123;&quot;</span>itheima<span class="string">&quot;: 666&#125;</span></span><br><span class="line"><span class="string"># 注意:元组类型设置类型详细注解，需要将每一个元素都标记出来</span></span><br><span class="line"><span class="string"># 注意:字典类型设置类型详细注解，需要2个类型，第一个是key第二个是value</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 类对象类型注解</span></span><br><span class="line"><span class="string">class Student:</span></span><br><span class="line"><span class="string">	pass</span></span><br><span class="line"><span class="string">stu: Student = Student()</span></span><br></pre></td></tr></table></figure>
<p>语法2:在注释中，<strong>#type:类型</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在注释中进行类型注解</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>:</span><br><span class="line">	<span class="keyword">pass</span></span><br><span class="line">var_1 = random.randint(<span class="number">1</span>, <span class="number">10</span>)     <span class="comment"># type:<span class="built_in">int</span></span></span><br><span class="line">var_2 = json.loads(data)          <span class="comment"># type: <span class="built_in">dict</span>[<span class="built_in">str</span>, <span class="built_in">int</span>]</span></span><br><span class="line">var_3 = func()                    <span class="comment"># type: Student</span></span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>:类型注解只是提示性的，并非决定性的。数据类型和注解类型无法对应也不会导致编译错误</p>
<h2 id="函数-方法-的类型注解"><a href="#函数-方法-的类型注解" class="headerlink" title="函数(方法)的类型注解"></a>函数(方法)的类型注解</h2><h3 id="形参的类型注解"><a href="#形参的类型注解" class="headerlink" title="形参的类型注解"></a>形参的类型注解</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">x: <span class="built_in">int</span>, y: <span class="built_in">int</span></span>):</span><br><span class="line">	<span class="keyword">return</span> x + y</span><br><span class="line">	</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">func</span>(<span class="params">data: <span class="built_in">list</span></span>):</span><br><span class="line">	<span class="keyword">pass</span></span><br></pre></td></tr></table></figure>
<h3 id="返回值的类型注解"><a href="#返回值的类型注解" class="headerlink" title="返回值的类型注解"></a>返回值的类型注解</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">函数方法名</span>(<span class="params">形参: 类型，......, 形参:类型</span>) -&gt; 返回值类型:</span><br><span class="line">	<span class="keyword">pass</span></span><br></pre></td></tr></table></figure>
<h2 id="Union联合类型注解"><a href="#Union联合类型注解" class="headerlink" title="Union联合类型注解"></a>Union联合类型注解</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 变量的联合类型注解</span></span><br><span class="line"><span class="keyword">from</span> typing <span class="keyword">import</span> <span class="type">Union</span></span><br><span class="line"></span><br><span class="line">my_list: <span class="built_in">list</span>[union[<span class="built_in">str</span>, <span class="built_in">int</span>]] = [<span class="number">1</span>, <span class="number">2</span>, <span class="string">&quot;itheima&quot;</span>, <span class="string">&quot;itcast&quot;</span>]</span><br><span class="line">my_dict: <span class="built_in">dict</span>[<span class="built_in">str</span>, <span class="type">Union</span>[<span class="built_in">str</span>, <span class="built_in">int</span>]] = &#123;<span class="string">&quot;name&quot;</span>: <span class="string">&quot;周杰伦&quot;</span>, <span class="string">&quot;age&quot;</span>:<span class="number">31</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 函数(方法)的联合类型注解</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">func</span>(<span class="params">data: union[<span class="built_in">int</span>, <span class="built_in">str</span>]</span>) -&gt; union[<span class="built_in">int</span>, <span class="built_in">str</span>]:</span><br><span class="line">	<span class="keyword">pass</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h1><p>多态常用在继承关系上。</p>
<ul>
<li>以父类做定义声明</li>
<li>以子类做实际工作</li>
<li>用以获得同一行为，不同状态<br>这种写法，就叫做抽象类(也可以称之为接口)</li>
</ul>
<p><strong>抽象类</strong>:含有抽象方法的类称之为抽象类<br><strong>抽象方法</strong>:方法体是空实现的(pass)称之为抽象方法</p>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage2_OOP/01_Object-Oriented" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>面向对象程序设计</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>面向对象</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 数据结构：列表、元组、集合与字典</title>
    <url>/2026/03/16/Python/%E6%95%B0%E6%8D%AE%E5%AE%B9%E5%99%A8/</url>
    <content><![CDATA[<h1 id="数据容器"><a href="#数据容器" class="headerlink" title="数据容器"></a>数据容器</h1><p>一种可以容纳多份数据的数据类型，每一份数据称之为1个<strong>元素</strong>。元素可以是<strong>任意类型</strong>的数据。</p>
<p>数据容器根据是<strong>否支持重复元素</strong>、<strong>是否可以修改</strong>、<strong>是否有序</strong>的不同，分为5类，分别是:<u>列表(list)</u>、<u>元组(tuple)</u>、<u>字符串(str)</u>、<u>集合(set)</u>、<u>字典(dict)</u></p>
<hr>
<h1 id="list"><a href="#list" class="headerlink" title="list"></a>list</h1><h2 id="列表的定义和特点"><a href="#列表的定义和特点" class="headerlink" title="列表的定义和特点"></a>列表的定义和特点</h2><h3 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 字面量</span></span><br><span class="line">[元素<span class="number">1</span>，元素<span class="number">2</span>，元素<span class="number">3</span>，元素<span class="number">4</span>，...]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义变量</span></span><br><span class="line">变量名称 = [元素<span class="number">1</span>，元素<span class="number">2</span>，元素<span class="number">3</span>，元素<span class="number">4</span>，...]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义空列表</span></span><br><span class="line">变量名称 = []</span><br><span class="line">变量名称 = <span class="built_in">list</span>()</span><br></pre></td></tr></table></figure>

<h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h3><ul>
<li>可以容纳多个元素（上限为2**63-1、9223372036854775807个）</li>
<li>可以容纳不同类型的元素（混装）</li>
<li>数据是有序存储的（有下标序号）</li>
<li>允许重复数据存在</li>
<li>可以修改（增加或删除元素等）</li>
<li>支持嵌套</li>
</ul>
<h2 id="列表的下标（索引）"><a href="#列表的下标（索引）" class="headerlink" title="列表的下标（索引）"></a>列表的下标（索引）</h2><p>通过下标索引可取出列表中特定位置的数据</p>
<h3 id="正向索引"><a href="#正向索引" class="headerlink" title="正向索引"></a>正向索引</h3><p><img src="https://raw.githubusercontent.com/NimbusFan/blog-images/main/assets/%E6%AD%A3%E5%90%91%E7%B4%A2%E5%BC%95.png" alt="正向索引.png"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">my_list = [<span class="string">&quot;fanyunfei&quot;</span>, <span class="string">&quot;dashuaibi&quot;</span>, <span class="string">&quot;youqianduojin&quot;</span>]</span><br><span class="line"><span class="built_in">print</span>(my_list[<span class="number">0</span>])    <span class="comment"># 输出fanyunfei</span></span><br><span class="line"><span class="built_in">print</span>(my_list[<span class="number">1</span>])    <span class="comment"># 输出dashuaibi</span></span><br><span class="line"><span class="built_in">print</span>(my_list[<span class="number">2</span>])    <span class="comment"># 输出youqianduojin</span></span><br></pre></td></tr></table></figure>

<h3 id="反向索引"><a href="#反向索引" class="headerlink" title="反向索引"></a>反向索引</h3><p>从后向前：从-1开始，依次递减（-1，-2，-3……）<br><img src="https://raw.githubusercontent.com/NimbusFan/blog-images/main/assets/%E5%8F%8D%E5%90%91%E7%B4%A2%E5%BC%95.png" alt="反向索引.png"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">my_list = [<span class="string">&quot;fanyunfei&quot;</span>, <span class="string">&quot;dashuaibi&quot;</span>, <span class="string">&quot;youqianduojin&quot;</span>]</span><br><span class="line"><span class="built_in">print</span>(my_list[-<span class="number">1</span>])    <span class="comment"># 输出youqianduojin</span></span><br><span class="line"><span class="built_in">print</span>(my_list[-<span class="number">2</span>])    <span class="comment"># 输出dashuaibi</span></span><br><span class="line"><span class="built_in">print</span>(my_list[-<span class="number">3</span>])    <span class="comment"># 输出fanyunfei</span></span><br></pre></td></tr></table></figure>
<h3 id="嵌套列表的索引"><a href="#嵌套列表的索引" class="headerlink" title="嵌套列表的索引"></a>嵌套列表的索引</h3><p><img src="https://raw.githubusercontent.com/NimbusFan/blog-images/main/assets/%E5%B5%8C%E5%A5%97%E5%88%97%E8%A1%A8%E7%9A%84%E7%B4%A2%E5%BC%95.png" alt="嵌套列表的索引.png"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">my_list = [[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br><span class="line"><span class="built_in">print</span>(my_list[<span class="number">0</span>])    <span class="comment"># 输出[1, 2, 3]</span></span><br><span class="line"><span class="built_in">print</span>(my_list[<span class="number">0</span>][<span class="number">1</span>])    <span class="comment"># 输出2</span></span><br></pre></td></tr></table></figure>

<h2 id="列表的常用操作"><a href="#列表的常用操作" class="headerlink" title="列表的常用操作"></a>列表的常用操作</h2><p>如下所述常用操作（除len外）是列表对象(变量)内置的方法(函数)</p>
<table>
<thead>
<tr>
<th align="center">编号</th>
<th align="center">使用方式</th>
<th align="center">作用</th>
</tr>
</thead>
<tbody><tr>
<td align="center">1</td>
<td align="center">列表.append(元素)</td>
<td align="center">向列表中追加一个元素</td>
</tr>
<tr>
<td align="center">2</td>
<td align="center">列表.extend(容器)</td>
<td align="center">将数据容器的内容依次取出，追加到列表尾部</td>
</tr>
<tr>
<td align="center">3</td>
<td align="center">列表.insert(下标，元素)</td>
<td align="center">在指定下标处，插入指定的元素</td>
</tr>
<tr>
<td align="center">4</td>
<td align="center">del列表[下标]</td>
<td align="center">删除列表指定下标元素</td>
</tr>
<tr>
<td align="center">5</td>
<td align="center">列表.pop(下标)</td>
<td align="center">删除列表指定下标元素</td>
</tr>
<tr>
<td align="center">6</td>
<td align="center">列表.remove(元素)</td>
<td align="center">从前向后，删除此元素第一个匹配项</td>
</tr>
<tr>
<td align="center">7</td>
<td align="center">列表.clear()</td>
<td align="center">清空列表</td>
</tr>
<tr>
<td align="center">8</td>
<td align="center">列表.count(元素)</td>
<td align="center">统计此元素在列表中出现的次数</td>
</tr>
<tr>
<td align="center">9</td>
<td align="center">列表.index(元素)</td>
<td align="center">查找指定元素在列表的下标，找不到报错ValueError</td>
</tr>
<tr>
<td align="center">10</td>
<td align="center">len(列表)</td>
<td align="center">统计容器内有多少元素</td>
</tr>
</tbody></table>
<h3 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h3><p>如果将函数定义为**class(类)**的成员，那么函数会称之为:<strong>方法</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 函数</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">x,y</span>):</span><br><span class="line">    <span class="keyword">return</span> x + y</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方法</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">add</span>(<span class="variable language_">self</span>，x,y):</span><br><span class="line">        <span class="keyword">return</span> x + y</span><br></pre></td></tr></table></figure>

<p>方法和函数功能一样，有传入参数，有返回值，只是方法的使用格式不同:</p>
<p>函数的使用：<code>num = add(1, 2)</code></p>
<p>方法的使用：<code>student = Student():</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">num = student.add</span><br></pre></td></tr></table></figure>

<h3 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h3><p>查找指定元素在列表的下标，找不到报错ValueError</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">列表.index(元素)</span><br></pre></td></tr></table></figure>

<h3 id="修改"><a href="#修改" class="headerlink" title="修改"></a>修改</h3><p>修改特定位置(索引)的元素值:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">列表[下表] = 值</span><br></pre></td></tr></table></figure>

<h3 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h3><p>在指定的下标位置，插入指定的元素</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">列表.insert(下标，元素)</span><br></pre></td></tr></table></figure>

<h3 id="追加"><a href="#追加" class="headerlink" title="追加"></a>追加</h3><p>将指定元素，追加到列表的尾部</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">列表.append(元素)    <span class="comment"># 追加单个元素</span></span><br><span class="line">列表.extend(其它数据容器)    <span class="comment"># 批量追加：将其它数据容器的内容取出，依次追加到列表尾部</span></span><br></pre></td></tr></table></figure>

<h3 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 通过下标删除</span></span><br><span class="line"><span class="keyword">del</span>列表[下标]    <span class="comment"># 方法1</span></span><br><span class="line">列表.pop(下标)    <span class="comment"># 方法2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 通过内容删除</span></span><br><span class="line">列表.remove(元素)   <span class="comment"># 从前往后搜索，删除找到的第一个元素</span></span><br></pre></td></tr></table></figure>

<h3 id="清空"><a href="#清空" class="headerlink" title="清空"></a>清空</h3><p>清空列表内容</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">列表.clear()py</span><br></pre></td></tr></table></figure>

<h3 id="统计"><a href="#统计" class="headerlink" title="统计"></a>统计</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">列表.count(元素)    <span class="comment"># 统计某个元素在列表中的数量</span></span><br><span class="line"><span class="built_in">len</span>(列表)    <span class="comment"># 统计列表内一共有多少个元素 </span></span><br></pre></td></tr></table></figure>

<h2 id="列表的循环遍历"><a href="#列表的循环遍历" class="headerlink" title="列表的循环遍历"></a>列表的循环遍历</h2><h3 id="while循环遍历列表元素"><a href="#while循环遍历列表元素" class="headerlink" title="while循环遍历列表元素"></a>while循环遍历列表元素</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = <span class="number">0</span></span><br><span class="line"><span class="keyword">while</span> index &lt; <span class="built_in">len</span>(列表)</span><br><span class="line">    元素 = 列表[index]</span><br><span class="line">    对元素进行处理</span><br><span class="line">    index += <span class="number">1</span> </span><br></pre></td></tr></table></figure>

<h3 id="for循环遍历列表元素"><a href="#for循环遍历列表元素" class="headerlink" title="for循环遍历列表元素"></a>for循环遍历列表元素</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> 临时变量 <span class="keyword">in</span> 数据容器：</span><br><span class="line">    对临时变量进行处理</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="tuple"><a href="#tuple" class="headerlink" title="tuple"></a>tuple</h1><h2 id="元组的定义格式"><a href="#元组的定义格式" class="headerlink" title="元组的定义格式"></a>元组的定义格式</h2><p>元组定义：定义元组使用小括号，且使用逗号隔开各个数据，可以是不同的数据类型。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义元组字面量</span></span><br><span class="line">(元素，元素，......，元素)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义元组变量</span></span><br><span class="line">变量名称 = (元素，元素，......，元素)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义空元组</span></span><br><span class="line">变量名称 = ()    <span class="comment"># 方式1</span></span><br><span class="line">变量名称 = <span class="built_in">tuple</span>()    <span class="comment"># 方式2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 元组的嵌套</span></span><br><span class="line">变量名称 = ((), ())</span><br></pre></td></tr></table></figure>

<h2 id="元组的特点"><a href="#元组的特点" class="headerlink" title="元组的特点"></a>元组的特点</h2><ul>
<li>可以容纳<strong>多个</strong>数据</li>
<li>可以容纳<strong>不同类型</strong>的数据(混装)数据是有序存储的(下标索引)</li>
<li>允许<strong>重复数据</strong>存在</li>
<li><strong>不可以修改</strong>(增加或删除元素等)，若元组中嵌套list，list中的元素可以修改</li>
<li>支持for&#x2F;while<strong>循环</strong></li>
</ul>
<h2 id="元组的常用操作"><a href="#元组的常用操作" class="headerlink" title="元组的常用操作"></a>元组的常用操作</h2><table>
<thead>
<tr>
<th align="center">编号</th>
<th align="center">方法</th>
<th align="center">作用</th>
</tr>
</thead>
<tbody><tr>
<td align="center">1</td>
<td align="center">index()</td>
<td align="center">查找某个数据，如果数据存在返回对应的下标，否则报错</td>
</tr>
<tr>
<td align="center">2</td>
<td align="center">count()</td>
<td align="center">统计某个数据在当前元组出现的次数</td>
</tr>
<tr>
<td align="center">3</td>
<td align="center">len(元组)</td>
<td align="center">统计元组内的元素个数</td>
</tr>
</tbody></table>
<hr>
<h1 id="str"><a href="#str" class="headerlink" title="str"></a>str</h1><p>字符串可以看作是字符的容器</p>
<h2 id="1-字符串的特点"><a href="#1-字符串的特点" class="headerlink" title="1.字符串的特点"></a>1.字符串的特点</h2><ul>
<li><strong>只可以存储字符串</strong></li>
<li>长度任意(取决于内存大小)</li>
<li>支持下标索引</li>
<li>允许重复字符串存在</li>
<li><strong>不可以修改</strong>(增加或删除元素等)</li>
<li>支持while&#x2F;for循环</li>
</ul>
<h2 id="2-字符串的常用操作"><a href="#2-字符串的常用操作" class="headerlink" title="2.字符串的常用操作"></a>2.字符串的常用操作</h2><table>
<thead>
<tr>
<th align="center">编号</th>
<th align="center">方法</th>
<th align="center">作用</th>
</tr>
</thead>
<tbody><tr>
<td align="center">1</td>
<td align="center">字符串[下标]</td>
<td align="center">根据下标索引取出特定位置字符从前向后，下标从0开始；从后向前，下标从-1开始</td>
</tr>
<tr>
<td align="center">2</td>
<td align="center">字符串.index(字符串)</td>
<td align="center">查找给定字符的第一个匹配项的下标</td>
</tr>
<tr>
<td align="center">3</td>
<td align="center">字符串.replace(字符串1，字符串2)</td>
<td align="center">将字符串内的全部字符串1，替换为字符串2<br>不会修改原字符串，而是得到一个新的</td>
</tr>
<tr>
<td align="center">4</td>
<td align="center">字符串.split(字符串)</td>
<td align="center">按照给定字符串，对字符串进行分隔<br>不会修改原字符串，而是得到一个新的列表</td>
</tr>
<tr>
<td align="center">5</td>
<td align="center">字符串.strip(字符串)</td>
<td align="center">移除首尾的空格和换行符或指定字符串<br>不会修改原字符串，而是得到一个新的</td>
</tr>
<tr>
<td align="center">6</td>
<td align="center">字符串.count(字符串)</td>
<td align="center">统计字符串内某字符串的出现次数</td>
</tr>
<tr>
<td align="center">7</td>
<td align="center">len(字符串)</td>
<td align="center">统计字符串的字符个数</td>
</tr>
<tr>
<td align="center">8</td>
<td align="center">new_string &#x3D; “分隔符”.join(iterable)</td>
<td align="center">用指定的分隔符，将一个可迭代对象（如列表、元组、字典）中的字符串元素连接成一个新的字符串</td>
</tr>
</tbody></table>
<hr>
<h1 id="序列"><a href="#序列" class="headerlink" title="序列"></a>序列</h1><h2 id="什么是序列"><a href="#什么是序列" class="headerlink" title="什么是序列"></a>什么是序列</h2><p>内容连续、有序，可使用下标索引的一类数据容器</p>
<p><strong>列表、元组、字符串均可以视为序列</strong></p>
<h2 id="序列的切片操作"><a href="#序列的切片操作" class="headerlink" title="序列的切片操作"></a>序列的切片操作</h2><p>语法:序列[起始下标:结束下标:步长]              （左闭右开）</p>
<p>表示从序列中，从指定位置开始，依次取出元素，到指定位置结束，得到一个新序列:</p>
<ul>
<li>起始下标表示从何处开始，可以省略，视作从头开始</li>
<li>结束下标(不含)表示何处结束，可以省略，视作截取到结尾</li>
<li>步长表示依次取元素的间隔，可以省略，视作步长为1<ul>
<li>步长1表示，一个个取元素</li>
<li>步长2表示，每次跳过1个元素取</li>
<li>步长N表示，每次跳过N-1个元素取</li>
<li>步长为负数，表示反向取(注意，起始下标和结束下标也要反向标记)</li>
</ul>
</li>
</ul>
<p>注意，<strong>此操作不会影响序列本身，而是会得到一个新的序列(列表、元组、字符串)</strong></p>
<hr>
<h1 id="set"><a href="#set" class="headerlink" title="set"></a>set</h1><h2 id="集合的定义格式"><a href="#集合的定义格式" class="headerlink" title="集合的定义格式"></a>集合的定义格式</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义集合字面量</span></span><br><span class="line">&#123;元素，元素，..，元素&#125;</span><br><span class="line"><span class="comment"># 定义集合变量</span></span><br><span class="line">变量名称=&#123;元素，元素，......，元素&#125;</span><br><span class="line"><span class="comment"># 定义空集合</span></span><br><span class="line">变量名称 = <span class="built_in">set</span>()</span><br></pre></td></tr></table></figure>

<h2 id="集合的特点"><a href="#集合的特点" class="headerlink" title="集合的特点"></a>集合的特点</h2><ul>
<li>可以容纳多个数据</li>
<li>可以容纳不同类型的数据(混装)</li>
<li><strong>数据是无序存储的(不支持下标索引)</strong></li>
<li><strong>不允许重复数据存在</strong></li>
<li>可以修改(增加或删除元素等)</li>
<li>支持for循环</li>
</ul>
<h2 id="集合的常见操作"><a href="#集合的常见操作" class="headerlink" title="集合的常见操作"></a>集合的常见操作</h2><table>
<thead>
<tr>
<th align="center">编号</th>
<th align="center">操作</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">1</td>
<td align="center">集合.add(元素)</td>
<td align="center">集合内添加一个元素</td>
</tr>
<tr>
<td align="center">2</td>
<td align="center">集合.remove(元素)</td>
<td align="center">移除集合内指定的元素</td>
</tr>
<tr>
<td align="center">3</td>
<td align="center">集合.pop()</td>
<td align="center">从集合中随机取出一个元素</td>
</tr>
<tr>
<td align="center">4</td>
<td align="center">集合.clear()</td>
<td align="center">将集合清空</td>
</tr>
<tr>
<td align="center">5</td>
<td align="center">集合1.difference(集合2)</td>
<td align="center">得到一个新集合，取集合1和2的差集</td>
</tr>
<tr>
<td align="center">6</td>
<td align="center">集合1.difference_update(集合2)</td>
<td align="center">在集合1中消除集合1和2的交集</td>
</tr>
<tr>
<td align="center">7</td>
<td align="center">集合1.union(集合2)</td>
<td align="center">得到1个新集合，取集合1和2的并集</td>
</tr>
<tr>
<td align="center">8</td>
<td align="center">len(集合)</td>
<td align="center">得到一个整数，记录集合的元素数量</td>
</tr>
</tbody></table>
<p>list和set之间可以通过list()和set()直接转换</p>
<hr>
<h1 id="dict"><a href="#dict" class="headerlink" title="dict"></a>dict</h1><h2 id="字典的定义"><a href="#字典的定义" class="headerlink" title="字典的定义"></a>字典的定义</h2><p>字典的定义与集合一样，同样使用“{}”不过存储的元素是一个个的:键值对，如下语法: </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义字典字面量</span></span><br><span class="line">&#123;key: value, key: value,key: value&#125;</span><br><span class="line"><span class="comment"># 定义字典变量</span></span><br><span class="line">my_dict = &#123;key: value, key: value,......, key: value&#125;</span><br><span class="line"><span class="comment"># 定义空字典</span></span><br><span class="line">my_dict = &#123;&#125;    <span class="comment"># 空字典定义方式1</span></span><br><span class="line">my_dict = <span class="built_in">dict</span>()    <span class="comment"># 空字典定义方式2</span></span><br></pre></td></tr></table></figure>

<h2 id="字典的特点"><a href="#字典的特点" class="headerlink" title="字典的特点"></a>字典的特点</h2><ul>
<li>可以容纳多个数据</li>
<li>可以容纳不同类型的数据</li>
<li><strong>每一份数据是Key, Value键值对</strong></li>
<li><strong>可以通过Key获取到Value，Key不可重复(重复会覆盖)</strong></li>
<li>不支持下标索引</li>
<li>可以修改(增加、删除或更新元素等)</li>
<li>支持for循环，不支持while循环</li>
</ul>
<h2 id="字典的常用操作"><a href="#字典的常用操作" class="headerlink" title="字典的常用操作"></a>字典的常用操作</h2><table>
<thead>
<tr>
<th align="center">编号</th>
<th align="center">操作</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">1</td>
<td align="center">字典[Key]</td>
<td align="center">获取指定Key对应的Value值</td>
</tr>
<tr>
<td align="center">2</td>
<td align="center">字典[Key]&#x3D;Value</td>
<td align="center">添加或更新键值对</td>
</tr>
<tr>
<td align="center">3</td>
<td align="center">字典.pop(Key)</td>
<td align="center">取出Key对应的Value并在字典内删除此Key的键值对</td>
</tr>
<tr>
<td align="center">4</td>
<td align="center">字典.clear()</td>
<td align="center">清空字典</td>
</tr>
<tr>
<td align="center">5</td>
<td align="center">字典.keys()</td>
<td align="center">获取字典的全部Key，可用于for循环遍历字典</td>
</tr>
<tr>
<td align="center">6</td>
<td align="center">len(字典)</td>
<td align="center">计算字典内的元素数量</td>
</tr>
</tbody></table>
<p>遍历字典：</p>
<ul>
<li><strong>需要键和值</strong>：用 <code>for k, v in my_dict.items():</code></li>
<li><strong>只需要值</strong>：用 <code>for v in my_dict.values():</code> </li>
<li><strong>只需要键</strong>：用 <code>for k in my_dict.keys():</code> 或者直接 <code>for k in my_dict:</code></li>
</ul>
<hr>
<h1 id="五类数据容器总结对比"><a href="#五类数据容器总结对比" class="headerlink" title="五类数据容器总结对比"></a>五类数据容器总结对比</h1><table>
<thead>
<tr>
<th align="center"></th>
<th align="center">列表</th>
<th align="center">元组</th>
<th align="center">字符串</th>
<th align="center">集合</th>
<th align="center">字典</th>
</tr>
</thead>
<tbody><tr>
<td align="center">元素数量</td>
<td align="center">支持多个</td>
<td align="center">支持多个</td>
<td align="center">支持多个</td>
<td align="center">支持多个</td>
<td align="center">支持多个</td>
</tr>
<tr>
<td align="center">元素类型</td>
<td align="center">任意</td>
<td align="center">任意</td>
<td align="center">仅字符</td>
<td align="center">任意</td>
<td align="center">Key：ValueKey：除字典外任意类型Value：任意类型</td>
</tr>
<tr>
<td align="center">下标索引</td>
<td align="center">支持</td>
<td align="center">支持</td>
<td align="center">支持</td>
<td align="center">不支持</td>
<td align="center">不支持</td>
</tr>
<tr>
<td align="center">重复元素</td>
<td align="center">支持</td>
<td align="center">支持</td>
<td align="center">支持</td>
<td align="center">不支持</td>
<td align="center">不支持</td>
</tr>
<tr>
<td align="center">可修改性</td>
<td align="center">支持</td>
<td align="center">不支持</td>
<td align="center">不支持</td>
<td align="center">支持</td>
<td align="center">支持</td>
</tr>
<tr>
<td align="center">数据有序</td>
<td align="center">是</td>
<td align="center">是</td>
<td align="center">是</td>
<td align="center">否</td>
<td align="center">否</td>
</tr>
<tr>
<td align="center">使用场景</td>
<td align="center">可修改、可重复的一批数据记录场景</td>
<td align="center">不可修改、可重复的一批数据记录场景</td>
<td align="center">一串字符的记录场景</td>
<td align="center">不可重复的数据记录场景</td>
<td align="center">以Key检索Value的数据记录场景</td>
</tr>
</tbody></table>
<hr>
<h1 id="数据容器通用操作"><a href="#数据容器通用操作" class="headerlink" title="数据容器通用操作"></a>数据容器通用操作</h1><h2 id="通用操作"><a href="#通用操作" class="headerlink" title="通用操作"></a>通用操作</h2><table>
<thead>
<tr>
<th align="center">功能</th>
<th align="center">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">通用for循环</td>
<td align="center">遍历容器(字典是遍历key)</td>
</tr>
<tr>
<td align="center">max</td>
<td align="center">容器内最大元素</td>
</tr>
<tr>
<td align="center">min(）</td>
<td align="center">容器内最小元素</td>
</tr>
<tr>
<td align="center">len()</td>
<td align="center">容器元素个数</td>
</tr>
<tr>
<td align="center">list()</td>
<td align="center">转换为列表</td>
</tr>
<tr>
<td align="center">tuple()</td>
<td align="center">转换为元组</td>
</tr>
<tr>
<td align="center">str()</td>
<td align="center">转换为字符串</td>
</tr>
<tr>
<td align="center">set()</td>
<td align="center">转换为集合</td>
</tr>
<tr>
<td align="center">sorted(序列, [reverse&#x3D;True])</td>
<td align="center">排序，reverse&#x3D;True表示降序<strong>得到一个排好序的列表</strong></td>
</tr>
</tbody></table>
<h2 id="sort排序方法"><a href="#sort排序方法" class="headerlink" title="sort排序方法"></a>sort排序方法</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">列表.sort(key=选择排序依据的函数，reverse=<span class="literal">True</span>|<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>
<ul>
<li>参数key，要求传入一个函数，表示将列表的每一个元素都传入函数中，返回排序的依据</li>
<li>参数reverse，是否反转排序结果，True表示降序，False表示升序</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 要求对外层列表进行排序，排序的依据是内层列表的第二个元素数字</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 带名函数形式</span></span><br><span class="line">my_list = [[<span class="string">&quot;a&quot;</span>, <span class="number">33</span>], [<span class="string">&quot;b&quot;</span>, <span class="number">55</span>], [<span class="string">&quot;e&quot;</span>, <span class="number">11</span>]]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义排序方法————意思是按照内层列表中的第二个元素进行排序</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">choose_sort_key</span>(<span class="params">element</span>):</span><br><span class="line">	<span class="keyword">return</span> element[<span class="number">1</span>]</span><br><span class="line">	</span><br><span class="line">my_list.sort(key=choose_sort_key, reverse=<span class="literal">True</span>)</span><br><span class="line"><span class="built_in">print</span>(my_list)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 匿名函数形式，lambda函数</span></span><br><span class="line">my_list = [[<span class="string">&quot;a&quot;</span>, <span class="number">33</span>], [<span class="string">&quot;b&quot;</span>, <span class="number">55</span>], [<span class="string">&quot;c&quot;</span>, <span class="number">11</span>]]</span><br><span class="line"></span><br><span class="line">my_list.sort(key=<span class="keyword">lambda</span> element:element[<span class="number">1</span>], reverse=<span class="literal">True</span>)</span><br><span class="line"><span class="built_in">print</span>(my_list)</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="字符串大小比较"><a href="#字符串大小比较" class="headerlink" title="字符串大小比较"></a>字符串大小比较</h1><p>1.字符串如何比较</p>
<p>从头到尾，一位位进行比较，其中一位大，后面就无需比较了。</p>
<p>2.单个字符之间如何确定大小?</p>
<p>通过ASCII码表，根据字符对应的码值数字来确定大小</p>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/05_Data_Containers" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>
]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>数据容器</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 条件判断与逻辑控制</title>
    <url>/2026/03/13/Python/%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD%E4%B8%8E%E9%80%BB%E8%BE%91%E6%8E%A7%E5%88%B6/</url>
    <content><![CDATA[<h1 id="布尔类型和比较运算符"><a href="#布尔类型和比较运算符" class="headerlink" title="布尔类型和比较运算符"></a>布尔类型和比较运算符</h1><h2 id="布尔类型"><a href="#布尔类型" class="headerlink" title="布尔类型"></a>布尔类型</h2><p>在 Python 中，布尔类型的字面量用 <code>True</code> 表示真，<code>False</code> 表示假。</p>
<h2 id="比较运算符"><a href="#比较运算符" class="headerlink" title="比较运算符"></a>比较运算符</h2><p>在逻辑判断中，我们经常需要对比两个值。Python 提供了以下比较运算符：</p>
<table>
<thead>
<tr>
<th align="center">运算符</th>
<th align="center">描述说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>==</code></td>
<td align="center">判断内容是否相等，满足为True，不满足为False</td>
</tr>
<tr>
<td align="center"><code>!=</code></td>
<td align="center">判断内容是否不相等，满足为True，不满足为 False</td>
</tr>
<tr>
<td align="center"><code>&gt;</code></td>
<td align="center">判断运算符左侧内容是否大于右侧满足为True，不满足为False</td>
</tr>
<tr>
<td align="center"><code>&lt;</code></td>
<td align="center">判断运算符左侧内容是否小于右侧满足为True，不满足为 False</td>
</tr>
<tr>
<td align="center"><code>&gt;=</code></td>
<td align="center">判断运算符左侧内容是否大于等于右侧满足为True，不满足为 False</td>
</tr>
<tr>
<td align="center"><code>&lt;=</code></td>
<td align="center">判断运算符左侧内容是否小于等于右侧满足为True，不满足为 False</td>
</tr>
</tbody></table>
<hr>
<h1 id="if-语句"><a href="#if-语句" class="headerlink" title="if 语句"></a>if 语句</h1><p>最基础的条件判断语句格式如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> 要判断的条件:</span><br><span class="line">    满足条件时要做的事情<span class="number">1</span></span><br><span class="line">    满足条件时要做的事情<span class="number">2</span></span><br><span class="line">    ...</span><br><span class="line">    满足条件时要做的事情N</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>⚠️ 注意事项：</strong></p>
<ul>
<li>判断条件的结果一定要是 <code>bool</code> 类型 。</li>
<li>判断条件后必须要有<strong>冒号</strong> <code>:</code> 。</li>
<li>归属于 <code>if</code> 语句的代码块，需在行前填充<strong>4个空格缩进 (1个Tab)</strong> 。</li>
<li>Python 是严格通过空格缩进来判断代码归属的 。</li>
</ul>
</blockquote>
<hr>
<h2 id="if-else-组合判断语句"><a href="#if-else-组合判断语句" class="headerlink" title="if-else 组合判断语句"></a>if-else 组合判断语句</h2><p>当条件不满足时，如果我们想执行另一套逻辑，就需要加入 <code>else</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> 要判断的条件:</span><br><span class="line">    满足条件时要做的事情<span class="number">1</span></span><br><span class="line">    满足条件时要做的事情<span class="number">2</span></span><br><span class="line">    ...</span><br><span class="line">    满足条件时要做的事情N</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    不满足条件时要做的事情<span class="number">1</span></span><br><span class="line">    不满足条件时要做的事情<span class="number">2</span></span><br><span class="line">    ...</span><br><span class="line">    不满足条件时要做的事情N</span><br></pre></td></tr></table></figure>

<p><strong>注意：</strong></p>
<ul>
<li><code>else</code> 后面不需要再写判断条件 </li>
<li><code>else</code> 后的代码块同样需要四个空格作为缩进</li>
</ul>
<h2 id="if-elif-else-组合语句"><a href="#if-elif-else-组合语句" class="headerlink" title="if-elif-else 组合语句"></a>if-elif-else 组合语句</h2><p>面对多种情况的条件分支，可以使用 <code>elif</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> 条件<span class="number">1</span>:</span><br><span class="line">    满足条件<span class="number">1</span>时要做的事情<span class="number">1</span></span><br><span class="line">    ...</span><br><span class="line">    满足条件<span class="number">1</span>时要做的事情N</span><br><span class="line"><span class="keyword">elif</span> 条件<span class="number">2</span>:</span><br><span class="line">    满足条件<span class="number">2</span>时要做的事情<span class="number">1</span></span><br><span class="line">    ...</span><br><span class="line">    满足条件<span class="number">2</span>时要做的事情N</span><br><span class="line"><span class="keyword">elif</span> 条件N:</span><br><span class="line">    满足条件N时要做的事情<span class="number">1</span></span><br><span class="line">    ...</span><br><span class="line">    满足条件N时要做的事情N</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    所有条件都不满足要做的事情<span class="number">1</span></span><br><span class="line">    ...</span><br><span class="line">    所有条件都不满足要做的事情N</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>⚠️ 注意事项：</strong></p>
<p><code>elif</code> 可以写多个，且每个条件后面都要加冒号 。每个判断是<strong>互斥且有序</strong>的，只要上面的某一个条件满足了，后面的条件就不会再进行判断了 。</p>
</blockquote>
<hr>
<h2 id="判断语句的嵌套"><a href="#判断语句的嵌套" class="headerlink" title="判断语句的嵌套"></a>判断语句的嵌套</h2><p>在实际开发中，经常会遇到“在某个前提条件下，再进行二次判断”的场景。这就需要用到嵌套结构：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> 条件<span class="number">1</span>:</span><br><span class="line">    满足条件<span class="number">1</span>时要做的事情<span class="number">1</span></span><br><span class="line">    ...</span><br><span class="line">    满足条件<span class="number">1</span>时要做的事情N</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> 条件<span class="number">2</span>:</span><br><span class="line">        满足条件<span class="number">2</span>时要做的事情<span class="number">1</span></span><br><span class="line">        ...</span><br><span class="line">        满足条件<span class="number">2</span>时要做的事情N</span><br></pre></td></tr></table></figure>

<p><strong>层级法则：</strong> Python 完全通过缩进来确定层级的包含关系 。内层嵌套的 <code>if</code> 需要叠加缩进。</p>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage1_Core_Basics/02_Condition_Statements" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>基础语法与数据结构</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>逻辑控制</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 进阶编程：闭包、装饰器与并发网络基础</title>
    <url>/2026/05/29/Python/%E9%97%AD%E5%8C%85%E3%80%81%E8%A3%85%E9%A5%B0%E5%99%A8%E4%B8%8E%E5%A4%9A%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h1><p>闭包：</p>
<ul>
<li>定义双层嵌套函数，内层函数可以访问外层函数的变量</li>
<li>将内存函数作为外层函数的返回，此内层函数就是闭包函数</li>
</ul>
<p>闭包的优点和缺点：</p>
<ul>
<li>优点:<ul>
<li>不定义全局变量，也可以让函数持续访问和修改一个外部变量 </li>
<li>闭包函数引用的外部变量，是外层函数的内部变量。作用域封闭难以被误操作修改</li>
</ul>
</li>
<li>缺点:<ul>
<li>额外的内存占用</li>
</ul>
</li>
</ul>
<p>nonlocal关键字的作用：<br>在闭包函数(内部函数中)想要修改外部函数的变量值需要用<code>nonlocal</code>声明这个外部变量</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">outer</span>(<span class="params">num1</span>):  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">inner</span>(<span class="params">num2</span>):  </span><br><span class="line">        <span class="keyword">nonlocal</span> num1  </span><br><span class="line">        num1 += num2  </span><br><span class="line">        <span class="built_in">print</span>(num1)  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">return</span> inner  </span><br><span class="line">  </span><br><span class="line">fn = outer(<span class="number">20</span>)  </span><br><span class="line">fn(<span class="number">10</span>)</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="装饰器"><a href="#装饰器" class="headerlink" title="装饰器"></a>装饰器</h1><p>装饰器就是使用创建一个闭包函数，在闭包函数内调用目标函数。<br>可以达到不改动目标函数的同时，增加额外的功能。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">outer</span>(<span class="params">func</span>):  </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">inner</span>():  </span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;我要睡觉了&quot;</span>)  </span><br><span class="line">        func()  </span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;我要起床了&quot;</span>)  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> inner  </span><br><span class="line"></span><br><span class="line"><span class="meta">@outer  </span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">sleep</span>():  </span><br><span class="line">    <span class="keyword">import</span> random  </span><br><span class="line">    <span class="keyword">import</span> time  </span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Sleeping...&quot;</span>)  </span><br><span class="line">    time.sleep(random.randint(<span class="number">1</span>, <span class="number">5</span>))  </span><br><span class="line">  </span><br><span class="line">sleep()</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="设计模式"><a href="#设计模式" class="headerlink" title="设计模式"></a>设计模式</h1><p>设计模式是一种编程套路，可以极大的方便程序的开发。<br>最常见、最经典的设计模式，就是面向对象。</p>
<p>除了面向对象外，在编程中也有很多既定的套路可以方便开发，我们称之为设计模式:<br>单例、工厂模式、建造者、责任链、状态、备忘录、解释器、访问者、观察者、中介、模板、代理模式等等</p>
<h2 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h2><p>单例模式就是对一个类，只获取其唯一的类实例对象，持续复用它。</p>
<ul>
<li>节省内存</li>
<li>节省创建对象的开销</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 单例模式实现  </span></span><br><span class="line"><span class="keyword">from</span> str_tools <span class="keyword">import</span> str_tool</span><br><span class="line"></span><br><span class="line">s1 = str_tool</span><br><span class="line">s2 = str_tool</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(s1)</span><br><span class="line"><span class="built_in">print</span>(s2)</span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">id</span>(s1))</span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">id</span>(s2))</span><br></pre></td></tr></table></figure>

<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">StrTools</span>:</span><br><span class="line">    <span class="keyword">pass</span></span><br><span class="line">  </span><br><span class="line">str_tool = StrTools()</span><br></pre></td></tr></table></figure>
<h2 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h2><p>工厂模式是将对象的创建由使用原生类本身创建转换到由特定的工厂方法来创建。</p>
<p>好处：</p>
<ul>
<li>大批量创建对象的时候有统一的入口，易于代码维护</li>
<li>当发生修改，仅修改工厂类的创建方法即可</li>
<li>符合现实世界的模式，即由工厂来制作产品(对象)</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span>:  </span><br><span class="line">    <span class="keyword">pass</span>  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Worker</span>(<span class="title class_ inherited__">Person</span>):  </span><br><span class="line">    <span class="keyword">pass</span>  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Teacher</span>(<span class="title class_ inherited__">Person</span>):  </span><br><span class="line">    <span class="keyword">pass</span>  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>(<span class="title class_ inherited__">Person</span>):  </span><br><span class="line">    <span class="keyword">pass</span>  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PersonFactory</span>:  </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get_person</span>(<span class="params">self, p_type</span>):  </span><br><span class="line">        <span class="keyword">if</span> p_type == <span class="string">&quot;worker&quot;</span>:  </span><br><span class="line">            <span class="keyword">return</span> Worker()  </span><br><span class="line">        <span class="keyword">elif</span> p_type == <span class="string">&quot;teacher&quot;</span>:  </span><br><span class="line">            <span class="keyword">return</span> Teacher()  </span><br><span class="line">        <span class="keyword">elif</span> p_type == <span class="string">&quot;student&quot;</span>:  </span><br><span class="line">            <span class="keyword">return</span> Student()  </span><br><span class="line">  </span><br><span class="line">pf = PersonFactory()  </span><br><span class="line">worker = pf.get_person(<span class="string">&quot;worker&quot;</span>)  </span><br><span class="line">teacher = pf.get_person(<span class="string">&quot;teacher&quot;</span>)  </span><br><span class="line">student = pf.get_person(<span class="string">&quot;student&quot;</span>)</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h1><p>现代操作系统比如Mac OSX,UNIX,Linux,Windows等，都是支持”多任务“的操作系统。</p>
<h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><p><strong>进程</strong>:一个程序运行在系统之上，便称这个程序为一个运行进程，并为其分配进程ID方便系统管理。<br><strong>多任务运行</strong>：操作系统中可以运行多个进程。</p>
<p>进程之间是内存隔离的，不同的进程拥有各自的内存空间。</p>
<h2 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h2><p><strong>线程</strong>:线程归属于进程，一个进程可以开启多个线程，执行不同的工作，是进程工作的最小单位。<br><strong>多线程运行</strong>一个进程内可以运行多个线程。</p>
<p>线程之间内存共享，线程属于进程，一个进程内的多个线程之间共享这个进程所拥有的内存空间。</p>
<h2 id="并行执行"><a href="#并行执行" class="headerlink" title="并行执行"></a>并行执行</h2><p>多个进程同时在运行，即不同的程序同时运行，称为<strong>多任务并行执行</strong><br>一个进程内的多个线程同时在运行，称为<strong>哆线程并行执行</strong></p>
<h2 id="多线程编程"><a href="#多线程编程" class="headerlink" title="多线程编程"></a>多线程编程</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time  </span><br><span class="line"><span class="keyword">import</span> threading  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">def</span> <span class="title function_">sing</span>(<span class="params">msg</span>):  </span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:  </span><br><span class="line">        <span class="built_in">print</span>(msg)  </span><br><span class="line">        time.sleep(<span class="number">1</span>)  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">def</span> <span class="title function_">dance</span>(<span class="params">msg</span>):  </span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:  </span><br><span class="line">        <span class="built_in">print</span>(msg)  </span><br><span class="line">        time.sleep(<span class="number">1</span>)  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:  </span><br><span class="line">	<span class="comment"># 创建线程</span></span><br><span class="line">    sing_thread = threading.Thread(target = sing, args = (<span class="string">&quot;singing...&quot;</span>, ))  </span><br><span class="line">    dance_thread = threading.Thread(target = dance, kwargs = &#123;<span class="string">&quot;msg&quot;</span>:<span class="string">&quot;dance...&quot;</span>&#125;)  </span><br><span class="line">	<span class="comment"># 启动线程</span></span><br><span class="line">    sing_thread.start()  </span><br><span class="line">    dance_thread.start()</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="网络编程"><a href="#网络编程" class="headerlink" title="网络编程"></a>网络编程</h1><p><strong>Socket服务端</strong>:等待其它进程的连接、可接受发来的消息、可以回复消息<br><strong>Socket客户端</strong>:主动连接服务端、可以发送消息、可以接收回复</p>
<h2 id="服务端开发"><a href="#服务端开发" class="headerlink" title="服务端开发"></a>服务端开发</h2><p>socket(简称 套接字)是进程之间通信的一个工具，负责进程之间的网络数据传输。<br>socket通讯一定要有客户端和服务端</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># socket服务端开发  </span></span><br><span class="line"><span class="keyword">import</span> socket  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 创建socket对象  </span></span><br><span class="line">socket_server = socket.socket()  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 绑定ip地址和端口  </span></span><br><span class="line">socket_server.bind((<span class="string">&#x27;localhost&#x27;</span>, <span class="number">8888</span>))  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 监听端口  </span></span><br><span class="line">socket_server.listen(<span class="number">1</span>)  </span><br><span class="line"><span class="comment"># listen方法内接受一个整数类参数，表示接收的链接数量  </span></span><br><span class="line">  </span><br><span class="line"><span class="comment"># 等待客户端连接  </span></span><br><span class="line">conn, address = socket_server.accept()</span><br><span class="line"><span class="comment"># accept方法返回的是二元元组(链接对象，客户端地址信息)</span></span><br><span class="line"><span class="comment"># 可以通过变量1,变量2=socket_server.accept()的形式,直接接收二元元组内的两个元素</span></span><br><span class="line"><span class="comment"># accept方法是阻塞的方法</span></span><br><span class="line">  </span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;接收到了客户端的连接，客户端的信息是：<span class="subst">&#123;address&#125;</span>&quot;</span>)</span><br><span class="line">  </span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="comment"># 接收客户端信息</span></span><br><span class="line">    data: <span class="built_in">str</span> = conn.recv(<span class="number">1024</span>).decode(<span class="string">&quot;utf-8&quot;</span>)</span><br><span class="line">    <span class="comment"># recv接收的参数是缓冲区大小，一般给1024即可</span></span><br><span class="line">    <span class="comment"># recv的返回值是一个字节数组（bytes对象），可通过decode方法通过utf-8解码，将字节数组转换为字符串</span></span><br><span class="line">    <span class="comment"># recv是阻塞的方法</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;客户端发来的消息是：<span class="subst">&#123;data&#125;</span>&quot;</span>)  </span><br><span class="line">  </span><br><span class="line">    <span class="comment"># 发送回复消息  </span></span><br><span class="line">    msg = <span class="built_in">input</span>(<span class="string">&quot;输入要和客户端回复的消息：&quot;</span>)  </span><br><span class="line">    <span class="keyword">if</span> msg == <span class="string">&#x27;exit&#x27;</span>:  </span><br><span class="line">        <span class="keyword">break</span>  </span><br><span class="line">    conn.send(msg.encode(<span class="string">&quot;UTF-8&quot;</span>))  <span class="comment"># 使用encode将字符串编码为字节数组对象  </span></span><br><span class="line">  </span><br><span class="line"><span class="comment"># 关闭链接  </span></span><br><span class="line">conn.close()  </span><br><span class="line">socket_server.close()</span><br></pre></td></tr></table></figure>
<h2 id="客户端开发"><a href="#客户端开发" class="headerlink" title="客户端开发"></a>客户端开发</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket  </span><br><span class="line">  </span><br><span class="line"><span class="comment">#  创建socket对象  </span></span><br><span class="line">socket_client = socket.socket()  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 连接到服务器  </span></span><br><span class="line">socket_client.connect((<span class="string">&#x27;localhost&#x27;</span>, <span class="number">8888</span>))  </span><br><span class="line">  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:  </span><br><span class="line">    <span class="comment"># 发送消息  </span></span><br><span class="line">    msg = <span class="built_in">input</span>(<span class="string">&quot;请输入要给服务端发送的消息：&quot;</span>)  </span><br><span class="line">    <span class="keyword">if</span> msg == <span class="string">&#x27;exit&#x27;</span>:  </span><br><span class="line">        <span class="keyword">break</span>  </span><br><span class="line">    socket_client.send(msg.encode(<span class="string">&#x27;UTF-8&#x27;</span>))  </span><br><span class="line">  </span><br><span class="line">    recv_data = socket_client.recv(<span class="number">1024</span>)  </span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;服务端回复的消息是：<span class="subst">&#123;recv_data.decode(<span class="string">&#x27;UTF-8&#x27;</span>)&#125;</span>&quot;</span>)  </span><br><span class="line">  </span><br><span class="line"><span class="comment"># 关闭连接  </span></span><br><span class="line">socket_client.close()</span><br></pre></td></tr></table></figure>
<hr>
<h1 id="正则表达式"><a href="#正则表达式" class="headerlink" title="正则表达式"></a>正则表达式</h1><p>正则表达式是一种字符串验证的规则，通过特殊的字符串组合来确立规则，用规则去匹配字符串是否满足<br>如<code>(^[\w-]+(\.[w-]+)*@[\w-]+(\.[\w-]+)+$)</code>可以表示为一个标准邮箱的格式</p>
<h2 id="基础匹配"><a href="#基础匹配" class="headerlink" title="基础匹配"></a>基础匹配</h2><p>re模块的三个主要方法:</p>
<ol>
<li>re.match，从头开始匹配，匹配第一个命中项</li>
<li>re.search，全局匹配，匹配第一个命中项</li>
<li>re.findall，全局匹配，匹配全部命中项</li>
</ol>
<h2 id="元字符匹配"><a href="#元字符匹配" class="headerlink" title="元字符匹配"></a>元字符匹配</h2><p>字符串的r标记，表示字符串内转移字符无效，作为普通字符使用<br>单字符匹配：</p>
<table>
<thead>
<tr>
<th align="center">字符</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">.</td>
<td align="center">匹配任意1个字符(除了\n)，<code>\.</code>匹配点本身</td>
</tr>
<tr>
<td align="center">[]</td>
<td align="center">匹配[]中列举的字符</td>
</tr>
<tr>
<td align="center">\d</td>
<td align="center">匹配数字，即0-9</td>
</tr>
<tr>
<td align="center">\D</td>
<td align="center">匹配非数字</td>
</tr>
<tr>
<td align="center">\s</td>
<td align="center">匹配空白，即空格、tab键</td>
</tr>
<tr>
<td align="center">\S</td>
<td align="center">匹配非空白</td>
</tr>
<tr>
<td align="center">\w</td>
<td align="center">匹配单词字符，即a-z、A-Z、0-9、_</td>
</tr>
<tr>
<td align="center">\W</td>
<td align="center">匹配非单词字符</td>
</tr>
<tr>
<td align="center">数量匹配：</td>
<td align="center"></td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">字符</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">**</td>
<td align="center">匹配前一个规则的字符出现0至无数次</td>
</tr>
<tr>
<td align="center">+</td>
<td align="center">匹配前一个规则的字符出现1至无数次</td>
</tr>
<tr>
<td align="center">？</td>
<td align="center">匹配前一个规则的字符出现0次或1次</td>
</tr>
<tr>
<td align="center">{m}</td>
<td align="center">匹配前一个规则的字符出现m次</td>
</tr>
<tr>
<td align="center">{m,}</td>
<td align="center">匹配前一个规则的字符出现最少m次</td>
</tr>
<tr>
<td align="center">{m,n}</td>
<td align="center">匹配前一个规则的字符出现m到n次</td>
</tr>
<tr>
<td align="center">边界匹配：</td>
<td align="center"></td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">字符</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">^</td>
<td align="center">匹配字符串开头</td>
</tr>
<tr>
<td align="center">$</td>
<td align="center">匹配字符串结尾</td>
</tr>
<tr>
<td align="center">\b</td>
<td align="center">匹配一个单词的边界</td>
</tr>
<tr>
<td align="center">\B</td>
<td align="center">匹配非单词边界</td>
</tr>
<tr>
<td align="center">分组匹配：</td>
<td align="center"></td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">字符</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">|</td>
<td align="center">匹配左右任意一个表达式</td>
</tr>
<tr>
<td align="center">()</td>
<td align="center">将括号中字符作为一个分组</td>
</tr>
</tbody></table>
<hr>
<h1 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h1><p>递归：方法（函数）自己调用自己的一种特殊编程写法<br>递归需要注意：</p>
<ul>
<li>退出的条件，否则容易变成无限递归</li>
<li>返回值的传递，确保从最内层，层层传递到最外层</li>
</ul>
<p>os模块的3个方法：</p>
<ul>
<li>os.listdir，列出指定目录下的内容</li>
<li>os.path.isdir，判断给定路径是否是文件夹，是返回True，否则返回False</li>
<li>os.path.exists，判断给定路径是否存在，存在返回True，否则返回False</li>
</ul>
<div class="note default simple"><p><strong>Repository</strong></p>
<p>本章示例代码已归档至 GitHub，可用于本地复现、调试与扩展。</p>
<p><a class="btn-beautify " href="https://github.com/NimbusFan/Python-Zero-to-Hero/tree/main/Stage4_Advanced_Techniques/01_Advanced_Techniques" title="查看本章源码"><i class="fab fa-github"></i><span>查看本章源码</span></a></p>
</div>]]></content>
      <categories>
        <category>Python 技术笔记</category>
        <category>进阶编程与工程基础</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>闭包</tag>
        <tag>装饰器</tag>
        <tag>设计模式</tag>
        <tag>多线程</tag>
        <tag>Socket</tag>
        <tag>正则表达式</tag>
        <tag>递归</tag>
      </tags>
  </entry>
  <entry>
    <title>Git &amp; GitHub：一文理清版本控制、分支、回退与协作流程</title>
    <url>/2026/06/13/daily/Git%20&amp;%20GitHub/</url>
    <content><![CDATA[<p>最近手头事情扎堆，新项目刚起步，老项目还得交接，每天在不同的 Repo里切来切去。决定挤出点时间，把 Git 版本控制逻辑学一下。</p>
<p>本文根据 <a href="https://www.youtube.com/watch?v=mAFoROnOfHs">Git &amp; GitHub Crash Course for Beginners [2026]</a> 整理，并结合实际开发中的常见场景进行了补充。</p>
<p>Git 的作用，本质上是帮我们管理项目的变化。它会记录每一次修改，让代码的演进过程变得可追踪、可比较、可回退。无论是一个人写项目，还是多人协作开发，Git 都能让我们清楚地知道：代码改了什么、是谁改的、什么时候改的，以及需要时如何回到之前的版本。<code>add</code>、<code>commit</code>、<code>push</code>、<code>pull</code>、<code>merge</code> 这些命令。单独看每个命令并不难，但如果只零散地记命令，一旦遇到分支冲突、版本回退或者远程同步问题，很容易不知道该从哪里下手。因此我们要先理解 Git 背后的工作流：本地修改如何变成一次提交，提交历史如何被查看和回退，分支如何支撑并行开发，远程仓库如何连接团队成员，以及 Pull Request 如何在代码合并前提供审查和讨论的空间。</p>
<hr>
<h1 id="Git-工作流"><a href="#Git-工作流" class="headerlink" title="Git 工作流"></a>Git 工作流</h1><p>Git 的流程可以概括为一句话：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">工作区修改代码，暂存区选择内容，本地仓库保存版本，远程仓库同步协作。</span><br></pre></td></tr></table></figure>

<p>对应到命令就是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;feat: add login page&quot;</span></span><br><span class="line">git push</span><br></pre></td></tr></table></figure>

<p>这三个命令分别对应 Git 的三个关键动作：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">git add     ：把工作区修改放入暂存区</span><br><span class="line">git commit  ：把暂存区内容保存成本地提交</span><br><span class="line">git push    ：把本地提交推送到远程仓库</span><br></pre></td></tr></table></figure>

<p>也就是说，Git 并不是一改文件就自动保存一个版本。它要求你主动选择哪些修改要进入下一次提交，然后再用 commit 固化成历史记录。</p>
<p>这也是暂存区存在的意义：它让你可以控制一次提交到底包含哪些内容。</p>
<p>例如，你同时修改了登录功能、README 文档和配置文件，但这三类修改并不属于同一个任务。此时就不应该全部一股脑提交，而是可以分批暂存、分批提交：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add src/login.py</span><br><span class="line">git commit -m <span class="string">&quot;feat: add login logic&quot;</span></span><br><span class="line"></span><br><span class="line">git add README.md</span><br><span class="line">git commit -m <span class="string">&quot;docs: update login usage&quot;</span></span><br><span class="line"></span><br><span class="line">git add config.yml</span><br><span class="line">git commit -m <span class="string">&quot;chore: update login config&quot;</span></span><br></pre></td></tr></table></figure>

<p>好的提交应该像项目历史中的一个清晰节点，让别人能看懂这次修改做了什么。</p>
<hr>
<h1 id="本地版本记录"><a href="#本地版本记录" class="headerlink" title="本地版本记录"></a>本地版本记录</h1><p>创建 Git 仓库通常有两种方式。</p>
<p>第一种是在已有项目中初始化：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> my-project</span><br><span class="line">git init</span><br></pre></td></tr></table></figure>

<p>执行后，项目目录中会出现一个隐藏文件夹：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">.git/</span><br></pre></td></tr></table></figure>

<p>这个 <code>.git</code> 文件夹保存了 Git 的核心数据，包括提交历史、分支信息、版本对象和本地配置。它就是当前项目成为 Git 仓库的根本原因。</p>
<p>第二种方式是从远程仓库克隆：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/username/repository.git</span><br></pre></td></tr></table></figure>

<p>克隆下来的项目天然就是一个 Git 仓库，因为它内部同样包含 <code>.git/</code> 目录。</p>
<hr>
<h2 id="查看当前状态"><a href="#查看当前状态" class="headerlink" title="查看当前状态"></a>查看当前状态</h2><p>日常开发中，最应该频繁使用的命令是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br></pre></td></tr></table></figure>

<p>它可以告诉你：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">哪些文件被修改了</span><br><span class="line">哪些文件是新建的</span><br><span class="line">哪些文件还没有被跟踪</span><br><span class="line">哪些文件已经进入暂存区</span><br><span class="line">当前所在分支</span><br></pre></td></tr></table></figure>

<p>常见状态包括：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">modified    ：文件被修改</span><br><span class="line">untracked   ：新文件，还没有被 Git 跟踪</span><br><span class="line">staged      ：已经暂存，等待提交</span><br><span class="line">clean       ：工作区干净，没有需要提交的内容</span><br></pre></td></tr></table></figure>

<p><code>git status</code> 就像 Git 的仪表盘。<br>在执行 <code>add</code>、<code>commit</code>、<code>merge</code>、<code>reset</code> 这类操作前，先看一眼状态，可以避免很多误操作。</p>
<hr>
<h2 id="添加暂存"><a href="#添加暂存" class="headerlink" title="添加暂存"></a>添加暂存</h2><p>添加单个文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add README.md</span><br></pre></td></tr></table></figure>

<p>添加当前目录及子目录下的修改：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br></pre></td></tr></table></figure>

<p>添加整个仓库中的所有修改：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add -A</span><br></pre></td></tr></table></figure>

<p>实际开发中，最常用的是在项目根目录执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add -A</span><br></pre></td></tr></table></figure>

<p>不太推荐把 <code>git add *</code> 作为默认习惯，因为它容易受到当前目录和通配符规则影响，对删除文件的处理也不如 <code>git add .</code> 或 <code>git add -A</code> 直观。</p>
<p>如果已经暂存但暂时不想提交，可以取消暂存：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git restore --staged README.md</span><br></pre></td></tr></table></figure>

<p>取消所有暂存：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git restore --staged .</span><br></pre></td></tr></table></figure>

<p>旧写法也可以：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset</span><br></pre></td></tr></table></figure>

<p>取消暂存不会删除代码修改，只是把文件从暂存区退回到工作区。</p>
<hr>
<h2 id="提交版本"><a href="#提交版本" class="headerlink" title="提交版本"></a>提交版本</h2><p>提交暂存区内容：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;feat: add user login&quot;</span></span><br></pre></td></tr></table></figure>

<p>提交信息不要写得太随意。</p>
<p>不推荐：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;update&quot;</span></span><br><span class="line">git commit -m <span class="string">&quot;fix&quot;</span></span><br><span class="line">git commit -m <span class="string">&quot;123&quot;</span></span><br></pre></td></tr></table></figure>

<p>推荐：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;feat: add user login&quot;</span></span><br><span class="line">git commit -m <span class="string">&quot;fix: resolve token expiration&quot;</span></span><br><span class="line">git commit -m <span class="string">&quot;docs: update installation guide&quot;</span></span><br></pre></td></tr></table></figure>

<p>一种常见的提交信息格式是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">类型: 简短描述</span><br></pre></td></tr></table></figure>

<p>常用类型包括：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">feat      新功能</span><br><span class="line">fix       修复问题</span><br><span class="line">docs      文档修改</span><br><span class="line">style     格式调整</span><br><span class="line">refactor  代码重构</span><br><span class="line">test      测试相关</span><br><span class="line">chore     构建、配置、依赖等杂项</span><br></pre></td></tr></table></figure>

<p>提交信息的核心目标不是“写得正式”，而是让团队成员和未来的自己能够快速理解这次提交的目的。</p>
<hr>
<h2 id="查看提交历史和差异"><a href="#查看提交历史和差异" class="headerlink" title="查看提交历史和差异"></a>查看提交历史和差异</h2><p>查看完整历史：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span></span><br></pre></td></tr></table></figure>

<p>简洁查看：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline</span><br></pre></td></tr></table></figure>

<p>更推荐的图形化查看方式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline --graph --decorate --all</span><br></pre></td></tr></table></figure>

<p>它可以直观看到分支、合并和提交之间的关系。</p>
<p>查看当前工作区修改：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff</span><br></pre></td></tr></table></figure>

<p>查看已经暂存但还没提交的修改：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff --staged</span><br></pre></td></tr></table></figure>

<p>比较两个提交：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff 提交ID1 提交ID2</span><br></pre></td></tr></table></figure>

<p>比较两个分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff main development</span><br></pre></td></tr></table></figure>

<p>diff 中常见符号：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">- 表示删除</span><br><span class="line">+ 表示新增</span><br><span class="line">空格表示没有变化的上下文</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line"> one</span><br><span class="line"><span class="deletion">-Hello</span></span><br><span class="line"><span class="addition">+Hello Git</span></span><br></pre></td></tr></table></figure>

<p>表示删除了 <code>Hello</code>，新增了 <code>Hello Git</code>。</p>
<p>需要注意，<code>git diff</code> 的比较顺序会影响显示结果：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff 旧版本 新版本</span><br></pre></td></tr></table></figure>

<p>表示从旧版本变成新版本发生了什么。</p>
<hr>
<h1 id="分支"><a href="#分支" class="headerlink" title="分支"></a>分支</h1><p>分支是 Git 最重要的能力之一。</p>
<p>可以把分支理解为一条独立的开发线。默认主分支通常叫：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">main</span><br></pre></td></tr></table></figure>

<p>以前很多项目使用：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">master</span><br></pre></td></tr></table></figure>

<p>主分支通常代表稳定版本。新功能、实验性修改、bug 修复都不应该直接在主分支上完成，而应该创建独立分支。</p>
<p>常见开发方式是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">main 分支保持稳定</span><br><span class="line">feature 分支开发新功能</span><br><span class="line">fix 分支修复问题</span><br><span class="line">开发完成后再合并回 main</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="分支的基本操作"><a href="#分支的基本操作" class="headerlink" title="分支的基本操作"></a>分支的基本操作</h2><p>查看分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch</span><br></pre></td></tr></table></figure>

<p>创建分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch development</span><br></pre></td></tr></table></figure>

<p>切换分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch development</span><br></pre></td></tr></table></figure>

<p>旧写法是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout development</span><br></pre></td></tr></table></figure>

<p>创建并切换分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch -c feature/login</span><br></pre></td></tr></table></figure>

<p>旧写法是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout -b feature/login</span><br></pre></td></tr></table></figure>

<p>删除本地分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch -d feature/login</span><br></pre></td></tr></table></figure>

<p>强制删除：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch -D feature/login</span><br></pre></td></tr></table></figure>

<p>对于初学者，更推荐使用 <code>git switch</code>。<br>因为 <code>checkout</code> 既能切换分支，也能切换历史提交，语义比较混杂；而 <code>switch</code> 专门用于分支切换，更直观。</p>
<hr>
<h2 id="合并分支"><a href="#合并分支" class="headerlink" title="合并分支"></a>合并分支</h2><p>当功能分支开发完成后，需要合并回主分支。</p>
<p>例如把 <code>development</code> 合并到 <code>main</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch main</span><br><span class="line">git merge development</span><br></pre></td></tr></table></figure>

<p>这条命令的含义是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">把 development 分支的修改合并到当前 main 分支。</span><br></pre></td></tr></table></figure>

<p>合并并不总是复杂。<br>如果两个分支修改的内容没有冲突，Git 通常可以自动完成合并。</p>
<p>但如果两个分支修改了同一个文件的同一部分，Git 无法判断应该保留哪一份，就会产生冲突。</p>
<hr>
<h2 id="解决合并冲突"><a href="#解决合并冲突" class="headerlink" title="解决合并冲突"></a>解决合并冲突</h2><p>冲突文件中通常会出现类似内容：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span><br><span class="line">当前分支的内容</span><br><span class="line">=======</span><br><span class="line">被合并分支的内容</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; development</span><br></pre></td></tr></table></figure>

<p>这几段标记分别表示：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD          当前分支的内容开始</span><br><span class="line">=======               两个版本的分隔线</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; development   被合并分支的内容结束</span><br></pre></td></tr></table></figure>

<p>解决冲突时，你需要手动决定最终保留什么内容：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">只保留当前分支</span><br><span class="line">只保留对方分支</span><br><span class="line">两个都保留</span><br><span class="line">重新整理成新的内容</span><br></pre></td></tr></table></figure>

<p>解决后删除冲突标记，然后执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;resolve merge conflict&quot;</span></span><br></pre></td></tr></table></figure>

<p>如果发现这次合并不应该继续，可以中止：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge --abort</span><br></pre></td></tr></table></figure>

<p>合并冲突并不是 Git 出错，而是 Git 把无法自动判断的业务选择交还给开发者。</p>
<hr>
<h1 id="远程协作"><a href="#远程协作" class="headerlink" title="远程协作"></a>远程协作</h1><p>本地提交只存在于自己的电脑上。<br>如果要备份代码、跨设备同步，或者和团队协作，就需要远程仓库。</p>
<p>GitHub 就是最常见的远程仓库平台之一。</p>
<p>查看远程仓库：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote -v</span><br></pre></td></tr></table></figure>

<p>添加远程仓库：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote add origin 仓库地址</span><br></pre></td></tr></table></figure>

<p>修改远程地址：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote set-url origin 新仓库地址</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="推送代码"><a href="#推送代码" class="headerlink" title="推送代码"></a>推送代码</h2><p>第一次推送 main 分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push -u origin main</span><br></pre></td></tr></table></figure>

<p>之后可以直接：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push</span><br></pre></td></tr></table></figure>

<p>推送其他分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push -u origin feature/login</span><br></pre></td></tr></table></figure>

<p>这里：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">origin 是远程仓库名称</span><br><span class="line">main 或 feature/login 是分支名称</span><br><span class="line">-u 表示建立本地分支和远程分支的关联</span><br></pre></td></tr></table></figure>

<p>建立关联后，当前分支后续可以直接使用 <code>git push</code> 和 <code>git pull</code>。</p>
<hr>
<h2 id="获取远程更新"><a href="#获取远程更新" class="headerlink" title="获取远程更新"></a>获取远程更新</h2><p><code>git fetch</code> 会下载远程更新，但不会自动合并到当前工作区：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git fetch</span><br></pre></td></tr></table></figure>

<p>它适合用来先查看远程发生了什么。</p>
<p><code>git pull</code> 会下载远程更新，并自动合并到当前分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull</span><br></pre></td></tr></table></figure>

<p>可以简单理解为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">git pull = git fetch + git merge</span><br></pre></td></tr></table></figure>

<p>团队项目中，更稳妥的习惯是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git fetch</span><br><span class="line">git <span class="built_in">log</span> --oneline --graph --decorate --all</span><br><span class="line">git pull</span><br></pre></td></tr></table></figure>

<p>如果团队希望提交历史更线性，也可以使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git pull --rebase</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Pull-Request-工作流"><a href="#Pull-Request-工作流" class="headerlink" title="Pull Request 工作流"></a>Pull Request 工作流</h2><p>Pull Request，简称 PR。</p>
<p>它的本质是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">我在某个分支完成了修改，请求把这些修改合并到目标分支。</span><br></pre></td></tr></table></figure>

<p>在 GitHub 创建 PR 时，通常会选择：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">base    ：目标分支，例如 main</span><br><span class="line">compare ：来源分支，例如 feature/login</span><br></pre></td></tr></table></figure>

<p>也就是说：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">把 compare 分支的修改合并到 base 分支。</span><br></pre></td></tr></table></figure>

<p>一个典型 PR 流程如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 从 main 拉取最新代码</span><br><span class="line">2. 创建 feature 分支</span><br><span class="line">3. 在 feature 分支开发</span><br><span class="line">4. commit 并 push 到 GitHub</span><br><span class="line">5. 创建 Pull Request</span><br><span class="line">6. 团队成员 Review</span><br><span class="line">7. 修改问题</span><br><span class="line">8. 合并到 main</span><br><span class="line">9. 删除临时分支</span><br></pre></td></tr></table></figure>

<p>对应命令示例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch main</span><br><span class="line">git pull</span><br><span class="line"></span><br><span class="line">git switch -c feature/login</span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改代码</span></span><br><span class="line"></span><br><span class="line">git status</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;feat: add login page&quot;</span></span><br><span class="line"></span><br><span class="line">git push -u origin feature/login</span><br></pre></td></tr></table></figure>

<p>然后在 GitHub 页面创建 Pull Request。</p>
<p>一个好的 PR 不应该只写“完成了”三个字，而应该说明：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">这次改了什么</span><br><span class="line">为什么要改</span><br><span class="line">如何测试</span><br><span class="line">是否影响已有功能</span><br><span class="line">关联了哪个 Issue</span><br></pre></td></tr></table></figure>

<p>示例：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="section">## What changed</span></span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> Added login page</span><br><span class="line"><span class="bullet">-</span> Added token validation</span><br><span class="line"><span class="bullet">-</span> Updated user service logic</span><br><span class="line"></span><br><span class="line"><span class="section">## Why</span></span><br><span class="line"></span><br><span class="line">Users need authentication before accessing the dashboard.</span><br><span class="line"></span><br><span class="line"><span class="section">## How to test</span></span><br><span class="line"></span><br><span class="line"><span class="bullet">1.</span> Start the backend service</span><br><span class="line"><span class="bullet">2.</span> Open <span class="code">`/login`</span></span><br><span class="line"><span class="bullet">3.</span> Enter a valid account</span><br><span class="line"><span class="bullet">4.</span> Confirm redirection to dashboard</span><br><span class="line"></span><br><span class="line"><span class="section">## Related issue</span></span><br><span class="line"></span><br><span class="line">Closes #12</span><br></pre></td></tr></table></figure>

<p>PR 的价值不只是合并代码，而是让代码进入主分支之前经过讨论、审查和验证。</p>
<hr>
<h1 id="撤销、回退与临时保存"><a href="#撤销、回退与临时保存" class="headerlink" title="撤销、回退与临时保存"></a>撤销、回退与临时保存</h1><p>Git 的强大之处，不只是保存历史，还在于它允许你安全地撤销错误。</p>
<p>不过不同命令适合不同场景，不能混用。</p>
<h2 id="丢弃工作区修改：restore"><a href="#丢弃工作区修改：restore" class="headerlink" title="丢弃工作区修改：restore"></a>丢弃工作区修改：restore</h2><p>如果你修改了文件，但还没有提交，并且确定这些修改不要了，可以恢复到最近一次提交状态：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git restore README.md</span><br></pre></td></tr></table></figure>

<p>恢复整个项目：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git restore .</span><br></pre></td></tr></table></figure>

<p>取消暂存但保留修改：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git restore --staged README.md</span><br></pre></td></tr></table></figure>

<p>区别如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">git restore 文件名           ：丢弃工作区修改</span><br><span class="line">git restore --staged 文件名  ：取消暂存，但保留修改</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="临时保存工作：stash"><a href="#临时保存工作：stash" class="headerlink" title="临时保存工作：stash"></a>临时保存工作：stash</h2><p>有时你正在开发一个功能，但还没写完，不适合 commit。<br>这时突然需要切换到另一个分支，Git 可能提示：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Please commit your changes or stash them before you switch branches.</span><br></pre></td></tr></table></figure>

<p>这时可以使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash</span><br></pre></td></tr></table></figure>

<p>它会把当前未提交的修改临时保存起来，让工作区恢复干净。</p>
<p>查看 stash 列表：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash list</span><br></pre></td></tr></table></figure>

<p>恢复最近一次 stash，并从列表中删除：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash pop</span><br></pre></td></tr></table></figure>

<p>恢复最近一次 stash，但保留在列表中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash apply</span><br></pre></td></tr></table></figure>

<p>删除 stash：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash drop</span><br></pre></td></tr></table></figure>

<p><code>pop</code> 和 <code>apply</code> 的区别：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">git stash pop    ：恢复修改，并删除 stash 记录</span><br><span class="line">git stash apply  ：恢复修改，但保留 stash 记录</span><br></pre></td></tr></table></figure>

<p>stash 适合临时切换任务，不适合长期保存代码。<br>长期开发应该使用分支和 commit。</p>
<hr>
<h2 id="安全撤销提交：revert"><a href="#安全撤销提交：revert" class="headerlink" title="安全撤销提交：revert"></a>安全撤销提交：revert</h2><p>如果某个提交已经 push 到远程，并且后来发现有问题，推荐使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git revert 提交ID</span><br></pre></td></tr></table></figure>

<p><code>revert</code> 不会删除历史，而是创建一个新的提交，用来抵消之前那次提交的修改。</p>
<p>也就是说：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">revert 是用一个新的提交来撤销旧提交。</span><br></pre></td></tr></table></figure>

<p>这对团队协作更安全，因为历史记录仍然完整，其他人也能清楚看到发生过一次撤销。</p>
<hr>
<h2 id="本地回退：reset"><a href="#本地回退：reset" class="headerlink" title="本地回退：reset"></a>本地回退：reset</h2><p><code>git reset</code> 用于把当前分支回退到某个状态。</p>
<p>常见三种模式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --soft HEAD~1</span><br><span class="line">git reset --mixed HEAD~1</span><br><span class="line">git reset --hard HEAD~1</span><br></pre></td></tr></table></figure>

<p>区别如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">--soft   ：撤销 commit，保留暂存区和工作区</span><br><span class="line">--mixed  ：撤销 commit，取消暂存，保留工作区</span><br><span class="line">--hard   ：撤销 commit，取消暂存，丢弃工作区修改</span><br></pre></td></tr></table></figure>

<p>最危险的是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --hard</span><br></pre></td></tr></table></figure>

<p>它会丢弃未提交的修改。<br>使用前一定先执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br></pre></td></tr></table></figure>

<p>简单记：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">已经 push 的提交：优先用 revert</span><br><span class="line">还没 push 的提交：可以考虑 reset</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Git-后悔药：reflog"><a href="#Git-后悔药：reflog" class="headerlink" title="Git 后悔药：reflog"></a>Git 后悔药：reflog</h2><p>如果你不小心执行了 <code>reset</code>、<code>rebase</code>、<code>checkout</code> 等操作，导致提交找不到了，可以先看：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reflog</span><br></pre></td></tr></table></figure>

<p>它会记录 HEAD 的移动历史。</p>
<p>找到之前的提交 ID 后，可以尝试恢复：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --hard 提交ID</span><br></pre></td></tr></table></figure>

<p><code>reflog</code> 是 Git 里非常重要的救命命令。</p>
<hr>
<h1 id="rebase：整理提交历史"><a href="#rebase：整理提交历史" class="headerlink" title="rebase：整理提交历史"></a>rebase：整理提交历史</h1><p>当你在 <code>feature</code> 分支开发时，<code>main</code> 分支可能已经有了新的提交。<br>这时你想把 <code>main</code> 的最新内容同步到 <code>feature</code>，有两种方式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">merge</span><br><span class="line">rebase</span><br></pre></td></tr></table></figure>

<p>使用 merge：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge main</span><br></pre></td></tr></table></figure>

<p>它会把 main 合并进当前分支，可能产生一个 merge commit。</p>
<p>使用 rebase：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git rebase main</span><br></pre></td></tr></table></figure>

<p>它会把当前分支的提交重新放到 main 最新提交之后。</p>
<p>可以理解成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">先接上 main 的最新内容</span><br><span class="line">再把自己的提交一个个重新应用上去</span><br></pre></td></tr></table></figure>

<p>rebase 的优点是历史更线性、更干净。<br>但它也有风险：rebase 会改写提交历史，提交 ID 也会变化。</p>
<p>所以要记住一个原则：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">不要随便 rebase 公共分支。</span><br></pre></td></tr></table></figure>

<p>适合 rebase 的场景：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">自己的本地 feature 分支</span><br><span class="line">还没有 push 的个人提交</span><br><span class="line">没有其他人依赖的分支</span><br></pre></td></tr></table></figure>

<p>不适合随便 rebase 的场景：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">main</span><br><span class="line">develop</span><br><span class="line">已经 push 且多人共同使用的分支</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="实际项目中的-Git-习惯"><a href="#实际项目中的-Git-习惯" class="headerlink" title="实际项目中的 Git 习惯"></a>实际项目中的 Git 习惯</h1><h2 id="使用-gitignore"><a href="#使用-gitignore" class="headerlink" title="使用 .gitignore"></a>使用 .gitignore</h2><p>不是所有文件都应该提交到 Git。</p>
<p>常见需要忽略的文件：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">.env</span><br><span class="line">node_modules/</span><br><span class="line">__pycache__/</span><br><span class="line">target/</span><br><span class="line">dist/</span><br><span class="line">.idea/</span><br><span class="line">.vscode/</span><br><span class="line">*.log</span><br><span class="line">.DS_Store</span><br></pre></td></tr></table></figure>

<p>示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Python</span><br><span class="line">__pycache__/</span><br><span class="line">*.pyc</span><br><span class="line">.venv/</span><br><span class="line">.env</span><br><span class="line"></span><br><span class="line"># Java</span><br><span class="line">target/</span><br><span class="line">*.class</span><br><span class="line"></span><br><span class="line"># IDE</span><br><span class="line">.idea/</span><br><span class="line">.vscode/</span><br><span class="line"></span><br><span class="line"># Logs</span><br><span class="line">*.log</span><br><span class="line"></span><br><span class="line"># macOS</span><br><span class="line">.DS_Store</span><br></pre></td></tr></table></figure>

<p>如果文件已经被 Git 跟踪，再加入 <code>.gitignore</code> 不会自动生效。<br>需要先取消跟踪：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">rm</span> --cached 文件名</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">rm</span> --cached .<span class="built_in">env</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="不要提交敏感信息"><a href="#不要提交敏感信息" class="headerlink" title="不要提交敏感信息"></a>不要提交敏感信息</h2><p>不要提交：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">数据库密码</span><br><span class="line">API Key</span><br><span class="line">Token</span><br><span class="line">SSH 私钥</span><br><span class="line">云服务密钥</span><br><span class="line">.env 文件</span><br><span class="line">证书文件</span><br></pre></td></tr></table></figure>

<p>如果已经提交到了远程仓库，不能只删除文件。<br>因为 Git 历史里可能仍然存在。</p>
<p>正确处理方式是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">立即让泄露的密钥失效</span><br><span class="line">重新生成新密钥</span><br><span class="line">清理 Git 历史</span><br><span class="line">通知团队重新同步</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="统一分支命名"><a href="#统一分支命名" class="headerlink" title="统一分支命名"></a>统一分支命名</h2><p>团队项目中建议统一分支名称。</p>
<p>常见格式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">feature/login-page</span><br><span class="line">fix/token-expired</span><br><span class="line">docs/update-readme</span><br><span class="line">refactor/user-service</span><br><span class="line">hotfix/production-error</span><br></pre></td></tr></table></figure>

<p>推荐结构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">类型/简短描述</span><br></pre></td></tr></table></figure>

<p>这样从分支名就能大致知道这个分支的用途。</p>
<hr>
<h2 id="使用-tag-标记版本"><a href="#使用-tag-标记版本" class="headerlink" title="使用 tag 标记版本"></a>使用 tag 标记版本</h2><p>发布版本时，推荐使用 tag。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git tag v1.0.0</span><br><span class="line">git push origin v1.0.0</span><br></pre></td></tr></table></figure>

<p>创建带说明的标签：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git tag -a v1.0.0 -m <span class="string">&quot;release v1.0.0&quot;</span></span><br><span class="line">git push origin v1.0.0</span><br></pre></td></tr></table></figure>

<p>常见版本命名：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">v1.0.0</span><br><span class="line">v1.1.0</span><br><span class="line">v2.0.0</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="常用命令速查"><a href="#常用命令速查" class="headerlink" title="常用命令速查"></a>常用命令速查</h1><h2 id="本地提交链路"><a href="#本地提交链路" class="headerlink" title="本地提交链路"></a>本地提交链路</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;feat: add feature&quot;</span></span><br><span class="line">git <span class="built_in">log</span> --oneline</span><br></pre></td></tr></table></figure>

<h2 id="分支开发"><a href="#分支开发" class="headerlink" title="分支开发"></a>分支开发</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch</span><br><span class="line">git switch -c feature/login</span><br><span class="line">git switch main</span><br><span class="line">git merge feature/login</span><br></pre></td></tr></table></figure>

<h2 id="远程协作-1"><a href="#远程协作-1" class="headerlink" title="远程协作"></a>远程协作</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote -v</span><br><span class="line">git push -u origin main</span><br><span class="line">git fetch</span><br><span class="line">git pull</span><br></pre></td></tr></table></figure>

<h2 id="查看差异"><a href="#查看差异" class="headerlink" title="查看差异"></a>查看差异</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git diff</span><br><span class="line">git diff --staged</span><br><span class="line">git diff 提交ID1 提交ID2</span><br><span class="line">git <span class="built_in">log</span> --oneline --graph --decorate --all</span><br></pre></td></tr></table></figure>

<h2 id="撤销与恢复"><a href="#撤销与恢复" class="headerlink" title="撤销与恢复"></a>撤销与恢复</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git restore 文件名</span><br><span class="line">git restore --staged 文件名</span><br><span class="line">git reset --soft HEAD~1</span><br><span class="line">git reset --mixed HEAD~1</span><br><span class="line">git reset --hard HEAD~1</span><br><span class="line">git revert 提交ID</span><br><span class="line">git reflog</span><br></pre></td></tr></table></figure>

<h2 id="临时保存"><a href="#临时保存" class="headerlink" title="临时保存"></a>临时保存</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash</span><br><span class="line">git stash list</span><br><span class="line">git stash pop</span><br><span class="line">git stash apply</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="推荐的日常工作流"><a href="#推荐的日常工作流" class="headerlink" title="推荐的日常工作流"></a>推荐的日常工作流</h1><p>个人项目：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;feat: add new feature&quot;</span></span><br><span class="line">git push</span><br></pre></td></tr></table></figure>

<p>团队项目：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch main</span><br><span class="line">git pull</span><br><span class="line"></span><br><span class="line">git switch -c feature/your-feature</span><br><span class="line"></span><br><span class="line"><span class="comment"># 开发代码</span></span><br><span class="line"></span><br><span class="line">git status</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;feat: add your feature&quot;</span></span><br><span class="line">git push -u origin feature/your-feature</span><br></pre></td></tr></table></figure>

<p>然后在 GitHub 上创建 Pull Request，经过 Review 后再合并到 main。</p>
<p>线上 bug 修复：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch main</span><br><span class="line">git pull</span><br><span class="line"></span><br><span class="line">git switch -c hotfix/login-error</span><br><span class="line"></span><br><span class="line"><span class="comment"># 修复 bug</span></span><br><span class="line"></span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;fix: resolve login error&quot;</span></span><br><span class="line">git push -u origin hotfix/login-error</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
        <tag>GitHub</tag>
        <tag>版本控制</tag>
        <tag>分支管理</tag>
        <tag>远程协作</tag>
        <tag>Pull_Request</tag>
        <tag>开发效率</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo 博客搭建：GitHub Pages 部署与 Butterfly 主题配置</title>
    <url>/2026/03/11/daily/Hexo%20%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p>在学习和开发过程中，零散的代码片段、配置记录和排错经验很容易被遗忘，但是只把笔记保存在本地也不是长久之计，于是我着手搭建了一个可以长期维护、方便检索、也能够对外展示的BLOG。</p>
<p>我选择的是 <strong>Hexo + GitHub Pages + Butterfly</strong> 这一套方案：</p>
<ul>
<li><strong>Hexo</strong>：生成静态博客，部署简单，生态成熟；</li>
<li><strong>GitHub Pages</strong>：免费托管静态网页，适合个人技术博客；</li>
<li><strong>Butterfly</strong>：基于 Hexo 的高可配置主题，视觉效果和功能扩展都比较完善。</li>
</ul>
<p>这篇文章记录从初始化、本地预览、GitHub Pages 部署，到 Butterfly 主题配置过程中遇到的问题和解决方法。</p>
<hr>
<h1 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h1><p>在开始之前，需要先安装以下工具：</p>
<table>
<thead>
<tr>
<th>工具</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>Node.js</td>
<td>提供 npm 环境，用于安装 Hexo 和相关依赖</td>
</tr>
<tr>
<td>Git</td>
<td>用于拉取主题、管理代码和部署到 GitHub</td>
</tr>
<tr>
<td>Hexo</td>
<td>静态博客生成器</td>
</tr>
<tr>
<td>GitHub Pages</td>
<td>博客网页托管平台</td>
</tr>
</tbody></table>
<p>安装完成后，可以在终端中检查版本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node -v</span><br><span class="line">npm -v</span><br><span class="line">git --version</span><br></pre></td></tr></table></figure>

<p>确认环境正常后，安装 Hexo 命令行工具：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="初始化-Hexo-项目"><a href="#初始化-Hexo-项目" class="headerlink" title="初始化 Hexo 项目"></a>初始化 Hexo 项目</h1><p>创建博客项目：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo init myblog</span><br><span class="line"><span class="built_in">cd</span> myblog</span><br><span class="line">npm install</span><br></pre></td></tr></table></figure>

<p>启动本地服务：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<p>浏览器访问：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">http://localhost:4000/</span><br></pre></td></tr></table></figure>

<p>如果能够看到 Hexo 默认页面，说明本地博客已经成功运行。</p>
<hr>
<h1 id="初始化时的网络超时问题"><a href="#初始化时的网络超时问题" class="headerlink" title="初始化时的网络超时问题"></a>初始化时的网络超时问题</h1><p>在执行 <code>hexo init myblog</code> 时，可能会遇到 GitHub 连接超时：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fatal: unable to access <span class="string">&#x27;https://github.com/hexojs/hexo-starter.git/&#x27;</span>:</span><br><span class="line">Connection timed out after 300030 milliseconds</span><br></pre></td></tr></table></figure>

<p>这个问题通常不是 Hexo 本身的问题，而是终端访问 GitHub 不稳定导致的。浏览器能打开 GitHub，不代表命令行中的 Git 或 npm 一定能正常访问。</p>
<p>可以采用两种方式处理。</p>
<h2 id="npm-镜像源安装依赖"><a href="#npm-镜像源安装依赖" class="headerlink" title="npm 镜像源安装依赖"></a>npm 镜像源安装依赖</h2><p>进入项目目录后，使用国内镜像源安装依赖：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install --registry=https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<h2 id="克隆-Hexo-Starter"><a href="#克隆-Hexo-Starter" class="headerlink" title="克隆 Hexo Starter"></a>克隆 Hexo Starter</h2><p>如果 <code>hexo init</code> 一直失败，可以手动克隆 starter 项目：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/hexojs/hexo-starter.git myblog</span><br><span class="line"><span class="built_in">cd</span> myblog</span><br><span class="line">npm install --registry=https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<p>随后继续执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="部署到-GitHub-Pages"><a href="#部署到-GitHub-Pages" class="headerlink" title="部署到 GitHub Pages"></a>部署到 GitHub Pages</h1><h2 id="创建-GitHub-Pages-仓库"><a href="#创建-GitHub-Pages-仓库" class="headerlink" title="创建 GitHub Pages 仓库"></a>创建 GitHub Pages 仓库</h2><p>如果 GitHub 用户名是 <code>NimbusFan</code>，则仓库名应为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">NimbusFan.github.io</span><br></pre></td></tr></table></figure>

<p>完整仓库地址类似：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">https://github.com/NimbusFan/NimbusFan.github.io</span><br></pre></td></tr></table></figure>

<p>GitHub Pages 对用户主页仓库的命名有固定要求，必须使用：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户名.github.io</span><br></pre></td></tr></table></figure>

<p>否则部署后可能无法通过 <code>https://用户名.github.io/</code> 访问。</p>
<hr>
<h2 id="安装部署插件"><a href="#安装部署插件" class="headerlink" title="安装部署插件"></a>安装部署插件</h2><p>Hexo 默认不会自动部署到 GitHub，需要安装部署插件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="修改-Hexo-根目录配置"><a href="#修改-Hexo-根目录配置" class="headerlink" title="修改 Hexo 根目录配置"></a>修改 Hexo 根目录配置</h2><p>打开 Hexo 根目录下的 <code>_config.yml</code>，找到 <code>deploy</code> 配置项：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">git@github.com:NimbusFan/NimbusFan.github.io.git</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure>

<p>这里推荐使用 SSH 地址，而不是 HTTPS 地址。HTTPS 在推送过程中容易出现连接中断、认证失败或令牌问题。</p>
<hr>
<h1 id="配置-GitHub-SSH-密钥"><a href="#配置-GitHub-SSH-密钥" class="headerlink" title="配置 GitHub SSH 密钥"></a>配置 GitHub SSH 密钥</h1><p>如果部署时出现类似错误：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Please make sure you have the correct access rights</span><br><span class="line">and the repository exists.</span><br></pre></td></tr></table></figure>

<p>通常说明当前电脑没有 GitHub 仓库的推送权限，需要配置 SSH 密钥。</p>
<h2 id="生成-SSH-密钥"><a href="#生成-SSH-密钥" class="headerlink" title="生成 SSH 密钥"></a>生成 SSH 密钥</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C <span class="string">&quot;你的邮箱&quot;</span></span><br></pre></td></tr></table></figure>

<p>一路回车即可。</p>
<h2 id="复制公钥"><a href="#复制公钥" class="headerlink" title="复制公钥"></a>复制公钥</h2><p>Windows 下公钥通常位于：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">C:\Users\你的用户名\.ssh\id_rsa.pub</span><br></pre></td></tr></table></figure>

<p>打开后复制全部内容。</p>
<h2 id="添加到-GitHub"><a href="#添加到-GitHub" class="headerlink" title="添加到 GitHub"></a>添加到 GitHub</h2><p>进入 GitHub：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Settings -&gt; SSH and GPG keys -&gt; New SSH key</span><br></pre></td></tr></table></figure>

<p>将刚才复制的公钥粘贴进去并保存。</p>
<h2 id="测试连接"><a href="#测试连接" class="headerlink" title="测试连接"></a>测试连接</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span><br></pre></td></tr></table></figure>

<p>如果看到类似下面的提示，说明 SSH 配置成功：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Hi NimbusFan! You<span class="string">&#x27;ve successfully authenticated.</span></span><br></pre></td></tr></table></figure>

<hr>
<h1 id="生成并部署博客"><a href="#生成并部署博客" class="headerlink" title="生成并部署博客"></a>生成并部署博客</h1><p>完成配置后，执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>也可以合并为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><br></pre></td></tr></table></figure>

<p>部署成功后，访问：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">https://nimbusfan.github.io/</span><br></pre></td></tr></table></figure>

<p>如果页面没有立即更新，可以等待一到三分钟，或者使用浏览器强制刷新：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Ctrl + F5</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="安装-Butterfly-主题"><a href="#安装-Butterfly-主题" class="headerlink" title="安装 Butterfly 主题"></a>安装 Butterfly 主题</h1><p>Hexo 默认主题比较基础，适合快速验证环境，但不太适合作为长期维护的技术博客。我最终选择了 Butterfly 主题。</p>
<p>进入博客根目录后执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/jerryc127/hexo-theme-butterfly.git themes/butterfly</span><br></pre></td></tr></table></figure>

<p>安装 Butterfly 需要的渲染器：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-renderer-pug hexo-renderer-stylus --save</span><br></pre></td></tr></table></figure>

<p>然后打开 Hexo 根目录下的 <code>_config.yml</code>，修改主题：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">theme:</span> <span class="string">butterfly</span></span><br></pre></td></tr></table></figure>

<p>重新生成并启动：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="使用独立主题配置文件"><a href="#使用独立主题配置文件" class="headerlink" title="使用独立主题配置文件"></a>使用独立主题配置文件</h1><p>Butterfly 的配置项很多，不建议直接长期修改：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">themes/butterfly/_config.yml</span><br></pre></td></tr></table></figure>

<p>更推荐在 Hexo 根目录下创建：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">_config.butterfly.yml</span><br></pre></td></tr></table></figure>

<p>然后将主题配置复制到这个文件中进行修改。</p>
<p>这样做有两个好处：</p>
<ol>
<li>主题升级时，不容易覆盖自己的配置；</li>
<li>站点配置和主题配置分离，后期维护更清晰。</li>
</ol>
<p>常见结构如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">myblog/</span><br><span class="line">├── _config.yml</span><br><span class="line">├── _config.butterfly.yml</span><br><span class="line">├── source/</span><br><span class="line">├── themes/</span><br><span class="line">│   └── butterfly/</span><br><span class="line">└── package.json</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li><code>_config.yml</code>：Hexo 站点基础配置；</li>
<li><code>_config.butterfly.yml</code>：Butterfly 主题配置；</li>
<li><code>source/</code>：文章、页面、图片和自定义资源；</li>
<li><code>themes/</code>：主题文件。</li>
</ul>
<hr>
<h1 id="配置过程中遇到的典型问题"><a href="#配置过程中遇到的典型问题" class="headerlink" title="配置过程中遇到的典型问题"></a>配置过程中遇到的典型问题</h1><h2 id="修改配置后页面没有变化"><a href="#修改配置后页面没有变化" class="headerlink" title="修改配置后页面没有变化"></a>修改配置后页面没有变化</h2><p>常见原因有三个：</p>
<p>第一，Hexo 没有重新生成：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<p>第二，浏览器缓存没有清理：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Ctrl + F5</span><br></pre></td></tr></table></figure>

<p>第三，配置文件改错了位置。</p>
<p>Butterfly 相关配置应优先检查：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">_config.butterfly.yml</span><br></pre></td></tr></table></figure>

<p>而不是只检查主题目录中的 <code>_config.yml</code>。</p>
<hr>
<h2 id="GitHub-链接跳转错误"><a href="#GitHub-链接跳转错误" class="headerlink" title="GitHub 链接跳转错误"></a>GitHub 链接跳转错误</h2><p>在配置社交链接时，可能会遇到这样的问题：已经把 GitHub 地址改成自己的账号，但页面上仍然跳转到主题默认的 <code>github.com/xxxxxx</code>。</p>
<p>这种情况通常说明主题中不止一个位置配置了 GitHub 链接。</p>
<p>可以在项目根目录中全局搜索：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">findstr /S /N <span class="string">&quot;xxxxxx&quot;</span> *.yml</span><br></pre></td></tr></table></figure>

<p>如果是在 VS Code 中，也可以直接使用全局搜索：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Ctrl + Shift + F</span><br></pre></td></tr></table></figure>

<p>搜索关键词：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">xxxxxx</span><br></pre></td></tr></table></figure>

<p>找到所有残留配置后，再逐一修改。</p>
<p>这个问题本质上不是缓存问题，而是配置没有改完整。</p>
<hr>
<h2 id="本地预览正常但部署失败"><a href="#本地预览正常但部署失败" class="headerlink" title="本地预览正常但部署失败"></a>本地预览正常但部署失败</h2><p>如果 <code>hexo s</code> 本地能正常打开，但 <code>hexo d</code> 失败，说明问题通常出在部署环节，而不是文章或主题本身。</p>
<p>优先检查：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">git@github.com:NimbusFan/NimbusFan.github.io.git</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure>

<p>还需要确认：</p>
<ul>
<li>GitHub 仓库是否存在；</li>
<li>仓库名是否为 <code>NimbusFan.github.io</code>；</li>
<li>当前电脑是否配置了正确的 SSH Key；</li>
<li>本地 Git 是否能正常连接 GitHub。</li>
</ul>
<p>可以使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span><br></pre></td></tr></table></figure>

<p>进行验证。</p>
<hr>
<h1 id="BLOG的基本结构建议"><a href="#BLOG的基本结构建议" class="headerlink" title="BLOG的基本结构建议"></a>BLOG的基本结构建议</h1><p>完成基础搭建后，博客不应该只停留在“文章列表”状态。一个更完整的个人技术博客，至少可以包含以下页面：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Home</span><br><span class="line">Projects</span><br><span class="line">Resume</span><br><span class="line">Archives</span><br><span class="line">Tags</span><br><span class="line">Categories</span><br><span class="line">About</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li><code>Projects</code> 用于展示项目经历；</li>
<li><code>Resume</code> 用于放置网页简历；</li>
<li><code>About</code> 用于介绍个人方向、技术栈和联系方式；</li>
<li><code>Archives / Tags / Categories</code> 用于文章管理和检索。</li>
</ul>
<p>对应页面可以通过以下命令创建：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new page projects</span><br><span class="line">hexo new page resume</span><br><span class="line">hexo new page about</span><br></pre></td></tr></table></figure>

<p>创建后，页面文件会出现在：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">source/projects/index.md</span><br><span class="line">source/resume/index.md</span><br><span class="line">source/about/index.md</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="最终常用命令"><a href="#最终常用命令" class="headerlink" title="最终常用命令"></a>最终常用命令</h1><p>本地预览：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<p>部署到 GitHub Pages：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>新建文章：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new <span class="string">&quot;文章标题&quot;</span></span><br></pre></td></tr></table></figure>

<p>新建页面：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new page 页面名称</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>博客搭建</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>Butterfly</tag>
      </tags>
  </entry>
  <entry>
    <title>时间复杂度：大O表示法与常见复杂度分析</title>
    <url>/2026/08/25/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%97%B6%E9%97%B4%E5%A4%8D%E6%9D%82%E5%BA%A6/</url>
    <content><![CDATA[<h1 id="大O表示法"><a href="#大O表示法" class="headerlink" title="大O表示法"></a>大O表示法</h1><p>大O表示法用于描述：<strong>当输入规模不断增大时，算法运行时间或占用空间的增长趋势。</strong><br>它不表示程序实际运行了多少秒，而是表示算法的效率会随着数据量如何变化。</p>
<h2 id="简化规则："><a href="#简化规则：" class="headerlink" title="简化规则："></a>简化规则：</h2><ul>
<li>忽略常数</li>
<li>忽略低阶项</li>
<li>连续代码的复杂度相加，最后取最大项</li>
<li>嵌套循环的复杂度相乘</li>
</ul>
<h2 id="常见时间复杂度"><a href="#常见时间复杂度" class="headerlink" title="常见时间复杂度"></a>常见时间复杂度</h2><table>
<thead>
<tr>
<th align="center">时间复杂度</th>
<th align="center">名称</th>
<th align="center">特点</th>
<th align="center">常见场景</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>O(1)</code></td>
<td align="center">常数复杂度</td>
<td align="center">执行次数不随数据规模变化</td>
<td align="center">数组按下标访问、变量赋值</td>
</tr>
<tr>
<td align="center"><code>O(log n)</code></td>
<td align="center">对数复杂度</td>
<td align="center">每次将问题规模缩小一部分</td>
<td align="center">二分查找、每次除以 2 的循环</td>
</tr>
<tr>
<td align="center"><code>O(n)</code></td>
<td align="center">线性复杂度</td>
<td align="center">需要遍历一次数据</td>
<td align="center">遍历数组、查找最大值、双指针</td>
</tr>
<tr>
<td align="center"><code>O(n log n)</code></td>
<td align="center">线性对数复杂度</td>
<td align="center">进行 <code>log n</code> 层操作，每层处理 <code>n</code> 个数据</td>
<td align="center">归并排序、堆排序、快速排序平均情况</td>
</tr>
<tr>
<td align="center"><code>O(n²)</code></td>
<td align="center">平方复杂度</td>
<td align="center">通常需要两层完整嵌套遍历</td>
<td align="center">冒泡排序、选择排序、枚举所有数对</td>
</tr>
<tr>
<td align="center"><code>O(n³)</code></td>
<td align="center">立方复杂度</td>
<td align="center">通常需要三层完整嵌套遍历</td>
<td align="center">三层循环、部分矩阵计算</td>
</tr>
<tr>
<td align="center"><code>O(2ⁿ)</code></td>
<td align="center">指数复杂度</td>
<td align="center">数据每增加一个，计算量可能翻倍</td>
<td align="center">枚举所有子集、暴力递归、部分回溯</td>
</tr>
<tr>
<td align="center"><code>O(n!)</code></td>
<td align="center">阶乘复杂度</td>
<td align="center">需要枚举所有排列，增长速度极快</td>
<td align="center">全排列、暴力旅行商问题</td>
</tr>
</tbody></table>
<p>复杂度从快到慢：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">O(1) &lt; O(log n) &lt; O(n) &lt; O(n log n) &lt; O(n²) &lt; O(n³) &lt; O(2ⁿ) &lt; O(n!)</span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">常数 &lt; 对数 &lt; 线性 &lt; 线性对数 &lt; 平方 &lt; 立方 &lt; 指数 &lt; 阶乘</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li><p>每次将数据规模减半，通常是 <code>O(log n)</code>。</p>
</li>
<li><p>完整遍历一次数据，通常是 <code>O(n)</code>。</p>
</li>
<li><p>两层完整嵌套循环，通常是 <code>O(n²)</code>。</p>
</li>
<li><p>枚举所有子集，通常是 <code>O(2ⁿ)</code>。</p>
</li>
<li><p>枚举所有排列，通常是 <code>O(n!)</code>。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>时间复杂度</tag>
      </tags>
  </entry>
  <entry>
    <title>散列表：哈希函数、冲突解决与映射实现</title>
    <url>/2026/08/25/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%A3%E5%88%97%E8%A1%A8/</url>
    <content><![CDATA[<h1 id="散列"><a href="#散列" class="headerlink" title="散列"></a>散列</h1><h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><table>
<thead>
<tr>
<th align="center"><code>概念</code></th>
<th align="center"><code>含义</code></th>
</tr>
</thead>
<tbody><tr>
<td align="center">散列</td>
<td align="center">通过计算直接确定数据存储位置的查找方法</td>
</tr>
<tr>
<td align="center">散列表</td>
<td align="center">按照散列值存储数据的数据结构</td>
</tr>
<tr>
<td align="center">槽 slot</td>
<td align="center">散列表中保存数据的位置</td>
</tr>
<tr>
<td align="center">散列函数</td>
<td align="center">将键转换为槽下标的函数</td>
</tr>
<tr>
<td align="center">散列值</td>
<td align="center">散列函数计算得到的槽下标</td>
</tr>
<tr>
<td align="center">表长 m</td>
<td align="center">散列表中槽的总数</td>
</tr>
<tr>
<td align="center">冲突</td>
<td align="center">不同键得到相同散列位置</td>
</tr>
<tr>
<td align="center">负载因子</td>
<td align="center">表中元素数量与槽数量的比值</td>
</tr>
</tbody></table>
<p>散列函数可以表示为：&#x3D;&#x3D;<strong>键 → 散列函数 → 槽下标</strong>&#x3D;&#x3D;<br>散列函数的输出范围为：&#x3D;&#x3D;<strong>0  ～  m - 1</strong>&#x3D;&#x3D;</p>
<h2 id="负载因子"><a href="#负载因子" class="headerlink" title="负载因子"></a>负载因子</h2><p>负载因子通常记为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">λ = 元素数量 / 散列表大小</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">负载因子情况</th>
<th align="center">影响</th>
</tr>
</thead>
<tbody><tr>
<td align="center">较小</td>
<td align="center">空槽多，冲突概率较低</td>
</tr>
<tr>
<td align="center">较大</td>
<td align="center">表逐渐变满，冲突概率增加</td>
</tr>
<tr>
<td align="center">接近表容量上限</td>
<td align="center">插入和查找效率明显下降</td>
</tr>
</tbody></table>
<hr>
<h1 id="完美散列函数"><a href="#完美散列函数" class="headerlink" title="完美散列函数"></a>完美散列函数</h1><p><strong>完美散列函数</strong>是指：将给定集合中的每个键映射到不同槽位，不产生散列冲突的散列函数。</p>
<p>获得完美散列函数的一种方法是扩大散列表的容量，大到所有可能出现的数据项都能够占据不同的槽(slot)。但这种方法对于可能数据项范围过大的情况并不实用。所以需要退而求其次，寻找近似完美的散列函数，好的散列函数需要具备：冲突最少(近似完美)、计算难度低(额外开销小)、充分分散数据项(节约空间)。</p>
<h2 id="常见散列函数设计方法"><a href="#常见散列函数设计方法" class="headerlink" title="常见散列函数设计方法"></a>常见散列函数设计方法</h2><table>
<thead>
<tr>
<th align="center">方法</th>
<th align="center">基本思想</th>
<th align="center">特点</th>
</tr>
</thead>
<tbody><tr>
<td align="center">求余法</td>
<td align="center">用键除以表长并取余数</td>
<td align="center">简单、常用</td>
</tr>
<tr>
<td align="center">折叠法</td>
<td align="center">将键分段后相加，再对表长取余</td>
<td align="center">适合较长的数字键</td>
</tr>
<tr>
<td align="center">平方取中法</td>
<td align="center">将键平方，提取中间部分，再取余</td>
<td align="center">能利用键的更多数字特征</td>
</tr>
<tr>
<td align="center">字符编码法</td>
<td align="center">将字符串中的字符转换为编码后计算</td>
<td align="center">用于字符串键</td>
</tr>
<tr>
<td align="center">加权字符编码法</td>
<td align="center">根据字符位置设置权重后计算</td>
<td align="center">能区分字符相同但顺序不同的字符串</td>
</tr>
</tbody></table>
<p>这些方法的目标不是保证完全无冲突，而是让键在散列表中分布得更均匀。</p>
<h3 id="求余法"><a href="#求余法" class="headerlink" title="求余法"></a>求余法</h3><p>求余法是最基本的散列函数：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">h(key) = key % table_size</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">要点</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">输出范围</td>
<td align="center"><code>0～table_size-1</code></td>
</tr>
<tr>
<td align="center">优点</td>
<td align="center">计算速度快、实现简单</td>
</tr>
<tr>
<td align="center">缺点</td>
<td align="center">数据规律可能造成集中冲突</td>
</tr>
<tr>
<td align="center">表长选择</td>
<td align="center">通常优先选择质数</td>
</tr>
</tbody></table>
<p>取余操作通常也会作为其他散列方法的最后一步，用来保证结果位于合法槽位范围内。</p>
<hr>
<h3 id="折叠法"><a href="#折叠法" class="headerlink" title="折叠法"></a>折叠法</h3><p>折叠法的处理过程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">键分段 → 各段相加 → 对表长取余</span><br></pre></td></tr></table></figure>

<p>部分折叠法还会翻转间隔的数据段，以进一步打乱原始数据规律。</p>
<table>
<thead>
<tr>
<th align="center">优点</th>
<th align="center">缺点</th>
</tr>
</thead>
<tbody><tr>
<td align="center">可以处理较长的键</td>
<td align="center">分段方式会影响分布效果</td>
</tr>
<tr>
<td align="center">能利用键的多个部分</td>
<td align="center">仍然可能产生冲突</td>
</tr>
<tr>
<td align="center">计算相对简单</td>
<td align="center">需要根据键的特点设计</td>
</tr>
</tbody></table>
<hr>
<h3 id="平方取中法"><a href="#平方取中法" class="headerlink" title="平方取中法"></a>平方取中法</h3><p>平方取中法的处理过程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">键平方 → 提取中间部分 → 对表长取余</span><br></pre></td></tr></table></figure>

<p>平方运算可以使键的不同部分相互影响，再通过中间数字生成散列值。</p>
<table>
<thead>
<tr>
<th align="center">优点</th>
<th align="center">缺点</th>
</tr>
</thead>
<tbody><tr>
<td align="center">能打乱部分原始规律</td>
<td align="center">计算量高于简单求余</td>
</tr>
<tr>
<td align="center">可以改善数据分布</td>
<td align="center">不能保证完全无冲突</td>
</tr>
<tr>
<td align="center">利用了键的多个数位</td>
<td align="center">提取规则需要统一</td>
</tr>
</tbody></table>
<hr>
<h3 id="字符串散列"><a href="#字符串散列" class="headerlink" title="字符串散列"></a>字符串散列</h3><p>字符串不能直接作为槽下标，需要先转换为数值。</p>
<p>基本过程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">字符 → 字符编码 → 组合计算 → 对表长取余</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">方法</th>
<th align="center">问题或特点</th>
</tr>
</thead>
<tbody><tr>
<td align="center">字符编码直接求和</td>
<td align="center">无法有效区分字符顺序</td>
</tr>
<tr>
<td align="center">字符编码加权求和</td>
<td align="center">将字符位置加入计算</td>
</tr>
<tr>
<td align="center">最后对表长取余</td>
<td align="center">保证结果处于合法槽位范围</td>
</tr>
</tbody></table>
<p>字符串散列不仅要使用字符本身的信息，还应考虑字符所在的位置。</p>
<hr>
<h1 id="冲突"><a href="#冲突" class="headerlink" title="冲突"></a>冲突</h1><p>当两个不同的键经过散列函数计算后，得到<strong>相同槽下标</strong>时，就发生了<strong>散列冲突</strong>。<br>冲突无法完全避免，因此散列表必须规定统一的冲突解决方案。</p>
<h2 id="开放寻址法"><a href="#开放寻址法" class="headerlink" title="开放寻址法"></a>开放寻址法</h2><p>开放寻址法发生冲突后，会继续在散列表中寻找其他空槽。<br>所有数据都直接存储在散列表内部。</p>
<h3 id="线性探测"><a href="#线性探测" class="headerlink" title="线性探测"></a>线性探测</h3><p>发生冲突后按照固定间隔依次检查后续槽：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">当前位置 → 下一位置 → 再下一位置 → ......</span><br></pre></td></tr></table></figure>

<p>重新散列函数通常表示为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">rehash(pos) = (pos + skip) % table_size</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">优点</th>
<th align="center">缺点</th>
</tr>
</thead>
<tbody><tr>
<td align="center">实现简单</td>
<td align="center">容易产生数据聚集</td>
</tr>
<tr>
<td align="center">不需要额外链表</td>
<td align="center">表越满，探测距离越长</td>
</tr>
<tr>
<td align="center">数据集中存储</td>
<td align="center">删除操作处理较复杂</td>
</tr>
</tbody></table>
<p>线性探测容易使数据集中在一段连续区域中，即聚集现象。<br>聚集会导致：</p>
<ul>
<li>新元素需要检查更多槽；</li>
<li>查找需要进行更多比较；</li>
<li>后续冲突进一步增加。</li>
</ul>
<h3 id="跳跃探测"><a href="#跳跃探测" class="headerlink" title="跳跃探测"></a>跳跃探测</h3><p>发生冲突后，不逐个检查槽，而是按照固定步长跳跃。<br>需要注意的是skip的取值，不能被散列表大小整除，否则会产生周期，造成很多空槽永远无法探测到，通常可以：</p>
<ul>
<li>将散列表的大小设置为质数(素数)；</li>
<li>使步长与表长互质。</li>
</ul>
<h3 id="二次探测"><a href="#二次探测" class="headerlink" title="二次探测"></a>二次探测</h3><p>不再固定skip的值，而是逐步增加skip值,如</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1、3、5、7、9</span><br></pre></td></tr></table></figure>

<p>这样槽号就会是原散列值以平方数增加:</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">h, h+1, h+4, h+9, h+16...</span><br></pre></td></tr></table></figure>

<p>相对于线性探测，二次探测可以减轻连续聚集问题。</p>
<h2 id="链地址法"><a href="#链地址法" class="headerlink" title="链地址法"></a>链地址法</h2><p>链地址法允许同一个槽保存多个数据项。<br>每个槽指向一个集合或链式结构，冲突的元素存储在同一个槽对应的集合中。</p>
<table>
<thead>
<tr>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody><tr>
<td>不需要寻找其他空槽</td>
<td>需要额外存储结构</td>
</tr>
<tr>
<td>表较满时仍可以插入</td>
<td>同一槽元素过多时查找变慢</td>
</tr>
<tr>
<td>删除操作较直观</td>
<td>冲突严重时可能退化为顺序查找</td>
</tr>
</tbody></table>
<hr>
<h1 id="抽象数据类型-“映射”：ADT-Map"><a href="#抽象数据类型-“映射”：ADT-Map" class="headerlink" title="抽象数据类型 “映射”：ADT Map"></a>抽象数据类型 “映射”：ADT Map</h1><h2 id="映射的定义"><a href="#映射的定义" class="headerlink" title="映射的定义"></a>映射的定义</h2><p>映射 <code>Map</code> 是由一组键值对组成的数据结构。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">key → value</span><br></pre></td></tr></table></figure>

<p>其主要特点是：</p>
<table>
<thead>
<tr>
<th align="center">特点</th>
<th align="center">含义</th>
</tr>
</thead>
<tbody><tr>
<td align="center">按键访问</td>
<td align="center">通过键查找对应的值</td>
</tr>
<tr>
<td align="center">键唯一</td>
<td align="center">一个映射中不能存在重复键</td>
</tr>
<tr>
<td align="center">值可以重复</td>
<td align="center">不同键可以关联相同的值</td>
</tr>
<tr>
<td align="center">无序抽象结构</td>
<td align="center">映射抽象数据类型本身不规定位置顺序</td>
</tr>
<tr>
<td align="center">一对一关联</td>
<td align="center">每个键只关联一个当前值</td>
</tr>
</tbody></table>
<p>当插入已经存在的键时，不会增加新键，而是更新对应的值。</p>
<h2 id="映射的基本操作"><a href="#映射的基本操作" class="headerlink" title="映射的基本操作"></a>映射的基本操作</h2><table>
<thead>
<tr>
<th align="center">操作</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>Map()</code></td>
<td align="center">创建空映射</td>
</tr>
<tr>
<td align="center"><code>put(key, value)</code></td>
<td align="center">添加键值对；键存在时更新值</td>
</tr>
<tr>
<td align="center"><code>get(key)</code></td>
<td align="center">根据键获取值</td>
</tr>
<tr>
<td align="center"><code>del map[key]</code></td>
<td align="center">删除指定键值对</td>
</tr>
<tr>
<td align="center"><code>len(map)</code> 或 <code>size()</code></td>
<td align="center">返回键值对数量</td>
</tr>
<tr>
<td align="center"><code>key in map</code></td>
<td align="center">判断键是否存在</td>
</tr>
</tbody></table>
<p>映射抽象数据类型只规定：</p>
<ul>
<li>数据应具有什么特征；</li>
<li>应支持哪些操作；</li>
<li>每个操作应产生什么结果。</li>
</ul>
<p>它不规定底层必须使用哪种数据结构。</p>
<h2 id="使用散列表实现映射"><a href="#使用散列表实现映射" class="headerlink" title="使用散列表实现映射"></a>使用散列表实现映射</h2><table>
<thead>
<tr>
<th align="center">列表</th>
<th align="center">保存内容</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>slots</code></td>
<td align="center">保存键</td>
</tr>
<tr>
<td align="center"><code>data</code></td>
<td align="center">保存值</td>
</tr>
</tbody></table>
<p>两个列表相同下标的数据互相关联：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">slots[i] 中的键 → data[i] 中的值</span><br></pre></td></tr></table></figure>

<h3 id="插入操作put"><a href="#插入操作put" class="headerlink" title="插入操作put"></a>插入操作<code>put</code></h3><p>插入过程：</p>
<ol>
<li>通过散列函数计算键的初始槽；</li>
<li>如果槽为空，保存键和值；</li>
<li>如果槽中已经是相同的键，更新对应值；</li>
<li>如果槽中是其他键，说明发生冲突；</li>
<li>按重新散列规则寻找可用位置。</li>
</ol>
<h3 id="查询操作get"><a href="#查询操作get" class="headerlink" title="查询操作get"></a>查询操作<code>get</code></h3><p>查询过程：</p>
<ol>
<li>计算键的初始槽；</li>
<li>比较槽中的键；</li>
<li>若键相同，返回对应值；</li>
<li>若键不同，按照相同的探测规则继续查找；</li>
<li>遇到终止条件后，判定键不存在。</li>
</ol>
<h3 id="Python特殊方法"><a href="#Python特殊方法" class="headerlink" title="Python特殊方法"></a>Python特殊方法</h3><p>为了使自定义映射支持字典形式的访问，可以实现：</p>
<table>
<thead>
<tr>
<th align="center">特殊方法</th>
<th align="center">对应功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>__getitem__</code></td>
<td align="center">获取指定键的值</td>
</tr>
<tr>
<td align="center"><code>__setitem__</code></td>
<td align="center">插入或修改键值对</td>
</tr>
<tr>
<td align="center"><code>__contains__</code></td>
<td align="center">判断键是否存在</td>
</tr>
<tr>
<td align="center"><code>__len__</code></td>
<td align="center">获取键值对数量</td>
</tr>
<tr>
<td align="center"><code>__delitem__</code></td>
<td align="center">删除键值对</td>
</tr>
</tbody></table>
<hr>
<h3 id="散列表实现映射的复杂度"><a href="#散列表实现映射的复杂度" class="headerlink" title="散列表实现映射的复杂度"></a>散列表实现映射的复杂度</h3><table>
<thead>
<tr>
<th>操作</th>
<th align="right">平均复杂度</th>
<th align="right">最坏复杂度</th>
</tr>
</thead>
<tbody><tr>
<td>插入 <code>put</code></td>
<td align="right"><code>O(1)</code></td>
<td align="right"><code>O(n)</code></td>
</tr>
<tr>
<td>查询 <code>get</code></td>
<td align="right"><code>O(1)</code></td>
<td align="right"><code>O(n)</code></td>
</tr>
<tr>
<td>判断键存在</td>
<td align="right"><code>O(1)</code></td>
<td align="right"><code>O(n)</code></td>
</tr>
<tr>
<td>删除</td>
<td align="right"><code>O(1)</code></td>
<td align="right"><code>O(n)</code></td>
</tr>
</tbody></table>
<p>最坏情况通常出现在：</p>
<ul>
<li>大量键发生冲突；</li>
<li>散列函数分布不均；</li>
<li>负载因子过高；</li>
<li>冲突解决效率较低。</li>
</ul>
<hr>
<h1 id="整体关系"><a href="#整体关系" class="headerlink" title="整体关系"></a>整体关系</h1><table>
<thead>
<tr>
<th>概念</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>映射 <code>Map</code></td>
<td>规定键值对结构及其操作</td>
</tr>
<tr>
<td>字典 <code>dict</code></td>
<td>Python 提供的映射类型</td>
</tr>
<tr>
<td>散列表</td>
<td>实现映射的一种数据结构</td>
</tr>
<tr>
<td>散列函数</td>
<td>将键转换为槽下标</td>
</tr>
<tr>
<td>冲突</td>
<td>不同键映射到同一槽</td>
</tr>
<tr>
<td>冲突解决方案</td>
<td>决定冲突数据如何存储和查找</td>
</tr>
<tr>
<td>负载因子</td>
<td>衡量散列表的填充程度</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>散列表</tag>
        <tag>哈希表</tag>
      </tags>
  </entry>
  <entry>
    <title>线性结构：栈、队列、双端队列与列表</title>
    <url>/2026/08/25/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E7%BA%BF%E6%80%A7%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[<h1 id="线性结构概述"><a href="#线性结构概述" class="headerlink" title="线性结构概述"></a>线性结构概述</h1><p>线性结构中的数据元素按照一定顺序排列，元素之间存在明确的前后关系。</p>
<p>除首尾元素外，每个元素通常都有：</p>
<ul>
<li>唯一的前驱元素；</li>
<li>唯一的后继元素。</li>
</ul>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">元素1 → 元素2 → 元素3 → 元素4</span><br></pre></td></tr></table></figure>

<p>栈、队列和双端队列都可以看作对列表操作位置进行限制后形成的数据结构。</p>
<table>
<thead>
<tr>
<th align="center">结构</th>
<th align="center">允许操作的位置</th>
<th align="center">核心规则</th>
</tr>
</thead>
<tbody><tr>
<td align="center">列表</td>
<td align="center">任意位置</td>
<td align="center">按位置组织元素</td>
</tr>
<tr>
<td align="center">栈</td>
<td align="center">同一端</td>
<td align="center">后进先出(LIFO)</td>
</tr>
<tr>
<td align="center">队列</td>
<td align="center">两端分工</td>
<td align="center">先进先出(FIFO)</td>
</tr>
<tr>
<td align="center">双端队列</td>
<td align="center">两端均可操作</td>
<td align="center">两端进、两端出</td>
</tr>
</tbody></table>
<hr>
<h1 id="栈-Stack"><a href="#栈-Stack" class="headerlink" title="栈 Stack"></a>栈 Stack</h1><h2 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h2><p>栈只允许在同一端添加和删除元素，这一端称为<strong>栈顶</strong>。</p>
<p>遵循：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">LIFO：Last In, First Out</span><br><span class="line">后进先出</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">入栈：A → B → C</span><br><span class="line">出栈：C → B → A</span><br></pre></td></tr></table></figure>

<h2 id="抽象数据类型"><a href="#抽象数据类型" class="headerlink" title="抽象数据类型"></a>抽象数据类型</h2><table>
<thead>
<tr>
<th align="center">操作</th>
<th align="center">含义</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>push(item)</code></td>
<td align="center">将元素压入栈顶</td>
</tr>
<tr>
<td align="center"><code>pop()</code></td>
<td align="center">删除并返回栈顶元素</td>
</tr>
<tr>
<td align="center"><code>peek()</code></td>
<td align="center">查看栈顶元素，不删除</td>
</tr>
<tr>
<td align="center"><code>isEmpty()</code></td>
<td align="center">判断栈是否为空</td>
</tr>
<tr>
<td align="center"><code>size()</code></td>
<td align="center">返回栈中元素数量</td>
</tr>
</tbody></table>
<h2 id="Python-实现"><a href="#Python-实现" class="headerlink" title="Python 实现"></a>Python 实现</h2><p>Python 列表的尾部适合模拟栈：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">stack = []</span><br><span class="line"></span><br><span class="line">stack.append(<span class="number">1</span>)   <span class="comment"># 入栈</span></span><br><span class="line">stack.append(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">top = stack[-<span class="number">1</span>]   <span class="comment"># 查看栈顶</span></span><br><span class="line">item = stack.pop()  <span class="comment"># 出栈</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> stack:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;栈为空&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>使用列表尾部进行 <code>append()</code> 和 <code>pop()</code>，时间复杂度通常都是 <code>O(1)</code>。</p>
<h2 id="常见应用"><a href="#常见应用" class="headerlink" title="常见应用"></a>常见应用</h2><ul>
<li>括号匹配；</li>
<li>表达式求值；</li>
<li>浏览器后退；</li>
<li>函数调用栈；</li>
<li>深度优先搜索；</li>
<li>单调栈。</li>
</ul>
<hr>
<h1 id="队列-Queue"><a href="#队列-Queue" class="headerlink" title="队列 Queue"></a>队列 Queue</h1><h2 id="定义-1"><a href="#定义-1" class="headerlink" title="定义"></a>定义</h2><p>队列从一端加入元素，从另一端删除元素。</p>
<ul>
<li>加入元素的一端：队尾；</li>
<li>删除元素的一端：队首。</li>
</ul>
<p>遵循：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">FIFO：First In, First Out</span><br><span class="line">先进先出</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">入队：A → B → C</span><br><span class="line">出队：A → B → C</span><br></pre></td></tr></table></figure>

<h2 id="抽象数据类型-1"><a href="#抽象数据类型-1" class="headerlink" title="抽象数据类型"></a>抽象数据类型</h2><table>
<thead>
<tr>
<th align="center">操作</th>
<th align="center">含义</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>enqueue(item)</code></td>
<td align="center">从队尾加入元素</td>
</tr>
<tr>
<td align="center"><code>dequeue()</code></td>
<td align="center">从队首删除并返回元素</td>
</tr>
<tr>
<td align="center"><code>isEmpty()</code></td>
<td align="center">判断队列是否为空</td>
</tr>
<tr>
<td align="center"><code>size()</code></td>
<td align="center">返回队列长度</td>
</tr>
</tbody></table>
<h2 id="Python-实现-1"><a href="#Python-实现-1" class="headerlink" title="Python 实现"></a>Python 实现</h2><p>推荐使用 <code>collections.deque</code>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> deque</span><br><span class="line"></span><br><span class="line">queue = deque()</span><br><span class="line"></span><br><span class="line">queue.append(<span class="number">1</span>)      <span class="comment"># 队尾入队</span></span><br><span class="line">queue.append(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">item = queue.popleft()  <span class="comment"># 队首出队</span></span><br></pre></td></tr></table></figure>

<p>不推荐频繁使用：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">queue = []</span><br><span class="line">queue.pop(<span class="number">0</span>)</span><br></pre></td></tr></table></figure>

<p>因为列表删除第一个元素后，后面的元素都需要向前移动，时间复杂度为 <code>O(n)</code>。</p>
<h2 id="常见应用-1"><a href="#常见应用-1" class="headerlink" title="常见应用"></a>常见应用</h2><ul>
<li>任务排队；</li>
<li>消息处理；</li>
<li>打印任务；</li>
<li>广度优先搜索；</li>
<li>层序遍历；</li>
<li>生产者—消费者模型。</li>
</ul>
<hr>
<h1 id="双端队列-Deque"><a href="#双端队列-Deque" class="headerlink" title="双端队列 Deque"></a>双端队列 Deque</h1><h2 id="定义-2"><a href="#定义-2" class="headerlink" title="定义"></a>定义</h2><p>双端队列允许在队首和队尾两端添加、删除元素。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">appendleft                appendright</span><br><span class="line">     ↓                         ↓</span><br><span class="line">    左端 [ A, B, C, D, E, F ]  右端</span><br><span class="line">     ↑                         ↑</span><br><span class="line">  popleft                   popright</span><br></pre></td></tr></table></figure>

<p>它同时具备栈和队列的部分功能。</p>
<h2 id="主要操作"><a href="#主要操作" class="headerlink" title="主要操作"></a>主要操作</h2><table>
<thead>
<tr>
<th align="center">操作</th>
<th align="center">含义</th>
</tr>
</thead>
<tbody><tr>
<td align="center"><code>append(item)</code></td>
<td align="center">右端加入</td>
</tr>
<tr>
<td align="center"><code>appendleft(item)</code></td>
<td align="center">左端加入</td>
</tr>
<tr>
<td align="center"><code>pop()</code></td>
<td align="center">右端删除</td>
</tr>
<tr>
<td align="center"><code>popleft()</code></td>
<td align="center">左端删除</td>
</tr>
</tbody></table>
<h2 id="Python-实现-2"><a href="#Python-实现-2" class="headerlink" title="Python 实现"></a>Python 实现</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> deque</span><br><span class="line"></span><br><span class="line">q = deque()</span><br><span class="line"></span><br><span class="line">q.append(<span class="number">1</span>)       <span class="comment"># 右边加入</span></span><br><span class="line">q.appendleft(<span class="number">2</span>)   <span class="comment"># 左边加入</span></span><br><span class="line"></span><br><span class="line">q.pop()           <span class="comment"># 右边删除</span></span><br><span class="line">q.popleft()       <span class="comment"># 左边删除</span></span><br></pre></td></tr></table></figure>

<p><code>deque</code> 两端的添加和删除通常都是 <code>O(1)</code>。</p>
<h2 id="常见应用-2"><a href="#常见应用-2" class="headerlink" title="常见应用"></a>常见应用</h2><ul>
<li>普通队列；</li>
<li>栈；</li>
<li>回文判断；</li>
<li>滑动窗口；</li>
<li>单调队列。</li>
</ul>
<p>滑动窗口最大值中：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">q.append(i)       <span class="comment"># 队尾加入当前下标</span></span><br><span class="line">q.pop()           <span class="comment"># 删除队尾无用元素</span></span><br><span class="line">q.popleft()       <span class="comment"># 删除队首过期元素</span></span><br></pre></td></tr></table></figure>
<hr>
<h1 id="列表-List"><a href="#列表-List" class="headerlink" title="列表 List"></a>列表 List</h1><h2 id="定义-3"><a href="#定义-3" class="headerlink" title="定义"></a>定义</h2><p>列表是一组按照位置排列的数据元素。<br>与栈和队列不同，列表通常允许在任意位置：</p>
<ul>
<li>添加元素；</li>
<li>删除元素；</li>
<li>查询元素；</li>
<li>修改元素；</li>
<li>遍历元素。</li>
</ul>
<h2 id="常见操作"><a href="#常见操作" class="headerlink" title="常见操作"></a>常见操作</h2><table>
<thead>
<tr>
<th>操作</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>add(item)</code></td>
<td>添加元素</td>
</tr>
<tr>
<td><code>remove(item)</code></td>
<td>删除元素</td>
</tr>
<tr>
<td><code>search(item)</code></td>
<td>查找元素</td>
</tr>
<tr>
<td><code>isEmpty()</code></td>
<td>判断是否为空</td>
</tr>
<tr>
<td><code>size()</code></td>
<td>获取元素数量</td>
</tr>
<tr>
<td><code>append(item)</code></td>
<td>在末尾添加元素</td>
</tr>
<tr>
<td><code>insert(index, item)</code></td>
<td>在指定位置插入元素</td>
</tr>
<tr>
<td><code>pop(index)</code></td>
<td>删除指定位置元素</td>
</tr>
<tr>
<td><code>index(item)</code></td>
<td>查找元素的位置</td>
</tr>
</tbody></table>
<h2 id="Python-列表"><a href="#Python-列表" class="headerlink" title="Python 列表"></a>Python 列表</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">nums = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"></span><br><span class="line">nums.append(<span class="number">4</span>)       <span class="comment"># 末尾添加</span></span><br><span class="line">nums.insert(<span class="number">1</span>, <span class="number">10</span>)   <span class="comment"># 指定位置插入</span></span><br><span class="line">nums.pop()           <span class="comment"># 删除末尾元素</span></span><br><span class="line">nums.pop(<span class="number">0</span>)          <span class="comment"># 删除第一个元素</span></span><br><span class="line">nums.remove(<span class="number">2</span>)       <span class="comment"># 删除第一个值为 2 的元素</span></span><br></pre></td></tr></table></figure>

<p>Python 的 <code>list</code> 底层属于动态数组，因此：</p>
<ul>
<li>下标访问速度快；</li>
<li>尾部添加、删除速度快；</li>
<li>首部或中间插入、删除速度较慢。</li>
</ul>
<hr>
<h1 id="Python-常用操作复杂度"><a href="#Python-常用操作复杂度" class="headerlink" title="Python 常用操作复杂度"></a>Python 常用操作复杂度</h1><table>
<thead>
<tr>
<th>操作</th>
<th align="right">时间复杂度</th>
</tr>
</thead>
<tbody><tr>
<td><code>list[index]</code></td>
<td align="right"><code>O(1)</code></td>
</tr>
<tr>
<td><code>list.append(x)</code></td>
<td align="right">平均 <code>O(1)</code></td>
</tr>
<tr>
<td><code>list.pop()</code></td>
<td align="right"><code>O(1)</code></td>
</tr>
<tr>
<td><code>list.insert(0, x)</code></td>
<td align="right"><code>O(n)</code></td>
</tr>
<tr>
<td><code>list.pop(0)</code></td>
<td align="right"><code>O(n)</code></td>
</tr>
<tr>
<td><code>x in list</code></td>
<td align="right"><code>O(n)</code></td>
</tr>
<tr>
<td><code>deque.append(x)</code></td>
<td align="right"><code>O(1)</code></td>
</tr>
<tr>
<td><code>deque.appendleft(x)</code></td>
<td align="right"><code>O(1)</code></td>
</tr>
<tr>
<td><code>deque.pop()</code></td>
<td align="right"><code>O(1)</code></td>
</tr>
<tr>
<td><code>deque.popleft()</code></td>
<td align="right"><code>O(1)</code></td>
</tr>
</tbody></table>
<hr>
<h1 id="四种结构对比"><a href="#四种结构对比" class="headerlink" title="四种结构对比"></a>四种结构对比</h1><table>
<thead>
<tr>
<th align="center">数据结构</th>
<th align="center">操作规则</th>
<th align="center">Python 常用实现</th>
<th align="center">典型应用</th>
</tr>
</thead>
<tbody><tr>
<td align="center">栈</td>
<td align="center">后进先出</td>
<td align="center"><code>list</code></td>
<td align="center">括号匹配、DFS、单调栈</td>
</tr>
<tr>
<td align="center">队列</td>
<td align="center">先进先出</td>
<td align="center"><code>deque</code></td>
<td align="center">BFS、任务调度</td>
</tr>
<tr>
<td align="center">双端队列</td>
<td align="center">两端均可进出</td>
<td align="center"><code>deque</code></td>
<td align="center">滑动窗口、单调队列</td>
</tr>
<tr>
<td align="center">列表</td>
<td align="center">可按位置操作</td>
<td align="center"><code>list</code></td>
<td align="center">数据存储、随机访问</td>
</tr>
</tbody></table>
<hr>
<h1 id="选择原则"><a href="#选择原则" class="headerlink" title="选择原则"></a>选择原则</h1><p>需要“后进先出”时，使用栈：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">stack = []</span><br></pre></td></tr></table></figure>

<p>需要“先进先出”时，使用队列：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> deque</span><br><span class="line">queue = deque()</span><br></pre></td></tr></table></figure>

<p>需要在两端高效操作时，使用双端队列：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">q = deque()</span><br></pre></td></tr></table></figure>

<p>需要按下标访问元素时，使用列表：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">nums = []</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>栈</tag>
        <tag>队列</tag>
        <tag>双端队列</tag>
      </tags>
  </entry>
</search>
